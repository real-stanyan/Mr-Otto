# Mr Otto 登录系统 v1 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 自托管 Supabase(第二套栈)+ Otto 客户端 Google/GitHub OAuth 登录,登录态只存主进程。

**Architecture:** 服务器起独立 Supabase 栈(Kong 只绑 127.0.0.1:8100),nginx 只反代 `/auth/v1/` 并上 Let's Encrypt;客户端主进程用 supabase-js(PKCE)+ 系统浏览器 + `mrotto://` 深链回调,token 落 userData 0600 文件,渲染层经 ShellBridge 只见 profile。

**Tech Stack:** Supabase self-hosted (docker compose) / nginx + certbot / @supabase/supabase-js(仅主进程)/ Electron deep link / vitest

**规范来源:** docs/superpowers/specs/2026-08-17-login-design.md;任务 issue #1。

## Global Constraints

- 服务器:`ssh stan@65.109.113.168 -p 2222`,域名 `otto-auth.stan.damianslife.com`(已解析)
- 端口:Kong `127.0.0.1:8100`;pooler `127.0.0.1:5433/6544`;不与 dryrun(8000/5432/6543)冲突;一切 supabase 端口只绑 127.0.0.1
- 公网只暴露 `https://otto-auth.stan.damianslife.com/auth/v1/*`,其余路径 404;Studio 不出公网
- client secret / service_role key / JWT secret 只存服务器 `~/otto-supabase/docker/.env`,永不进 git、不进聊天
- anon key + URL 可进客户端代码(设计如此,配合 RLS)
- token 永不回流渲染层;渲染层只见 `{ signedIn, email, name, avatarUrl }`
- 登录不落 SessionEvent(非 model-visible)
- 客户端代码走分支 `feat/login` + PR,merge commit,PR 关 #1;Gate = `npm test`
- 深链主方案 `mrotto://auth-callback`;备胎 loopback `http://127.0.0.1:43110/callback`(两个都进 GoTrue 允许列表,macOS dev 下深链注册若不可靠即切备胎)

---

### Task 1: 服务器——Supabase 第二套栈起起来

**Files:**
- Create: `deploy/otto-auth/gen-keys.mjs`(本地跑,生成密钥打印,不落盘)
- Create: `deploy/otto-auth/compose.override.yml`(repo 内存档,scp 到服务器)
- Create: `deploy/otto-auth/README.md`(部署步骤存档)

**Interfaces:**
- Produces: 服务器 `~/otto-supabase/docker/` 可用栈;`curl http://127.0.0.1:8100/auth/v1/health` 返回 JSON

- [ ] **Step 1: 本地写密钥生成脚本**(node:crypto 手搓 HS256 JWT,输出 POSTGRES_PASSWORD/JWT_SECRET/ANON_KEY/SERVICE_ROLE_KEY/DASHBOARD_PASSWORD,只打印到 stdout)

```js
// deploy/otto-auth/gen-keys.mjs
import { randomBytes, createHmac } from "node:crypto";
const b64u = (buf) => Buffer.from(buf).toString("base64url");
const jwt = (payload, secret) => {
  const h = b64u(JSON.stringify({ alg: "HS256", typ: "JWT" }));
  const p = b64u(JSON.stringify(payload));
  const s = createHmac("sha256", secret).update(`${h}.${p}`).digest("base64url");
  return `${h}.${p}.${s}`;
};
const now = Math.floor(Date.now() / 1000);
const exp = now + 10 * 365 * 24 * 3600;
const jwtSecret = randomBytes(32).toString("hex");
const claims = (role) => ({ role, iss: "supabase", iat: now, exp });
console.log("POSTGRES_PASSWORD=" + randomBytes(24).toString("hex"));
console.log("JWT_SECRET=" + jwtSecret);
console.log("ANON_KEY=" + jwt(claims("anon"), jwtSecret));
console.log("SERVICE_ROLE_KEY=" + jwt(claims("service_role"), jwtSecret));
console.log("DASHBOARD_PASSWORD=" + randomBytes(16).toString("hex"));
```

- [ ] **Step 2: 服务器拉 supabase/docker**:`git clone --depth 1 https://github.com/supabase/supabase ~/otto-supabase-src && cp -r ~/otto-supabase-src/docker ~/otto-supabase/docker`(目录名以实际 checkout 为准),`cp .env.example .env`
- [ ] **Step 3: 改 `.env`**:填入 Step 1 生成的值;`SITE_URL=https://otto-auth.stan.damianslife.com`、`API_EXTERNAL_URL=https://otto-auth.stan.damianslife.com`、`ADDITIONAL_REDIRECT_URLS=mrotto://auth-callback,http://127.0.0.1:43110/callback`、`KONG_HTTP_PORT=8100`、`POOLER_PROXY_PORT_TRANSACTION=6544`、`DISABLE_SIGNUP=false`、`ENABLE_EMAIL_SIGNUP=false`、`ENABLE_ANONYMOUS_USERS=false`(键名以 .env.example 实际为准,对不上的写进 compose override 的 auth 服务 environment)
- [ ] **Step 4: compose override 锁端口到 127.0.0.1**(kong 8100、pooler 5433/6544;studio 不加 ports)。写 `deploy/otto-auth/compose.override.yml` 后 scp 到服务器
- [ ] **Step 5: `docker compose -p otto up -d`**,确认与 dryrun 容器共存、无端口冲突
- [ ] **Step 6: 健康检查**:服务器上 `curl -s http://127.0.0.1:8100/auth/v1/health`(带 anon apikey 头),预期 GoTrue 版本 JSON
- [ ] **Step 7: commit**(repo 内只有脚本/override/README,无密钥)

### Task 2: 服务器——nginx vhost + TLS,只放行 /auth/v1/

**Files:**
- Create: `deploy/otto-auth/nginx-otto-auth.conf`(存档,scp 到 `/etc/nginx/sites-available/otto-auth`)

**Interfaces:**
- Produces: `curl https://otto-auth.stan.damianslife.com/auth/v1/health` 公网可达;其余路径 404

- [ ] **Step 1: 写 vhost**

```nginx
server {
    listen 80;
    server_name otto-auth.stan.damianslife.com;
    location /auth/v1/ {
        proxy_pass http://127.0.0.1:8100;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
    location / { return 404; }
}
```

- [ ] **Step 2: scp + `ln -s` 启用 + `nginx -t` + reload**
- [ ] **Step 3: certbot**:`sudo apt-get install -y certbot python3-certbot-nginx`(已装则跳过)→ `sudo certbot --nginx -d otto-auth.stan.damianslife.com`(自动改写 vhost 上 443 + 80 跳转)
- [ ] **Step 4: 验证**:本机 `curl -s https://otto-auth.stan.damianslife.com/auth/v1/health` 返回 JSON;`curl -s -o /dev/null -w "%{http_code}" https://otto-auth.stan.damianslife.com/` 得 404
- [ ] **Step 5: commit** 存档 conf

### Task 3: 服务器——GoTrue 挂 Google/GitHub + profiles 表(用户杂务门)

**Files:**
- Modify: 服务器 `~/otto-supabase/docker/.env`(用户亲手粘 client id/secret)
- Create: `deploy/otto-auth/profiles.sql`

**Interfaces:**
- Consumes: 用户在 Google Cloud Console / GitHub 建好 OAuth app(回调均填 `https://otto-auth.stan.damianslife.com/auth/v1/callback`)
- Produces: `GET /auth/v1/authorize?provider=google` 302 到 Google;github 同理;`public.profiles` 表 + RLS + 注册自动建行触发器

- [ ] **Step 1: 用户杂务**(等待用户完成,给出精确点击路径):Google OAuth client(Web application)+ GitHub OAuth App,凭证 SSH 粘进 `.env`:`GOTRUE_EXTERNAL_GOOGLE_ENABLED=true` / `GOTRUE_EXTERNAL_GOOGLE_CLIENT_ID=...` / `GOTRUE_EXTERNAL_GOOGLE_SECRET=...` / `GOTRUE_EXTERNAL_GOOGLE_REDIRECT_URI=https://otto-auth.stan.damianslife.com/auth/v1/callback`;GITHUB 四件套同构(键名若 .env.example 用 `ENABLE_GOOGLE_SIGNUP` 风格则以其为准,必要时写 compose override 的 auth environment)
- [ ] **Step 2: `docker compose -p otto up -d auth`** 重建 auth 容器
- [ ] **Step 3: 验证 302**:`curl -s -o /dev/null -w "%{http_code} %{redirect_url}" "https://otto-auth.stan.damianslife.com/auth/v1/authorize?provider=google"` 预期 302 到 accounts.google.com;github 同理到 github.com/login
- [ ] **Step 4: profiles.sql**(psql 进 db 容器执行)

```sql
create table if not exists public.profiles (
  id uuid primary key references auth.users(id) on delete cascade,
  display_name text,
  avatar_url text,
  created_at timestamptz not null default now()
);
alter table public.profiles enable row level security;
create policy "own profile read"  on public.profiles for select using (auth.uid() = id);
create policy "own profile write" on public.profiles for update using (auth.uid() = id);
create or replace function public.handle_new_user() returns trigger
language plpgsql security definer set search_path = public as $$
begin
  insert into public.profiles (id, display_name, avatar_url)
  values (new.id,
          coalesce(new.raw_user_meta_data->>'name', new.raw_user_meta_data->>'user_name'),
          new.raw_user_meta_data->>'avatar_url')
  on conflict (id) do nothing;
  return new;
end $$;
drop trigger if exists on_auth_user_created on auth.users;
create trigger on_auth_user_created after insert on auth.users
  for each row execute function public.handle_new_user();
```

- [ ] **Step 5: commit** profiles.sql + README 更新

### Task 4: 客户端——auth 存储适配器(TDD)

**Files:**
- Create: `src/main/authStorage.ts`
- Test: `tests/main/authStorage.test.ts`
- 分支:`git checkout -b feat/login`;`npm i @supabase/supabase-js`

**Interfaces:**
- Produces: `createAuthStorage(filePath: string, io?: AuthStorageIO)`,返回 supabase-js 自定义 storage `{ getItem(key): string|null; setItem(key, value): void; removeItem(key): void }`;`AuthStorageIO = { read(p): string|null; write(p, data): void; remove(p): void }`(write 落 0600,单文件 JSON,多 key 合并存)

- [ ] **Step 1: 失败测试**:注入假 io——setItem 后 getItem 取回;removeItem 后 getItem 为 null;文件不存在 getItem 为 null;write 收到 mode 0600 的语义(nodeIO 用 `writeFileSync(p, data, { mode: 0o600 })`,测试断言假 io 收到序列化 JSON);坏 JSON 当空处理
- [ ] **Step 2: 跑测确认红**(`npx vitest run tests/main/authStorage.test.ts`)
- [ ] **Step 3: 实现**(照 keyVault 注入 fs 模式)
- [ ] **Step 4: 跑测绿**
- [ ] **Step 5: commit**

### Task 5: 客户端——AccountManager(TDD)

**Files:**
- Create: `src/main/account.ts`
- Create: `src/main/authConfig.ts`(`SUPABASE_URL = "https://otto-auth.stan.damianslife.com"`、`SUPABASE_ANON_KEY = "<Task 1 生成的 anon key>"`——anon key 公开无害)
- Test: `tests/main/account.test.ts`

**Interfaces:**
- Produces:
  - `parseAuthCallback(url: string): string | null`(从 `mrotto://auth-callback?code=x` 或 loopback URL 提取 code;非回调 URL 回 null)
  - `toAccountInfo(user: { email?: string; user_metadata?: Record<string, unknown> } | null): AccountInfo`;`AccountInfo = { signedIn: boolean; email: string; name: string; avatarUrl: string }`(name 取 metadata.name ?? user_name ?? email 前缀;avatar 取 avatar_url ?? picture ?? "")
  - `class AccountManager { constructor(deps: { openExternal(url: string): void; onChange(info: AccountInfo): void; client?: SupabaseLike }); signIn(provider: "google" | "github"): Promise<void>; handleCallback(url: string): Promise<void>; signOut(): Promise<void>; getAccount(): AccountInfo }`——signIn 调 supabase `signInWithOAuth({ provider, options: { redirectTo: "mrotto://auth-callback", skipBrowserRedirect: true } })` 拿 URL 后 `openExternal`;handleCallback 走 `exchangeCodeForSession`;`SupabaseLike` 最小接口便于注入假 client
- Consumes: Task 4 的 `createAuthStorage`(真 client 初始化:`createClient(url, key, { auth: { flowType: "pkce", persistSession: true, autoRefreshToken: true, detectSessionInUrl: false, storage } })`)

- [ ] **Step 1: 失败测试**:parseAuthCallback 三态(深链带 code / loopback 带 code / 无 code 或错 scheme 回 null);toAccountInfo(Google 形态 metadata、GitHub 形态、null 用户);AccountManager 注入假 client——signIn 后 openExternal 收到 provider URL;handleCallback 后 onChange 收到 signedIn=true 的 info;signOut 后 onChange 收到 signedIn=false
- [ ] **Step 2: 红** → **Step 3: 实现** → **Step 4: 绿**
- [ ] **Step 5: commit**

### Task 6: 客户端——主进程接线(深链 + IPC + Bridge)

**Files:**
- Modify: `src/shared/shellBridge.ts`(+`AccountInfo`;ShellBridge +`getAccount(): Promise<AccountInfo>`、`signIn(provider: "google" | "github"): Promise<void>`、`signOut(): Promise<void>`;CHANNELS +`getAccount/signIn/signOut/accountChanged`)
- Modify: `src/preload/index.ts`(expose 三方法 + `onAccountChanged(cb)` 订阅)
- Modify: `src/main/index.ts`

**Interfaces:**
- Consumes: Task 5 `AccountManager`、`parseAuthCallback`
- Produces: 渲染层可用 `bridge.getAccount()/signIn()/signOut()` + `accountChanged` 推送

- [ ] **Step 1: shellBridge/preload 加接口**(照 listSkills 模式)
- [ ] **Step 2: main/index.ts**:启动时 `app.setAsDefaultProtocolClient("mrotto")`(dev 下 macOS 传 execPath 无益,直接单参调用);`app.on("open-url")` → `accountManager.handleCallback(url)`;二级保险 `app.requestSingleInstanceLock` 非必需(macOS open-url 直达运行实例);ipcMain.handle 三条 + onChange 时 `win.webContents.send(CHANNELS.accountChanged, info)`
- [ ] **Step 3: `npx tsc --noEmit` + 全量 `npm test` 绿**
- [ ] **Step 4: commit**

### Task 7: 客户端——设置页账号区 UI

**Files:**
- Modify: `src/renderer/src/store.ts`(state +`account: AccountInfo`;boot 时 getAccount;订阅 accountChanged;actions `signIn/signOut`)
- Modify: `src/renderer/src/App.tsx`(Settings 顶部加「账号」区:未登录 = 两个按钮「用 Google 登录」「用 GitHub 登录」+ 一行说明"登录后可在多台设备同步配置(即将上线)";已登录 = 头像(img,无则首字母圆片)+ name + email + 「退出登录」)
- Modify: `src/renderer/src/app.css`(`.account-row`、`.account-avatar`(28px 圆)、按钮沿用现有 ghost 风格;遵循 emil/apple 指南:按钮 :active scale(0.97),无新动画)

**Interfaces:**
- Consumes: Task 6 bridge 接口

- [ ] **Step 1: store 接线** → **Step 2: UI + css** → **Step 3: `npx tsc --noEmit` + `npm test` 绿**
- [ ] **Step 4: commit**

### Task 8: e2e 验收 + 收尾

- [ ] **Step 1: 重启 app**(主进程改了):pkill 旧 Electron → `npm run dev` 后台
- [ ] **Step 2: 真人过一遍**:设置页点 Google 登录 → 浏览器授权 → 跳回 app 显示头像;GitHub 同理;退出登录;重启 app 仍保持登录(token 落盘生效)。若 macOS dev 深链不唤起:切备胎——AccountManager redirectTo 换 `http://127.0.0.1:43110/callback` + 主进程起临时 http server 收 code(allow list 已含),记录进 README
- [ ] **Step 3: 检查 `userData/auth.json` 权限 0600;确认渲染层 devtools 里摸不到 token**
- [ ] **Step 4: Gate `npm test` 全绿;PR(feat/login → main,merge commit,body 关 #1)**
- [ ] **Step 5: quiz 追加一题(登录态为什么不进事件日志)+ docs commit**

## Self-Review 结论

- spec 覆盖:栈/nginx/TLS/provider/profiles/RLS/深链/PKCE/存储/Bridge/UI/测试/杂务——每节有对应 Task(1-8)✓
- 无占位符;备胎方案(loopback)在 Global Constraints + Task 8 落地 ✓
- 类型一致:`AccountInfo` 在 Task 5 定义、6/7 消费同名 ✓
