{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    // #1356 A1：mobile/ 有自己的 package.json 与 node_modules（AGENTS.md 门禁一节），
    // @supabase/supabase-js 在它那里与仓库根各装了一份。src/shared/ 里的
    // supabaseWorkspacesApi.ts / homeWorkspace.ts / agentAdmin.ts 只写
    // `import type { SupabaseClient }`，TS 从它们自己的目录往上找，解到根那份声明；
    // mobile/src/supabase.ts 的 createClient() 解到的是 mobile 那份。SupabaseClient
    // 带 protected 的 supabaseUrl——TS 对带 private/protected 成员的类按「是不是同一处
    // 声明」判兼容，两份物理上不同的 .d.ts 天生判不兼容：把 mobile 建出来的客户端递给
    // 这些函数就报 TS2345。
    //
    // 指到 mobile 那份、不指根：手机上真正跑的客户端实例是 mobile/src/supabase.ts 建的
    // （那个文件在 mobile/ 里，解出来的是 mobile 这份），shared 那几个函数调的正是它的
    // 方法——tsc 该拿这份去核 shared 的调用。指根的话，根单独升一次 supabase-js、mobile
    // 没跟着升，mobile tsc 照样绿，而手机上那个客户端可能没有 shared 新用到的方法。
    //
    // 别拿 metro.config.js 的 nodeModulesPaths 当理由：Metro 先从发起 import 的那个文件
    // 所在目录逐级往上找 node_modules，nodeModulesPaths 只是找不到时的后备——src/shared/
    // 里的文件若真在运行时 import 这个包，打进手机包的会是根那份。今天没有这种 import
    // （上面三处都是 import type，打包时整行抹掉）；哪天有了，这条重定向就不再和打包的
    // 结果一致，得重新判。只影响类型解析，Metro 不读 tsconfig 的 paths。
    "paths": {
      "@supabase/supabase-js": ["./node_modules/@supabase/supabase-js"]
    }
  }
}
