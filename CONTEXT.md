# Domain context — Mr Otto

Domain glossary. All agents' understanding of domain terms is grounded here; code naming stays consistent with the terms defined here.

## 协议术语（Gearbox）

读规则的人要查的词。来源是 Gearbox 协议 + 本仓的 ADR。

| Term | Definition | Notes |
|---|---|---|
| single source of truth | Rules are written in exactly one place (`AGENTS.md`); other agent configs (e.g. `CLAUDE.md`) only `@`-reference it, never copy it | Prevents rules from drifting across multiple locations |
| empty-shell contract | `CLAUDE.md`'s content is exactly one line, `@AGENTS.md` — a physical guarantee that Claude Code and Z Code read the same rules | The structural self-check script asserts this |
| handoff | One agent passes a task to another agent — **this only happens the moment an issue closes / a PR merges**, never mid-task | It isn't a handoff just because things were "explained clearly" — it's a handoff only when the issue closes |
| protocol gap | A question the repo's persistent artifacts (AGENTS.md / ADR / CONTEXT.md) can't answer | Hitting one requires opening an issue — silent judgment calls are not allowed |
| The three issue roles | The three non-overlapping uses of issues/PRs in this protocol: **Task** / **Memory** (handoff memory) / **Protocol gap** | Every issue should fall into exactly one of these — see AGENTS.md |
| 一次性 worktree (single-use worktree) | 一个 worktree 只服务一个任务、用完即弃、从不 `git checkout` 换活；开工都在它里面，主 checkout 冻结在默认分支只读 | ADR-0149。会切分支的长期 worktree 等于第二个主 checkout——隔离来自「一次性」，不来自「有 worktree」 |
| gate | The command that must be all-green before ending a shift. See the Gate section in AGENTS.md for this repo's command | CI runs the same command — red means no merge |
| L1/L2 tiers | Two authorization tiers for protocol changes: **L1 strict tier** (Hard rules / Gate / Tech stack / the "Changing the protocol itself" section itself) requires explicit maintainer agreement before merging; **L2 autonomous tier** (the rest of Working agreement / indexes) the agent can merge on its own | ADR-0006; boundary criteria in ADR-0012 |
| Mechanism reference (criterion) | Any new content that references L1/L2, Hard rules, Working agreement, or other protocol mechanisms (by keyword or semantic dependency) is treated as L1 | ADR-0012, "mechanism reference takes priority"; guards against using "optional + pure addition" as an L2 loophole to expand the protocol |
| Memory five-part format | The minimum valid format for a handoff comment: ① what's done ② what's blocked ③ what's next ④ close the issue if the task is complete ⑤ rationale/trade-offs (write "none" if no decision was made) | ADR-0004; missing any item makes the handoff invalid |
| terminal shift | The form a shift ends in when archiving / confirming there's no next shift: a handoff issue may be skipped, but the last closed issue must explicitly declare "no next shift" + a reason. Repo-level, not lane-level — invalid while another lane is still live | ADR-0009; a silent ending doesn't count as terminal; repo-level scope per ADR-0048 |
| blocking edge | A literal `Blocked by: #N` line in a dependent Task issue's body, declaring one prerequisite Task per line | ADR-0044; a hygiene convention — a stale edge costs a judgment call, not a violation |
| frontier task | An open Task issue with no open blockers — the only kind of task a shift may claim; when a blocker closes, its dependents join the frontier | ADR-0044 |
| claim | Self-assignment on a Task issue (`gh issue edit <N> --add-assignee @me`), first wins; a "claiming this" comment where assignment isn't possible. An open frontier task with no assignee and no claim comment is free | ADR-0047; single-human repos may skip — the value begins at the second human |
| lane | One shift plus the tasks it has claimed; parallel shifts are allowed iff lanes are disjoint (each works only on frontier tasks it claimed) | ADR-0048; handoff issues are per-lane |
| context-only handoff | A handoff issue whose lane finished with nothing to transfer — kept for its Memory comment, closed by its first reader after reading. **Closing one is not taking over that lane**: the reader still owes its own handoff, or a terminal declaration | ADR-0048; ADR-0069 (closing ≠ takeover) |
| downstream | A project that copies this Gearbox protocol and then evolves independently; sync status is self-checked downstream via `gearbox-version` (pull-primary, ADR-0026 — the upstream fleet dashboard was retired in ADR-0033) | See ADR-0026 |
| backfill | Downstream pulls Gearbox protocol improvements into its local copy; **pull-triggered** — downstream runs `gearbox-version` at the start of a shift to self-check, and `gearbox-update` if it's behind, with no dependency on upstream pushing; it's alignment, not enforcement — downstream can decline | ADR-0013 → ADR-0026 (push-triggered was downgraded to pull-triggered) |
| protocol version number | A semver-variant tag: **major** = cross-tool/cross-repo contract change; **minor** = a new mechanism added; **patch** = revision of an existing file. Every protocol PR declares a `Version bump`; the author tags after merge; the downstream local version is recorded in the `.gearbox-version` stamp (written and read by tooling) | ADR-0023; baseline v0.0.0 |
## 产品 / 技术术语（Mr Otto）

写这个产品的人要查的词。新概念随写随加（AGENTS.md：add new terms as they come up），
不追补历史欠账；边界模糊的词按「读者是谁」归节（ADR-0070）。

| Term | Definition | Notes |
|---|---|---|
| 独立工作副本 (isolated workspace) | 每只水獭干活的那份 git worktree：独立目录、独立 HEAD、独立分支，落在 userData 下 | ADR-0156 / 0157（后者取消了「第一只不隔离」）。日志里是 `session_created.isolated`，所以「我在副本上」这件事可从日志推导 |
| 终端面板（Terminal panel） | 会话里内嵌的真 PTY 终端，纯人用。输出不进事件日志、不进模型上下文（ADR-0031）——它不是任何事实的投影，是人的旁路工具。 | |
| 回滚缓冲（terminal ring buffer） | 主进程为每个终端保留的末尾约 200 KB 输出。面板关掉时渲染层的 xterm 实例就没了而 pty 还在吐，靠它接住；重开面板一次性灌回去。内存态，不落盘，与 pty 进程同生共死。 | |
| 轨迹（Trajectory） | 会话日志的第二种投影（第一种是聊天区）：一步一行，工具请求 + 审批 + 开跑 + 结果按 toolCallId 合成一行，顶部泳道时间轴（Input / Model / Tools）按 Duration / Turns / Calls 三种刻度铺开。对标 deepseek-harness 的 trajectory 视图。纯渲染层、只读、不进 store；取代了早期的「画布 + 函数轨迹」教学式回放（#151）。 | `src/renderer/src/replay/trajectory.ts` |
| 连接器（Connector） | 产品层的名字：**一台你可以接进来的外部服务**。设置页顶部那张目录卖的是 *Notion / GitHub / Figma* 这些名字，用户点「添加 Notion」时不需要知道底下走什么协议。**连接器 ≠ MCP**——今天在本仓两者外延恰好重合，但那是实现的偶然不是定义，两个方向都掰得开：好友代理借来的服务在用户眼里是连接器，却不是你机器上的一条 `mcp.json` 记录（ADR-0166）；右上「新建」手打 URL 建的那台是 MCP server，却没进目录、不在策展层里。分界线：**能发现、能点、能装的那一面叫连接器；要你懂传输方式的那一面才叫 MCP** | ADR-0178、ADR-0171；`src/renderer/src/components/McpDirectory.tsx` |
| MCP（Model Context Protocol） | 一个标准协议，让 Otto 作为 **client** 连外部**server**，把 server 提供的 tools / resources / prompts 接进来。v1.x 只做 client 这一半，不做 server；不算插件系统——server 是跨进程外部程序，不向 Otto 进程注入代码 | ADR-0049、ADR-0050 |
| MCP server | 一个外部程序，通过标准协议（stdio 或 streamable-http）向 client 提供 tools / resources / prompts。Otto 只认协议返回的形状（`McpClientConn`），不加载、不执行 server 的任何代码 | ADR-0049；配置见 `~/.otter/mcp.json` |
| transport（stdio / streamable-http） | MCP 连接的传输方式：`stdio` = 本地 `spawn` 一个子进程，走标准输入输出对话；`streamable-http` = 连一个远程 HTTP 端点。配置里有 `command` 走前者，有 `url` 走后者，两者都有则报错不猜 | ADR-0050；`src/main/mcpClient.ts` |
| elicitation | MCP 协议里 server 调用到一半反过来向用户要字段的机制。设计上打算复用既有的 `elicitation-form` 元素（原为 `ask_user` 准备）多接一个调用方；**本版（tasks 1–7）尚未接线**，只是协议里存在、设计文档里点过名的一个词条 | 设计文档 `docs/superpowers/specs/2026-08-21-mcp-design.md` §八 |
| 工作区在场（WorkspacePresence） | 一个人此刻「在哪个仓库、哪根分支」：`{repoKey, branch}`。repoKey = 规范化 remote URL 的 sha256 前 16 位（只能比对同不同仓库，看不到地址）；branch 是本地短名，detached 为 null。两条腿广播——Realtime presence 的 track meta ∪ 心跳写入 `profiles.repo_key/repo_branch`——Git Graph 把同仓库好友的头像贴到对应分支徽章上。**可见性:写只能写自己那行,但读是对所有注册用户开放的**（`profiles` 的 select policy 是 `using(true)`，好友搜索依赖它）——即 `repo_branch` 明文对任何注册用户可见，不止好友；维护者已判定接受（#236） | ADR-0055（含 2026-08-23 订正）；`src/shared/repoKey.ts`、`src/main/workspacePresence.ts`、`src/shared/friendBranches.ts` |
| 长期记忆（Memory） | `~/.mr-otto/memories/MEMORY.md`（agent 笔记，2200 字符）+ `USER.md`（用户画像，1375 字符），`§` 分隔；`memory` 工具维护。文件是投影，事件是事实 | ADR-0060 |
| 记忆快照（memory_loaded） | 主会话的第 2 条事件（子会话 / sys-memory-edits 不带），模型整个 session 看到的记忆；中途写盘下个 session 才可见 | ADR-0060 |
| 记忆云同步 | 记忆四档跟账号走：本地 `memories/**` 是缓存，云端 `memory_docs(uid,key,content,updated_at)` 是账号级副本；后写胜、内容相同不动；写路径只有 `memoryFiles` 一个口 + LocalWorld 钩子；触发点两个（本地写完防抖 / 登录恢复对账），不轮询；离线不阻塞、登出不清 | ADR-0207、#852 |
| 退化循环（degenerate loop） | 一条 turn 里模型把**同一组工具调用逐字重复**多遍、零进展。判据是「后缀上存在周期 p ≤ 24 且原样重复 ≥ 3 遍」，不是「连续两圈相同」——真实形态里相邻两圈从来不相等。命中只往对话里注一条 `user_message`（`origin:"loop_guard"`）把事实摆给模型，**不停 turn**：无步数天花板仍是 ADR-0006 的决定，硬停只有停止键 | ADR-0212、#891；`src/shared/toolLoopGuard.ts` |
| 任务文件夹 | 内置 Default 下每个任务会话一个子目录 `<Default>/<sessionId>/`（完整 id，不是前 8 位）；旧会话直接等于 Default 根也算任务，判据 `isDefaultWorkspace`；归档不删，设置页可清空壳 | ADR-0206、#851 |
| 任务会话云端日志 | 登录用户的每条任务会话在 Supabase（`task_sessions` + `task_session_events`）有一份账号级副本，**云端那份是事实，本机 sqlite 是它的前缀副本**；桌面复制器 `taskSessionSync` 推 / 拉，冲突按「云端赢、纯人为动作重放、含 turn 痕迹分叉」处理；冲突时有子智能体会话的不 purge，冻结（frozen）并说清——frozen 是**持久**记号（落 task-sync.json）且在账号页以 frozen 状态原样显示，不许说「会自动重试」。项目会话、子智能体会话、引用式分支（「回到这一步」）都不上云 | ADR-0291，#1223 |
| 笔（pen） | 任务会话同一时刻谁在跑 turn 的租约：`task_sessions.pen_holder/pen_until`，30 s ttl、10 s 续、收口帮手跑完才放。executor 类事件（跑 turn 的痕迹）必须握笔才能追加，人的动作（`PEN_VERDICTS` 里的 `human`）免笔——手机从不握笔，天然只发得出人话；建行那一批由 RPC 顺手发给创建者；只在没有下一轮在跑/准入时才在收口帮手之后放。**推送方为落 executor 事件临时借的那支（自己 acquire 的 / 建行发下来的）推完即放**——三处 releasePen 都只服务「turn 的笔」，不放的话开机回填之后每条会话都被本机永久握着 | ADR-0291，`src/shared/taskSync.ts` |
| 执行器（executor） | 此刻替用户跑这条任务会话的一方：`desktop`（本机全工具）或 `cloud`（VPS runtime，无沙箱，②子项目）。换执行器是日志事实 `executor_changed`，投影成 system 尾块；一条都没有 = 桌面 | ADR-0291，spec §3.4 |
| 主题桶（TOPIC 档） | 记忆第四档：`memories/topics/<slug>.md`，种子 work/hobbies/life/learning + 模型可建（先看索引的闸），预算 700 字/桶、封顶 8 桶、整份注入不做检索 | ADR-0204、#846 |
| 记忆巡检（reviewer 第一步） | memory-reviewer 每次被派出（每 10 个 user turn）先对既有条目逐条判四件事：错档（MEMORY 里点名当前项目的搬进 PROJECT，**先 add 后 remove**）、与项目指令重复（删）、重复合并、过时或矛盾（**只有对话或项目指令给出反证才改，没有证据不猜不删**）；之后才看对话里有没有新事实。任务串附主会话本次注入的项目指令（同一条日志的 `project_instructions`，不另读盘）。子会话从父会话原样继承 `memoryProject`，否则 reviewer 没有 project 档也不过点名守卫——真机上近七成记忆写入由它落进全局档 | ADR-0269、#1155 |
| 会话主题（session topic） | 任务会话（workspaceKind=default）挂靠某个主题桶：turnAnnotator 合并调用自动分类落 `session_topic_assigned`（投影丢弃），手动 `session_topic_set` 压过自动结果；侧栏任务栏据此分组 | ADR-0204、#846 |
| 跨会话回忆（session_search） | 模型主动查历史会话的工具，四形态零 LLM：query 全文检索 / session_id 读整段 / session_id+around_seq 看前后 / 无参列最近。不自动注入；排除**系统**归档、子会话、当前会话（用户归档照常可搜，ADR-0087） | ADR-0065 |
| 会话归档（archive） | 删除之外的中间态：`session_archived(reason:"user")` 把会话从主列表收进侧栏「已归档」区——日志完整保留、仍可被 session_search 召回、`session_unarchived` 可恢复。归档状态 = 最后一条 archived/unarchived 事件说了算。`reason:"system"`（或缺席 = 遗留）是系统藏会话：列表和召回都排除（sys-memory-edits） | ADR-0087 |
| 历史索引（events_fts） | `events` 表的 FTS5 trigram 派生索引（user/assistant/tool_result 正文），insert 触发器同步、老库首开回填、purge 连带删；可 DROP 重建，不是事实 | ADR-0065 |
| 自动压缩（auto compact） | 上下文占用 ≥ 窗口 × 阈值（≥512K 窗口 0.50，否则 0.75；可调 0.3–0.9）时，loop 在下一次模型调用前自动 /compact；一 turn 一次；摘要 prompt 带脱敏后的记忆快照 | ADR-0062 |
| OTA 换包更新（swap update） | 打包版自升级：定时查 GitHub Releases，新版 zip 静默下载 + SHA256SUMS 校验 + `ditto -xk` 解包待命，用户点「重启更新」后由 detached 脚本换 .app 并重启（旧版留 `.app.bak` 可回滚）。无签名公证（ADR-0026）走不了 electron-updater，才有这套；Translocation / 目录不可写降级为开 Release 页手装 | ADR-0075；`src/main/updater{,Core,Host}.ts`、`scripts/release.mjs` |
| 会话自动命名（session_autotitled） | 第一条 user_message 首行超过 24 字时，turn 收口后的合并调用（turnAnnotator 任务三）把它浓缩成短标题落 `session_autotitled` 事件。标题优先级：`session_renamed`（手动）> `session_autotitled` > 首行原文；已有任一命名事件不再触发（一会话最多一条）；失败静默、下 turn 自愈；不喂回模型 | #335；`src/main/sessionTitler.ts` |
| 项目指令注入（project_instructions） | bot 读工作区的 AGENTS.md/CLAUDE.md 类文件：从 workspace 向上到 root（.git 标记）逐层收集、root→cwd 按序拼接（不覆盖）；每层 `.override` 文件名优先；总量 32KB 预算，超的整段丢弃标 truncated。**信任门禁**：未信任的工作区不加载（防陌生仓库的 prompt injection），横幅给「信任并加载」入口，信任跨会话持久（trustedWorkspaces.json）。注入落 `project_instructions` 事件（快照自包含 + 每段来源路径 provenance），时间线可点开 | issue #353；`src/main/projectInstructions.ts`、`workspaceTrust.ts` |
| 会话分支（fork，引用型零拷贝） | 「从这条消息重来」的形态：新会话只存 `session_created.forkedFrom {sessionId, seq}` 位置引用，不复制事件行；load 沿链取父前缀（≤ fork 点）+ 自己的追加段。seq 播种（分支首事件 = fork 点 seq+1）让链视图全局递增，所有 seq 语义免翻译。fork 点必须是 turn_ended（不继承半截 turn，结构保证）；父会话有分支引用时拒绝 purge。UI 二期 | ADR-0084、issue #352；`EventStore.fork/forkOrigin` |
| 有界上下文重建（bounded context scan） | 模型上下文不全量读：反向找最近 context_compacted checkpoint，正向重放「清场幸存者（session_created / memory_loaded / 全部 skill_invoked）+ 最后一个活 user turn 连续段 + checkpoint 起的尾段」。真实 turn 边界 = 日志层的 user_message 事件（skill/图片/记忆的"伪 user"都是独立事件类型，不猜前缀）；逃生舱（无 checkpoint / 连续空跑回溯超限）退回全量。等价性契约：deriveMessages(有界) === deriveMessages(全量) 逐字节（前缀缓存依赖，ADR-0073） | issue #351；`src/session/modelContextScan.ts`、engine.snapshot 首圈 |
| 工具钩子（Pre/PostToolUse） | 工具管线的两个钩子位（issue #350）：Pre 可拦截（error 回模型，工具不执行、无 started 事件）或改写入参（执行用改后那份，模型原话不动）；Post 可拒绝结果（原始输出存进事件审计）或注入反馈（日志/UI 存原始输出，投影把反馈包装进模型看到的 tool 消息——两个消费者分离）。四种干预都落 `tool_hook` 事件，模型视野仍可从日志推导。alias 表兼容 Claude Code 风格钩子名（Bash→bash 等） | issue #350；`src/loop/middleware.ts`（ToolHook）、engine.execute、`deriveMessages` 的 feedbackByCall |
| MCP 双 ID（raw / 模型可见名） | MCP 工具两套 ID：raw（server.id + 协议原名，回调路由用，走 createMcpTools 的闭包）与模型可见名（`mcp__<server>__<tool>` 消毒 + 撞名哈希后缀 + 64 字节上限）。可见名由 assignMcpToolNames **整桌统一分配**（撞名 salt 取决于先来后到，同清单同分配）；相同原始身份去重跳过；同名后到工具在 engine 注册期被拒（先到的赢，外部占不了内置名） | issue #349（前身 #156）；`src/shared/mcp.ts` |
| 工具暴露（ToolExposure） | 工具可见性三态：direct（初始工具表就有，缺席默认）/ deferred（注册了但要 tool_search 搜到才可见）/ hidden（永不进模型工具表，但 toolsByName 仍有——内部可调、误调有明确错误）。MCP 工具超 12 把整批 deferred；spec 体积预算单把 8KB / 总量 64KB，超了降 hidden 不报错 | issue #348；`src/tools/exposure.ts`、`src/tools/toolSearch.ts` |
| execpolicy（命令前缀规则） | 声明式命令安全静态判定：`{pattern: 有序前缀 token（元素可为「或」列表）, decision: allow\|prompt\|forbidden}`，多规则命中取最严；加载期跑规则自带 match/not_match 用例（坏规则整份拒载，fail-safe 空规则）；禁止前缀清单（bash -lc / sudo / env / 裸 git…）挡住"后面能接任意东西"的 allow 前缀；包装命令递归拆壳 + rm force 降级只收紧不放宽。审批 UI「永久」优先产出整条精确 allow 规则（掺 cwd），撞清单退回 #342 精确 key。forbidden 在审批链最外层，bypass 模式压不过 | issue #347；`src/shared/execPolicy.ts`、`src/main/execPolicyStore.ts`（userData/execPolicy.json） |
| 沙箱档位（SandboxTier） | `none`（v1 本机直跑）/ `external`（已在外部沙箱内：全盘访问但仍守网络设置，无升级环）/ `container`（v2 Docker，唯一有升级环的档位）。三态现在钉死，v2 上线不改协议 | ADR-0083；`src/world/sandbox.ts` |
| 沙箱升级环（sandbox escalation） | container 档位下的半环：沙箱拒绝（`SandboxDeniedError`，确定性标记，不做 stderr 启发式）→ 带失败原因二次审批 → 同意后无沙箱重跑。world 装饰器实现，工具无感；policy 含拒读路径时升级分支硬拒（逃逸 = 对拒读清单的静默授权） | ADR-0083；`src/world/escalatingWorld.ts` |
| 请求信封（request_envelope） | 每次模型调用前落盘的 log-only 审计快照：渲染后的 system 全文 + 工具声明表 + model/wireModel/thinking——请求里日志推不出的那半（工具表是磁盘/MCP 动态状态，thinking 是运行时偏好）。与上一条相同不落（典型会话一两条）；模型不可见；带 ignorable。用途：任何历史请求可从日志逐字节重构 | ADR-0086 决定 1、issue #383；engine.appendEnvelopeIfChanged |
| 单调守卫（ToolGuard） | Pre 钩子后、执行留痕前的 deny-only 闸：返回 reason = 拒绝（落 `tool_hook{action:"guard_deny"}`），返回 undefined = 弃权，**没有 allow**——注册顺序翻不了案。看到的是最终生效参数（审批改参/钩子改参之后），内置守卫在这复查 execpolicy forbidden。与钩子的分工：钩子 fail-open（超时弃权），守卫 fail-closed（受信代码不设超时） | ADR-0086 决定 2；`src/loop/middleware.ts`（ToolGuard） |
| 向前兼容拒读（ignorable） | 「旧版读新日志」的契约（硬规则只定义了反方向）：事件可标 `ignorable: true` = 不认识它的旧版本可安全跳过；resume 读到未知且未标的类型抛 UnknownSessionEventError（话术指向升级），不静默跳过。默认拒是刻意的：忘标的代价是多拒一次，静默跳过的代价是复活残缺会话。只把 resume 的门，列表/回看宽容 | ADR-0086 决定 3；events.assertReplayable |
| 合成收口（interrupted） | resume 时发现开着的 turn（最后一条 turn_ended 之后仍有 turn 活动）补 `turn_ended{outcome:"interrupted"}`——loop 永不产生这个值，「修的」和「跑出来的」永远可区分。追加不改写、幂等；崩溃空跑 turn 从此被 barrenTurns 正确跳出上下文 | ADR-0086 决定 4；agent.ts resume 修复块 |
| 新鲜区折叠（maxFreshToolOutputChars） | 投影对保真区 tool_result 的独立上限（默认 50K 字符；老区仍 400）——此前新鲜区无上限，read_file/MCP 一条超长输出直接吃穿窗口。日志存全文不动（UI/摘要人照读），折叠只住投影层；hermes 的 spill-to-disk 在本仓不需要：日志本身就是那块盘 | ADR-0086 决定 5；deriveMessages tool_result case |
| 错误分类（errorClass） | 模型 API 错误的三类标记：`rate-limit` / `retryable`（瞬态）/ `fatal`，**抛错的地方分类**（状态码还在手上，不从错误文案倒推），贴属性不建子类。与 retryable 标记正交：class 是种类，retryable 是种类×流位置的重试许可（首 token 后的瞬态错不可重试）。turn_ended 可选 errorClass 字段补记分类，error 仍存原文 | ADR-0088 决定 1；`src/model/errorClass.ts` |
| 后台回注（background re-injection） | bash `run_in_background` 起的任务（world.execDetached，不绑 turn 信号、30 分钟超时）完成后，结果进对话的三条路（ADR-0205，修订 ADR-0088 决定 2）：有 `wait_task` 等着 → 从那把工具的 tool_result 回，只记账；turn 在跑 → `engine.appendBackground` **当场尾部追加**（纯 append，前缀不变，prefix cache 无损——0088 拿 ADR-0073 反对的是中段重写），模型下一次采样就看到；idle → 另开一轮。压缩中攒（pendingBg）收口后合并另开；aborted 不排空。采样窗口里到的攒到那条 assistant_message 之后再落（否则投影像模型已经答过）。`background_task_completed` 事件是 log-only 审计，模型可见载体是 user_message(origin:"background") | ADR-0205 / ADR-0088 决定 2；`src/main/backgroundTasks.ts`、`src/loop/engine.ts` |
| wait_task | 模型在**同一 turn 里**阻塞等一个后台任务出结果（Claude Code TaskOutput 对照）。结局是 `kind`：done / timeout（带输出尾巴，可再等）/ unknown；默认 300 秒、上限 1800 秒；中断 reject 成 AbortError。`available` 现查 armed。没有它，模型说完「等 CI」就交卷，idle 重唤起照样发生——它是「一件事别拆成好几段会话」真正靠的那一条 | ADR-0205；`src/tools/waitTask.ts`、`BackgroundTasks.wait` |
| 沙箱事实上报（ExecResult.sandbox） | 命令跑完了但沙箱在过程中拦了什么/自身出了什么状况——随 ExecResult 回来的**事实**（与 SandboxDeniedError 的"整条被拒抛错走升级环"互补）。denials（拦了=约束在工作）与 failures（坏了=约束可能失效）严格分开；判定责任在沙箱实现侧，不做 stderr 启发式。v1 无生产者，协议即测试 | ADR-0088 决定 3；`src/world/sandbox.ts`（SandboxEnforcementFacts） |
| 不变量校验（checkInvariants） | 事件流结构合法性的可执行断言（工具调用三方配对 / toolCallId 唯一 / turn 不双收口），resume 修复**之后**跑、只告警不拦——违例是写入方有 bug 的线索，不是拒读理由（拒读那道门只属于 assertReplayable）。合法反例（ts 乱序/fork 双创建/steer 多 user_message/修复尾巴）明文豁免 | ADR-0088 决定 4；`src/session/invariants.ts` |
| keyless 回放 | 「无 API key 也能查看历史会话与轨迹」的契约：key 门只在发送那一刻（modelRoute blocked），启动/恢复/投影全程不碰 key。功能今天就通，靠单元 + e2e 双层测试钉住不回退 | ADR-0088 决定 5；`tests/main/keylessReplay.test.ts`、`tests/e2e/keylessReplay.e2e.ts` |
| 微压缩（micro compact） | 设置开启时每 turn 收口后（turn 锁外、串行）把最老的未吸收 exchange 的 assistant/tool 部分并进 running summary，落 `micro_compacted{summary, coversUpTo(seq)}`；投影只认最新一条，把被吸收事件换成一条 `[对话摘要]` assistant 消息，user_message 永不吸收；保护区 = 最新 context_compacted 后第一个 exchange + 尾部 keepRecentTurns；摘要 >2000 token 先 defrag；默认关（每轮改写历史会让前缀缓存失效） | ADR-0064 |
| 工作区检查点（checkpoint） | 每个用户 turn 开跑前把工作区文件快照进**影子 git**（--git-dir 住 `~/.mr-otto/checkpoints/<hash>`，--work-tree 指回工作区），落 `checkpoint_created{checkpointId}`（ignorable，模型不消费）。「回到这一步」= fork 会话到检查点前最近的 turn_ended（零拷贝，ADR-0084）+ `reset --hard` 回快照，新分支落 `workspace_restored`。边界：工作区自己的 .git 永不被碰；.gitignore 区不进快照也不被还原；从未存过档的文件 restore 不动。能力挂在 ExecutionWorld.checkpoint（v2 可换 docker commit） | ADR-0090 决定 1、issue #395；`src/world/checkpoints.ts` |
| 用户钩子（user hooks） | `userData/hooks.json` 声明的 shell 命令挂进 Pre/PostToolUse（#350 基础设施的用户侧）：stdin 收 JSON 上下文，stdout 回 JSON 裁决（pre: block/reviseArgs；post: reject/feedback），exit 2 = 快捷否决（stderr 作理由），其余失败一律弃权（fail-open——安全边界仍是守卫和审批门）。加载 fail-safe（坏文件 = 空钩子）、每次工具调用现读（热更新）；只挂主会话，钩子命令跑在专用 LocalWorld（cwd=工作区、凭据已剥、10s 超时） | ADR-0090 决定 2、issue #395；`src/main/userToolHooks.ts`、`src/shared/userHooks.ts` |
| 前台自动转后台（auto-background） | 回注已接线（armed）的装配里，前台 bash 超 30 秒不再 SIGTERM 硬杀：超时经 `ExecOptions.timeoutMs` 显式放宽到后台档（30 分钟），工具层把还在跑的**同一个 in-flight** 登记成后台任务（不重跑副作用），完成走既有回注链路。未 armed 的装配维持 30s 硬杀。迁移进程仍绑 turn 中断信号（停止键停一切；显式 run_in_background 才是不绑信号的例外） | ADR-0090 决定 3、issue #395；`src/tools/bash.ts`（AUTO_BACKGROUND_AFTER_MS） |
| 派活总量上限（SUBAGENT_SESSION_CAP） | 单会话派活硬上限 100：计数从父日志 `subagent_spawned` 推导（不另立计数器），闸在建子会话之前，超限抛错给模型让它收手。与长 turn 软告警互补：那边喊的是模型步数，这边拦的是「每次派活烧一整个子会话」的失控循环 | ADR-0090 决定 4、issue #395；`src/main/subagentRunner.ts` |
| 模拟器面板（Simulator panel） | 右侧栏那块 iOS 模拟器：人能看能点，agent 用 `simulator` 工具操控同一台设备。与浏览器/终端/GitGraph 共用同一块右侧槽位（互斥）。**app 级单例**，不按会话分——一台机器只有一套模拟器，人和任一会话里的 agent 点的必然是同一台。画面走 500ms 轮询截图（缩到 480 宽转 JPEG），不进事件日志、不进模型上下文（ADR-0031 延伸） | ADR-0092、issue #401；`src/main/simulatorHub.ts`、`src/renderer/src/components/SimulatorPanel.tsx` |
| 截图像素坐标（frame pixels） | 模拟器这块屏**唯一**的坐标系。面板上点的、`describe` 报的元素框、agent `tap` 给的坐标全在这套空间里（= 缩放后那张帧的像素）；macOS 屏幕坐标只活在 Swift helper 那一侧，两者之间的换算是纯算术、住在 hub 里可单测。窗口矩形每次现问 helper 不缓存——窗口会被拖走，缓存旧值的代价是点歪且无症状 | ADR-0092 决定 1；`src/shared/simulator.ts`（pixelToScreen / screenToPixel） |
| 输入通道（input channel） | `native/MrOttoSimInput`（Swift，CGEvent 发点击/打字、AXUIElement 读 iOS 无障碍树）+ `simInputBridge` 那条**请求-响应** NDJSON 管子（与岛的单向推桥不同：点一下要知道点没点上，故带 id 配对与超时）。降级是分层的：helper 缺席或没「辅助功能」授权时，看画面/开关机/装 app/起 app 照常，只有点击/打字/读屏不可用 | ADR-0092 决定 3；`src/main/simInputBridge.ts` |
| 盲管道（blind pipe） | `services/edge` 的 `/rl/v1/connect` 中继：只按 `user_id` 把同一个账号下的几条连接的字节互转，**不解析负载、不落盘、不打印**。三条都是安全性质而非省事——端到端加密的密文对中继就该是不透明字节。收件人认不出、或对端一条都没有，直接丢弃，不排队（排队 = 落盘）。跑在 Durable Object 上之后「不落盘」是字面意义的：整个中继一个 storage API 都不调。有专门的测试守着"负载从不进日志" | ADR-0095、ADR-0129、ADR-0130；`services/edge/src/relay.ts`、`services/edge/src/worker.ts`、`tests/edge/relay.test.ts` |
| TOFU pin | 首次信任的落地：手机首次连某台桌面时把它的 `identity_pub`（Ed25519）pin 进本地，之后握手一律用**pin 住的**那把公钥验签，对不上就拒绝连接并红字告警，不静默接受；换电脑要用户显式确认一次。选它是因为「按账号配对」与 E2E 天然打架——密钥若从账号体系下发，掌握 Supabase 的人就能发一把假的。代价写在 ADR-0095 的威胁模型表里：首次 pin 之前的假公钥挡不住 —— 走扫码配对那条路时这个代价没有了（公钥不经目录，ADR-0142），走 6 位码降级路径时仍在 | ADR-0095 / ADR-0142；`src/shared/remote/handshake.ts` 的 `deriveSession` |
| 扫码配对 / 持有证明（pairing offer / pair proof） | 把「两端各人工确认一次」压成「手机扫一次」的那条带外通道：桌面显示一次性二维码（身份公钥 + 32 字节 secret，TTL 3 分钟、配上一台即废、只在内存里），手机扫到后直接 pin 桌面公钥，并在下一轮握手里附一条**持有证明** = `Ed25519Sign(手机身份私钥, "otto-pair-proof-v1\|角色\|设备id\|" ‖ secret ‖ ephPub ‖ nonceHalf)`。桌面据此认下这台手机。secret 进签名 ⇒ 没扫过码签不出；eph/nonceHalf 进签名 ⇒ 截走的证明换条连接用不了。**目录仍然不是信任来源**：hello 里自称的公钥是验签的输入，不是查表查出来的可信值。6 位安全码那条路降级保留（手机没给摄像头权限时） | ADR-0142；`src/shared/remote/pairing.ts`、`src/main/remoteBridge.ts` 的 `tryPair` |
| 上行帧 / 下行帧（UpFrame / DownFrame） | 手机与桌面之间的加密载荷，方向分明：下行（桌面→手机）是 `fleet` / `timeline` / `ping`；上行（手机→桌面）恰好五个词 `approve` / `deny` / `send` / `watch` / `unwatch`，没有 `focusSession`，`approve` 没有 grant 档（ADR-0096）。解码规矩是"认得的键集合之外还有别的键 = 整条丢弃"，不是剥掉多余字段放行。**与握手包 `HandshakeHello` 不是一回事**：后者是明文、走同一根线、在加密建立之前发 | ADR-0094 / ADR-0096；`src/shared/remote/frames.ts` |
| 在场信号（`:peer <cid>`） | 中继在任一端接上时往**两侧各发**的控制消息，对端**每条连接各一条**（同一个 cid 再来 = 那边重连了，旧密钥作废，重开）。握手是双向的、中继又不排队，而桌面长命、手机后到 —— 盲发的 hello 必然掉进虚空。谁到场只有中继知道（唯一同时看得见所有连接的人），所以由它说。这是握手唯一的起点：`RemoteTransport.onPeer(cid)` 触发跟**那一条**的 `startRound()`；`onClose` 只清状态、不发东西。对应的离场信号是 `:gone <cid>` | ADR-0100、ADR-0130；`src/shared/remote/wire.ts`、`services/edge/src/worker.ts`、`src/main/remoteBridge.ts` |
| 连接 id（cid） | 中继给每条连接编的号。**发帧必须指名收件人，收帧必须知道发件人**——每条连接有自己一套会话密钥，广播过去的帧在别人那儿解不开，而 `sealedStream` 还带计数器校验，收到别人的帧会被判成异常而不是无害的噪音。cid 是中继编的、不是设备自称的，它只回答"这份字节塞哪根管子"；端到端身份始终只由握手签名决定。**随机而不是递增**：Durable Object 睡醒后内存清零，计数器会归零撞上还开着的连接 | ADR-0130、ADR-0129；`src/shared/remote/wire.ts` 的 `newCid` / `encodeFrame` |
| 密封流（sealed stream） | 单向加密流的线格式：`[8 字节大端计数器][密文][16 字节 tag]`，nonce = 4 字节前缀（握手派生，每方向一条）+ 8 字节计数器。丢掉 libsodium secretstream 换桌面侧零依赖，代价是 nonce 管理与重放检测自己写：计数器不回绕（到顶抛错、断线重连）、收端拒收不严格递增的计数器、**水位线只在验签通过后推进**（否则伪造一个大计数器就能饿死后续真帧）。同 key 同 nonce 是灾难性失效，所以每连接的临时密钥绝不可复用 | ADR-0095 / ADR-0097；`src/shared/remote/sealedStream.ts` |
| 好友（friendships / messages） | 账号级的人对人关系，活在 Supabase 里由 RLS 看门，跟「谁的哪台电脑开着」无关。桌面与手机**各写各的**、都直连库：手机端不经中继（ADR-0114 推翻 ADR-0094 里「写操作留在电脑上」那一条）。手机仍不碰 presence / 工作区在场那一层。表与策略在 `0001_friends.sql`，publication 在 `0013`。 | ADR-0114；`mobile/src/friendsApi.ts`、`src/main/supabaseFriendsApi.ts` |
| 降级轮询（degraded → poll） | Realtime 通道只要有一条不是 `SUBSCRIBED`，健康度就整体判 `degraded`，客户端切到定时拉取兜底——功能仍在，只是慢几秒。宁可整体降级，也不要「看着是好的但其实哑了」。手机端还多一条：`AppState` 回 `active` 立刻对表（挂起期间 WebSocket 多半已被系统收走）。 | ADR-0027；ADR-0114；`mergeChannelHealth`（`src/shared/friendsQuery.ts`） |
| loopback 回调 | MCP OAuth 授权时在 `127.0.0.1:0`（系统分配随机端口）起的一次性 http server，`redirect_uri = http://127.0.0.1:<port>/callback`：收到一次请求立刻关端口，5 分钟无人到访自杀。选它而不是 `mrotto://` 深链，是因为部分 OAuth 服务端拒绝非 http 的 redirect_uri（RFC 8252 的标准做法）。**`state` 由我们自己验**——SDK 的 `finishAuth(code)` 只收 code，这是回调唯一的防伪造闸 | ADR-0121；`src/main/mcpOAuth.ts` |
| needs-auth | MCP server 的一种连接状态：连上了但服务端要 OAuth（`McpAuthRequiredError`）。它是「授权」按钮出现的唯一条件，也是 token 过期且 refresh 续不动时的回落状态——不是错误态，是「差一次人点同意」 | ADR-0121；`src/main/mcpHub.ts`、`src/renderer/src/components/McpSettings.tsx` |
| 工具表热更新 | LoopEngine 的工具表**每个 turn 开始重算一次、turn 内冻结**（`LoopEngineOptions.tools` 收 `Tool[] \| (() => Tool[])`）。冻结是硬要求：模型看到的声明表和 dispatch 时查的 `toolsByName` 必须是同一份，turn 中途换表会让模型按旧表发出的调用收到「未知工具」。顺带修好「会话中途重连的 MCP server 当前会话看不见」这个存量缺陷 | ADR-0119；`src/loop/engine.ts` 的 `rebuildTools()` |
| 团队（team）/ 工作区（workspace） | **界面上是两个词，代码里都叫 workspace** —— 分清它们是读这个仓库的第一道坎。**团队** = `workspaces` 表那一行：一群人 + 他们贡献的连接器 + 云会话 + agent 名册（侧栏第三档、`WorkspacePage` 那扇抽屉、`services/runtime` 整套）。**工作区** = 水獭在哪儿干活的那个文件夹：本机会话的 cwd（`ExecutionWorld` 的围栏、内置 Default、工作区检查点/锁/协作记录/在场），以及云端每个团队那一个共用工作目录（一容器一卷）。界面文案从「工作区」改名「团队」时只动了前者（#1126）；**代码标识符、DB 表名、既有 ADR 一个字没改** —— 改了会断账、断历史引用。所以读到 `workspace` 这个符号要先问一句「这是一群人还是一个文件夹」，判据是它旁边有没有成员/连接器/云会话 | #1126；`src/renderer/src/components/WorkspacePage.tsx`（团队）、`src/renderer/src/components/WorkspaceSettings.tsx`（文件夹） |
| 云会话 (cloud session) | 工作区里跑在 VPS runtime 上的群聊会话：权威 EventStore 落在 VPS SQLite（每工作区一库），loop 在宿主 daemon 跑，工具经 DockerWorld 落进每工作区一个容器。桌面/手机是**显示器**——真正的执行不在本机，客户端订阅 cs 帧直播 + 拉 backlog 投影出同一份聊天区。云会话从不 `store.append`（本地 EventStore），天生不在本地 `fleetSessions()` 里，靠合成的虚拟 `SessionSummary` 才挂得上 fleet/手机 | ADR-0199；`services/runtime/src/sessionService.ts`、`src/main/cloudSessionClient.ts` |
| cs 帧（cloud session frame） | 云会话的线上协议：`CsUp`（hello/create/say/backlog/approve/config/archive）与 `CsDown`（welcome/created/denied/event/backlog/error），走 relay 按 cid 寻址的通道，编解码三端共用一份。`CS_PROTOCOL_VERSION` 破坏性变更 +1，版本不匹配时客户端提示升级、runtime 拒帧不崩 | ADR-0199；`src/shared/remote/cloudSession.ts` |
| 控制房（control room） | cs 协议里固定房名 `cs-ctl` 的房间，只用来发 `create` 拿一个新 `sessionId`——「按需连，拿到 created 即断」，不像会话房那样长期持有；没有 welcome 概念，hello 成功是静默的 | ADR-0199；`src/shared/remote/cloudSession.ts` 的 `csCtlChannel`、`services/runtime/src/frameHandler.ts` |
| otto-sandbox | 每工作区一个 Docker 容器 + 一个具名卷（容器名/卷名同为 `otto-ws-<workspaceId>`）共用的镜像名。资源上限 `--memory 2g --cpus 2 --pids-limit 512`；空闲 30 分钟自动 stop（卷保留）、再来会话自动重启；工作区已不存在的容器/卷先标记、7 天宽限期后才真删（两阶段孤儿回收，防 Supabase 查询抖动误杀） | ADR-0199；`services/runtime/src/sandbox.ts`、`services/runtime/sandbox/Dockerfile` |
| 发起人审批（initiator approval） | 云会话审批归属模型：能批 `approval_request` 的只有**这条 turn 的发起人**与**工作区 owner**，其余成员只读；应答的 `decidedBy{uid,label}` 是显式参数直接落盘（不经旁路 Map，避免背靠背两次 approve 的 TOCTOU），超时 10 分钟自动 deny——发起人/owner 都不在线时 fail-closed 卡住而不是放行 | ADR-0199；`services/runtime/src/approvalRouter.ts` |
| 托管模式（hosted） | 用户买订阅档位，网关拿**官方自己的** key 打上游、按 credit 记账；与 BYOK 并行的第二条路，付费订阅下优先于自带 key | ADR-0176；`src/main/modelRoute.ts` |
| BYOK（bring your own key） | 用户自带上游 key，本地直连，不走网关——key 只活在主进程 env，永不上传，免费档能力全开 | ADR-0176（key 不过桥这条不为托管让路） |
| 5h 窗 / 周窗 | 双固定窗额度：5h 窗上限是周额度的 20%，两个窗口都不超才放行；固定窗不是滑动窗——到点整窗清零，不做连续倒计时 | ADR-0174 决定 2/3；ADR-0203 决定 2/5（落地用累计数不用环形桶） |
| credit | 显示层单位，1 credit = 1 美分 = 10_000 micro-USD；服务端记账全用 micro-USD 整数，credit 只是换算给用户看的数字 | ADR-0174 决定 1；`src/shared/billing.ts` |
| hold（预扣） | 流式请求先按请求体字节 + `max_tokens` 高估一笔预扣额度，再转发上游，最后按实际用量 settle 退差额；中断三出口（flush/cancel/abort）统一走 release，不占坑到 TTL 自然过期 | ADR-0174 决定 9；ADR-0203 决定 3/13 |
| Quota DO | 每用户一个的 Durable Object，落盘窗口用量与加购余额的**投影**；钱的唯一事实是 Supabase `usage_event` 表，DO 冷启动从事实重建，重建失败 throw 不落空态 | ADR-0203 决定 5/11；`services/edge/src/quota.ts` |
| on-behalf-of | 云 runtime 代表**工作区所有者**（不是发起 turn 的人——ADR-0217 推翻了 ADR-0203 决定 9）调网关的头 `x-otto-on-behalf-of`，配 `x-runtime-secret` 一起用；真人自己的请求带这个头一律 400；切片 3 起同一请求还带 `x-otto-agent` 记归因 | ADR-0203 决定 9 → ADR-0217；ADR-0221 |
| reroute | 网关 429 `quota_exhausted` 时桌面 adapter 单列的一类失败——不退避、立刻重解析端点一次再打一遍，与限流退避不同类 | ADR-0203 决定 14 |
| 工作区 agent | `workspace_agents` 表的一行：这个工作区里 @ 得着的一只水獭，有自己的名字、一句话职责、提示词和型号白名单。每个工作区开箱自带一只种子「管理员」（migration 的触发器种的），它删不掉——不点名时由派活分类器按职责挑接话的那只，**没人对口的活归它**，分类器判不出来才回落名单第一只（ADR-0270）。与「会话」正交：一条云会话里站着这个工作区**此刻**的全部 agent，建/改下一 turn 生效 | ADR-0219 / ADR-0220；`supabase/migrations/0021_workspace_agents.sql`、`src/renderer/src/components/WorkspaceAgentsTab.tsx` |
| 连接器白名单（agent） | `workspace_agents.tools`：这只 agent 拿得到工作区里贡献的哪几台连接器、每台哪几个工具。两层「空 = 全给」（顶层 `[]` = 整池放行，条目 `tools: []` = 整台放行），与 `workspace_connectors` / 好友代理同口径；匹配只看 `serverId` 不看 `hostUid`。是「配错了」的护栏不是越权闸——真正的三道闸在 edge 的 pxGate | ADR-0221（ADR-0151/0164 的同一口径）；`src/shared/agentToolAllow.ts` |
| 用量归因 | `usage_event.agent_id`：托管路由的每一笔花费记在哪只工作区 agent 名下（随 `x-otto-agent` 头上报，带 id 不带名——改名不断账；空串 = 桌面直连或 0022 之前的旧行，聚合时单列成「未归因」）。设置页「用量」tab 按 **owner 的**周窗现聚合（工作区烧的是 owner 的额度），不碰 Quota DO | ADR-0221；`services/edge/src/usageAttribution.ts`、`supabase/migrations/0022_usage_event_agent_id.sql` |
| 工作区记忆（shared / own） | `workspace_memories` 一档一行（`agent_id` 空串 = 工作区共享档、非空 = 那只 agent 的私有档；主键 `(workspace_id, agent_id)`，DB 只存整份文本，条目切分留在纯层）。两档判据一句话「换一只 agent 还成立吗？」——成立写 shared（2200 字符，接替本机 `project` 档的位置），不成立写 own（1100）。`user`/`memory` 两档在工作区里**不出**（群里「用户」是谁没有答案；云容器里没有本机环境事实）。RLS 成员在籍即可读写（对称 ADR-0198 决策③），共享档每条带写入者前缀 `[名字] `——由写入路径拼，桌面手改不拼。**已被 ADR-0282 推翻**（#1140）：落点与形状换成团队 wiki，表与事件只为重放/迁移保留 | ADR-0222；`src/shared/workspaceMemory.ts`、`supabase/migrations/0023_workspace_memories.sql` |
| `workspace_memory_loaded` | 云会话里一只 agent 起 turn 前的记忆快照事件（两档全量 + `agentId`/`agentName`）：**缺席或内容变了才落**、投影**最新一条胜出**。与本机 `memory_loaded`「一会话一条」的差别来自共享档是多写者可变状态——它会在会话进行中被别的 agent 改，所以一条会话里一只 agent 会有多条快照，投影只认最后一条（叠加 = 模型读到新旧两套口径且不报错）。两档都空时也落一条基线快照。**已被 ADR-0282 推翻**（#1140）：落点与形状换成团队 wiki，表与事件只为重放/迁移保留 | ADR-0222；`src/session/events.ts`、`src/session/deriveMessages.ts` 的 `renderWorkspaceMemoryPrompt` |
| 团队 wiki | 团队记忆的落点（ADR-0282，推翻 ADR-0222）：`/work/wiki/` 里由智能体维护、人也能改的互链 markdown 页面。`index.md` 由工具从页头生成（禁止手写）、`log.md` 追加、`SCHEMA.md` 约定、`team.md` 恒常驻、`agents/<id>.md` 每只一页（全员可读、只注入给它自己、只有它自己或人能写）；其余页一层目录小写 kebab。两把刀 `wiki_read`（parallelSafe）/ `wiki`（write / remove / check），不过审批门。两个预算：常驻合计 2200、自己那页 1100（= 原两档的上限），其余无上限 | ADR-0282；`src/shared/wiki.ts`、`services/runtime/src/wikiService.ts` |
| `workspace_wiki_loaded` | 云会话里一只 agent 起 turn 前的 wiki 快照事件（索引 + 常驻页 + 自己那页 + 只给管理员的 nudge）：缺席或内容变了才落、投影最新一条胜出（与 `workspace_memory_loaded` 同一条判据）。正文是落盘那一刻已经过 `scanThreat` 的版本——事件里的内容就是注入的内容。快照缓存的规则一条：**容器停着 = 卷没变**（所有写者都在容器里跑），只聊天的 turn 不把停着的容器叫起来 | ADR-0282；`sessionService.loadWikiIfChanged` |
| wiki journal | `workspace_wiki_journal` 追加表：文件是事实，它是**单向**备份 + 历史（每次写入一行，content null = 删）。恢复只发生在 `wiki/` 不存在时；bash 绕开工具改的页由 `check` 补记 `external`。客户端只读不写（同 `workspace_mentions` 的理由） | ADR-0282；migration 0034、`services/runtime/src/wikiJournal.ts` |
| 接力（`agent_relay`） | 一只工作区 agent 在**自己那条给人看的回复里** @ 了另一只，服务端替它把这句 @ 当成一次点名，接着起下一条 turn。载体是**两条事件**：`agent_relay`（群事实——谁传给谁、第几棒 `depth`；`ignorable`，模型不可见，时间线画线与护栏/上限的判据都从它重放）+ 一条带 `relay` 的 `user_message`（起 turn 的载体，`fromUid` 仍是点火的人）。三道刹车，**钱只能往下压不能往上抬**（ADR-0238）：① 一次点火总棒数 `RELAY_MAX_HOPS_PER_IGNITION`（24）**无条件**，绝对天花板；② 预算闸——已花 >= 所有者 5h 窗口**剩余**的一半就停；③ 周期护栏两档，`minRepeats` 注话不停（ADR-0212）、`RELAY_SPIN_STOP_REPEATS` 硬停。问不出剩余额度时降级，补回 `DEFAULT_RELAY_MAX_DEPTH`（6）那道分支闸 = 改动前的行为。owner 可配的 `workspaces.relay_max_depth` 已撤（那一列还在库里，没有读者也没有写者）。只在 `completed` 后接力，归档后一律不接力。**不是通用编排框架**——本机子 agent 仍不能再派子 agent | ADR-0223、ADR-0238；`src/shared/agentRelay.ts` |
| 人话点火 | 一条接力链的起点：**人**点名的那条 `user_message`（带 `mentions` 且没有 `relay`）。`relayChain` 只算最近一次人话点火之后的 `agent_relay`，所以人每说一句就是一次新的授权、`depth` 归零——护栏与棒数上限的作用域是**一条链**，不是整条会话 | ADR-0223 决策 4；spec §8；`src/shared/agentRelay.ts` 的 `relayChain` |
| 派活（dispatch） | 人在群里说了一句话、**没 @ 任何人**时，runtime 用网关最便宜那款读一遍「名册（名字 + 一句话职责）+ 最近 8 句 + 这句话」，回该由哪几只接（通常一只、最多 3 只）；判出来的那几只落成**一条** `user_message{mentions, dispatch:"auto"}`，与人亲手 @ 的那条逐字节同一条路（排队/停止/护栏/接力链首全免费拿到，`dispatch` 只是记号）。判成 none = 闲聊，只落 `chat_message`；明确是活但没人对口 → 管理员；正文里有任何 @ 或点了人类成员 = 人已经在指名，**不判**。失败一律回落改动前的行为：开局卡/旧手机（`mention:true`）回落名单第一只，composer（`mention:false`）落 `chat_message` + 一句「没派出去」；**所有者没订阅是 `skipped`——回落但不出声**（头部那行 blocked 已经在说，逐句再说是噪音）。**判完再落、单事件**——否决了接力那种「先落 chat_message 再补隐藏开场白」（会双答），代价是发送方的消息晚 0.3～1 秒出现。agent 的回复**不**触发派活（那是自激，只走 @ 接力） | ADR-0270；`services/runtime/src/dispatch.ts`、`sessionService.ts` 的 `say()` |
| 语音通话（voice call） | 团队云会话里的「微信群语音」：拉几只 agent 进通话，之后它们的回复经 edge 网关调 MiniMax `speech-2.8-turbo` 读出来。**通话是团队事实**——日志里一条 `voice_call_changed`（名单带名字快照，空 = 结束），投影 `voiceCallOf` 三端一份；**通话里只有通话成员参与**：派活候选、开局卡回落只在名单里挑，agent @ 了通话外的不接力（群里一句），人亲手 @ 了通话外的先落并集名单再落开场白；agent 要拉人先问用户、口头同意后调 `invite_to_call`（不过审批门，每只都挂）。TTS 走 `/llm/v1/speech`（`model_route.kind='tts'`，钱按字符数、听的人用自己的额度），没订阅 / 网关不供语音不画语音钮。「一段写完就出声」：delta 快照按空行切段、完成的段立刻合成，终态补读没读过的；全局串行播放、预取下一段。「结束」= 全组（空名单事件，二次确认），「静音」= 本机；「我在听」是本机状态不落盘。音色按 agent_id 派生（同头像）。**拉进来的先开口**（ADR-0272）：新名单落下后对新增的每只各落一条带 `greeting` 记号的招呼开场白并起一轮，人亲手 @ 通话外的那条不打；不 @ 也能聊走 ADR-0270 的派活，且**通话里必须有人应**（ADR-0275）：闲聊 / 分类失败落到最近开口的那只，只有一只时不问分类器。**人说话走 macOS 本机识别**（ADR-0273）：`native/MrOttoSpeech` 一个 Swift helper（SFSpeechRecognizer，on-device、不过网关），进通话就开麦、说完停顿自动当成一句发出（不 @ = 派活）；两道 TCC 权限没过各自说清去哪儿勾。**有回声消除就不半双工**（ADR-0277）：helper 开系统 VPIO，麦真常开，人在 agent 说话时开口（够长、不像回声）= 插嘴，停播放；断句按能量（停嘴 700 ms / 没说完 2.5 s）、TTS 按句出声、`level` 画声浪；开不了 AEC 才半双工闭麦；AEC 开着时 agent 的语音由 helper 自己的音频引擎播（ADR-0280，不被 VPIO 压低）。**全屏通话视图**（ADR-0278）：点通话栏展开，头像里有发起人 + 自己（别的成员是否在听不落盘、不画），每格在说 / 在想 / 在听，字幕 + 同一批控制 | ADR-0271、ADR-0272、ADR-0273、ADR-0275、ADR-0277、ADR-0278、ADR-0280；`src/shared/voiceCall.ts`、`services/runtime/src/inviteToCallTool.ts`、`src/renderer/src/components/VoiceCallBar.tsx`、`VoiceCallOverlay.tsx` |
| `openTurns`（排队中 / 正在回复） | 云会话里「谁欠谁一个回答」的**日志投影**，不是内存队列的状态：点了名的 `user_message` → 它之后那只 agent 有没有动静 → 有没有它的 `turn_ended`，三种事件形状配对出 `queued` / `running`，收了口的不出现。runtime 的重启补跑与渲染层的状态行共用这一份纯函数 | ADR-0220（#932 坑 ②）；`src/shared/turnLedger.ts` |
| 补跑记号（interrupted） | daemon 重启补跑一条未收口的开场白之前，先落一条 `turn_ended{outcome:"interrupted", agentId, readUpToSeq, error:"重启补跑第 N 次"}`——**每只 agent 一条，不是每条开场白一条**：`readUpToSeq` 取这只 agent 全部待补跑开场白里最早那条的 `seq − 1`（对全部开场白中性，不会误收口更晚的那条）。补跑次数按「该 agentId 在这条开场白之后的 interrupted 条数」现数（晚开的自然少算），达到 `MAX_CATCHUP_ATTEMPTS`（3）改落真正的收口（`outcome:"error"`，`readUpToSeq` 取这条开场白自己的 seq）并停止补跑。是把「重启到底试了几次」做成可从日志推导的答案，**否决内存计数器**（重启即丢，#933） | ADR-0225（#957 A-9）；`services/runtime/src/sessionService.ts` 的 `MAX_CATCHUP_ATTEMPTS` |
| agent 头像（内置像素图） | 工作区 agent 在群聊气泡旁与智能体名单页画的那张脸：13 张内置像素人像（`src/renderer/src/assets/agent-avatars/`），**不落库、按 `agent_id` 哈希派生**（改名不换脸、删一只别人不换脸），管理员固定第 7 张，同一工作区内撞脸按名单顺序（created_at 升序）往后挪。人类成员的头像另一条路：`profiles.avatar_url` 随成员表批查进 `WorkspaceMemberRow.avatarUrl`，没设过退回首字母 | ADR-0229，#971；`src/renderer/src/lib/agentAvatarSlot.ts`、`agentAvatar.ts` |
| 中间步骤（`isAgentStep`） | 云会话时间线上一只 agent 一轮里最终答案之前的那几条 `assistant_message`（要了工具的、或一个字没说的）。ADR-0229 把它们折成一行 `<details>`（「「运营」处理过程 · 3 步 · 5 次工具调用」），**ADR-0250 连那一行也撤了**：判据合进 `hiddenFromCloudTimeline` 第 ⑥ 条，整段不上时间线，`foldAgentSteps` 那台状态机（它的全部理由就是给折叠头算摘要）随之删掉。**渲染层的取舍，不动日志不动投影**；「它此刻在忙」改由输入指示器说（下一条） | ADR-0229 → ADR-0250，#971 / #1055；`src/renderer/src/lib/cloudTimeline.ts` |
| 输入指示器（typing indicator） | 云会话时间线末尾那枚三点跳动的气泡，画在 `openTurns` 里 `state === "running"` 的每一行上：头像 + 名字 + 一张与 `AssistantMessageRow` **逐字同款**的 `muted` 气泡——答案落下来是同一张气泡里点变成字。中间步骤自 ADR-0250 起整段不画，所以它是「这只 agent 在忙」在界面上**唯一**的痕迹，不是装饰。`queued` **故意不画**（它一个 token 都还没跑，画上去就是撒谎的勾），照旧一行小灰字，两者的分别是结构性的而不是「跳的点 vs 不跳的点」。组件是 assistant-ui registry 抄件，唯一非机械改动是 `aria-label` 开放成 `label`（群聊里同一刻可能有好几只在打字） | ADR-0250，#1055；`src/renderer/src/components/elements/typing-indicator.tsx`、`CloudSessionPage.tsx` 的 `PendingTurnLines` |
| 时间线窗口 | 本机会话时间线的**渲染层**后缀窗口：assistant-ui runtime 里是全量消息，DOM 只挂载尾部一段（首窗 60 条，顶部哨兵向上补挂，只增不缩），窗口状态在 OttoThread（`hiddenCount`）。与它配套的是 `toThreadMessages` 的身份保持（append-only 前缀复用，流式期间转换缓存才命中）。划词引用只选得到窗口内的内容；分区跳转窗口外目标走 reveal 桥 | ADR-0285，#1190；`src/renderer/src/lib/messageWindow.ts`、`components/assistant-ui/thread.tsx` |

- **Files 面板**：右侧槽位第 6 个互斥视图，工作区文件树 + 过滤/内容搜索 + 只读预览。树和搜索都全显（含 `node_modules`、隐藏文件），一条规矩管两处。纯人用旁路：内容不进事件日志、不进模型上下文，`@` 动作只塞路径不塞内容（ADR-0092，同 ADR-0031 的边界）。
- **`@路径` 高亮**：输入框里 `@` 开头的路径跟 `$skill` / `/命令` 一样画成 chip。判定方式不同——那两种比名单，路径没有名单可比，靠形状（`@` 前是行首或空白、后跟非空白、句末标点不吃进去），见 `aui/ottoDirectives.ts` 的 `ottoPathFormatter`。
- **打开方式菜单**：Files 面板预览头上那颗外部打开按钮，列出本机探到的编辑器（`EDITOR_CATALOG` 固定名单 × `/Applications`、`~/Applications`）+ 系统默认 + 访达。每次弹菜单不记忆；`filesReveal` 只认 `filesEditors()` 给过的名字（ADR-0092 决策 7）。
- **懒加载列目录**：Files 面板展开哪个目录才发一次 `filesList`，不是开面板扫全树。树能全显（含 `node_modules`）的前提就是这条（ADR-0092）。
- **任务 / 项目（侧栏分栏）**：任务 = 内置 Default 工作区（文档区 `Mr Otto/Default`，#559）的会话，平铺不露路径——新手不用先懂「文件夹」；项目 = 其余工程的分组视图。语义钉在内置路径（`builtinWorkspace`）上，与设置里改没改默认工作文件夹无关。档位纯本机视图状态，不落日志。
- **打包为项目**：Default 会话里产出成形时 agent 主动询问、经审批把点名的文件搬进 `文档区/Mr Otto/<项目名>`（`package_project` 工具，唯一故意越出围栏的能力）。旧对话留在任务栏不动（日志 append-only）；时间线上出一张带「在新项目开会话」CTA 的卡（ADR-0135）。

- **协作记录**：非 git 工作区根目录那本 `Mr Otto 协作记录.md`。多只水獭同时在一个文件夹里干活时互相留言——时间 · 谁 · 动了哪个文件 · 一句为什么（`write_file` 的 `reason` 参数供料，没写退回会话标题）。明文可见是**故意的**：人和水獭都要读，与 ADR-0155 那把刻意藏起来的锁正相反。配套的是**文件级互斥**：撞上同一个文件才拦一次要求重读，不同文件一律放行（ADR-0161）。
- **连带授权（分享即借出）**：`@好友` 分享会话时，把**这个会话用到的**那几台 MCP 服务一并借给对方——邀请码随信封走，B 在分享卡上一键握手，不再手动粘码。范围是「实际调用过 + 此刻还连着」（不是 A 接通的全部），A 在分享前的确认框里默认全勾、可减。**默认不等于自动**：那一次确认是知情同意，因为白名单内没有逐次审批（ADR-0151）。没用到 MCP 的会话不弹框，纯对话分享一步不多（ADR-0177）。
- **`session_shared`**：`@好友` 分享成功后落在**发送方**会话日志里的一行标记事件（log-only、`ignorable`、模型不消费）。存在的理由是那条正文不进模型（ADR-0179），于是时间线上本来什么都没有、看起来像消息被吞了；而分享可能连带借出 MCP 服务，「谁在什么时候把这条会话连同哪几台服务给了谁」只有它答得出。**进隐私闸**——发给对方的包里剥掉（那是发送方的社交关系与授权史）。只记好友显示名不记 uid：日志是历史记录，记的就该是当时那个名字。
- **文件级互斥**：ADR-0161 收窄 ADR-0152 的结果。非 git 目录里，判据从「同一个文件夹同一时刻只跑一条 turn」变成「别的家族在我看过这个文件之后动过它」。git 仓不变——`git checkout` 抹掉的是 `.git` 里的状态，文件级看不见。
- **登录记录**：这台机器上 `userData/auth.json` 里**存没存着一份 session**（`authStorage.hasSession()` —— 值解析得出非空 `access_token`，且 key 里不含 `code-verifier`）。判据不能松到「文件里有任意一个 key」：PKCE 的 `-code-verifier` 是 supabase 在 `signInWithOAuth` 一开始就写的，它证明有人**点过**登录按钮，不证明登录成功过（ADR-0183 收紧 0182，#729）。它**不等于**「此刻登录着」（`account.signedIn`）：后者要等主进程 `restore()` 的一次网络往返才准，冷启动那一拍必假、断网时永远假。**进门闸**（`SignInScreen` —— 没有登录记录时 app 只画这一屏、里面的树一个都不挂）认的是前者，判定收在 `lib/identity.ts` 的 `needsSignIn` 一处。代价是过期 session 拦不住，换来的是断网不锁人（ADR-0182）。
- **账号抽屉**：本机数据按登录账号分家的那一层目录（`<userData>/accounts/<sha256(uid) 前16位>/` 与 `~/.mr-otto/accounts/<同名>/`，ADR-0187）。`auth.json` **留在 userData 根**——它是 uid 的来源，抽屉名由它算出来；`updates/`（OTA 缓存）和别家产品的 skill 安装位是机器级，也不进抽屉。工作区级的 `<工程>/.mr-otto/` 跟着项目走，与抽屉无关。uid 不必等 `restore()` 的网络往返：supabase 落的那份 session 自带 `user.id`，和**登录记录**读同一个文件、同样同步离线（`sessionIdentity`）。抽屉名不可读，所以每间放一张 `who.txt`（同 `root.txt` 的做法：目录自描述，不建中心索引），界面上凡是说「去哪手改」的地方一律现取 `configRoot` 而不是写死路径。**存量能归属才归属**：升级后第一次开机时 `auth.json` 里还有 session 才归给它，没有就停进 `accounts/_unclaimed/` 且不再自动归给任何人（ADR-0188 修正 0187 —— 「归给第一个登录的人」会把 A 的数据交给 B，#755）。**换号靠重启**（`needsRelaunch`：现在登录的人 ≠ 开机时打开的抽屉就 `relaunch`）——抽屉在装配那一刻钉死，重启换得干净；登出不触发。
- **作用域键（项目档）**：项目记忆归到「哪个项目」名下的那个字符串 —— `origin` 的 remote URL 归一化后的 `host/path`（如 `github.com/real-stanyan/Mr-Otto`），没有 remote 的仓退回项目根绝对路径。它同时是 `root.txt` 的内容、目录名的哈希原文（`memories/projects/<sha16(键)>`）和设置页那份清单的身份。**不是本机路径**：路径每台机器都不同，项目档因此从来没跨过机器（ADR-0211 收掉 ADR-0207 的这条天花板）。与它成对但不同的是**本机项目根**（`root`）—— 提示词里的「当前项目（…）」和全局档点名检测按它走，因为模型写进记忆里的是它工作的那条路径。
- **墓碑（`merged.txt`）**：旧项目记忆目录被并进新作用域键后留在原地的标记文件，内容是并去了哪把键。列表 / 注入 / 再迁移都跳过它。**为什么是加文件不是删目录**：记忆云同步没有墓碑机制（`planReconcile` 里「本地有、云端没有」一律推上去），删除会被任何一台还揣着旧副本的机器推回来，变成删了又回的无限来回；它同时保证「跑第二遍什么都不合」，用户在设置页手删的条目不会下次开机又冒出来（ADR-0211）。
- **静默漏跑（测试）**：一个测试文件存在、看着像测试，却一次都没被执行，而门禁全绿 —— 没有失败、没有 skip 计数、没有任何线索。本仓已知两种成因，形状完全不同：**文件名没落进 `vitest.config.ts` 的 `include`**（真的安静；补 `.test.tsx` 之前本仓没有任何 `.tsx` 测试跑过），和 **node 版本不对导致 worker 在跑第一条断言前崩掉**（`Errors N`，**退出码是 1、门禁是红的**，只是摘要行 `Test Files 370 passed (403)` 读起来像几个用例挂了 —— 那是归因错位，不是静默）。两道闸分别是 `tests/architecture.testDiscovery.test.ts` 与 `scripts/check-node.mjs`（ADR-0213，#897）。
- **代建（管理员生成 agent）**：工作区群聊里 @ 管理员说一句「建一只管 X 的」，管理员用 `create_agent` 工具整理出名字/职责/提示词/型号/连接器，弹一张逐字段的审批卡，人批准后 `workspace_agents` 多一行，群里下一句起就能 @ 它。只有 `agent_id = 'admin'` 那只有这把刀；`created_by` 记的是点火的那个人，不是管理员（agent 没有 uid，spec §4.2）。与桌面设置页亲手建的那条路落同一张表、同一种 id（ADR-0224）。
- **回执帧**：cs 协议里专门用来确认"一次上行调用到底有没有生效"的下行帧（`say_result`/`approve_result`/`stop_result`/`config_result`），与 `error`/`notice` 那条一次性的、跟被 `await` 的那次调用完全解耦的旧路径分开。判据是形状不是命名巧合：`{ ok: boolean; message?: string }`（`approve_result` 多带 `callId` 配对），桌面按帧类型各自挂一个（或按 `callId` 分 Map 的一组）挂起态等它，超时/断线/拒绝三处终态路径统一结掉。`say`/`approve` 长期没有这一类帧是 #964/#927 记录的协议缺口——旧协议下业务拒绝（限速、权限、无在跑对象）走的是与调用解耦的一次性 `error`/`notice`，按钮/草稿只能靠本地兜底（订阅 `workspaceGroupsError` + 定时器、种草稿）去猜；协议进位到 6（ADR-0227，#957 第三批）补齐三条回执之后，第四批（ADR-0228 决策 6）把审批卡那两条本地兜底**删了**（订阅 `workspaceGroupsError` 的「迟到拒绝」effect + 15 秒 `ackTimer`）——`approve_result` 已是权威，兜底在 `ok === true` 分支上只可能说假话（一次无关的工作区报错会把一张已经批准的卡改写成「被拒绝」）；composer 的「种草稿」同批收窄成只处理**确定**失败，`unknown` 改走「重新发送」那一行
- **订阅用户（isSubscribed）**：`status === "active"` 且有档的人。判据在 `src/renderer/src/lib/billingView.ts`，与真正花钱那层（`main/hostedQuota.ts` 的 `routeInput().subscribed`）**逐字同一条**；`null`（还没查到）一律算**不是**。这个人**不许自带 API key**（ADR-0248，#1051，同 ADR-0233 对云会话那条）：`routeModel` 在这一侧只剩 hosted / blocked，选单里只有「订阅」那一组（连免 key 的本机 Ollama 也不列 —— 留着就是一条「选单里没有、路由却通」的路），设置里收起「模型配置」与「子智能体」。**收起来 ≠ 关掉功能**：子智能体、代读员、后台小模型都改走订阅额度（三处都补了 `hosted`；两个辅助型号由 `visionModelFor` / `helperModelFor` 换成订阅供的最便宜那款）。
- **Auto（型号那一格）**：型号选择器里与具体型号并列的一个**取值**，不是并排的第二个控件。含义是「每轮起跑前先用最便宜那款读一遍这条请求，判 simple 还是 hard，再据此挑贵的还是便宜的；判不出来一律不改型号」。两处都有：工作区智能体那一格（云会话，ADR-0237）与输入框右下角那一枚（本机会话，ADR-0244），**判据共用 `src/shared/autoModel.ts` 一份**，两边只差怎么向网关证明身份。口令是 `AUTO_MODEL`（`"__auto__"`，同样只有一份——它要同时走选单 value、IPC 和主进程分支）。本机这一半的开关状态是 `model_changed` 的投影（可选字段 `auto?: true`，两种事件都带它），与「当前型号」「当前 lane」读同一条事件，所以 resume 自动回来、不加第二种持久化。至少要有两款可挑才画得出来——不到两款时它一律回 null，画出来就是点了什么都不发生的钮。
- **说话人标签（safeSpeakerLabel）**：工作区群聊里一条消息投影进模型上下文时，署名前缀 `[label]: ` 里的那个 `label`。三层各自幂等地过一遍（`daemon.labelOf` / `sessionService.say`+`logChat` / `deriveMessages` 投影）：先 `promptSafe`（折叠空白；结构分隔符各换一个同形不同码的替身，五个：`]（）「」` → `］()｢｣`，挡住提前闭合结构或伪造换行），再判保留名——「系统」这个名字只有 `uid === "system"` 才用得了，别人撞上就退回 uid 前 8 位（挡住冒充护栏/接力那几句系统旁白）。这是**结构闸**，不是替代写入时的校验：`profiles.name` 本身没有写入校验，旧日志里已经落库的字段也要靠它兜底。配套纪律（**半角 `(` `)` 与 `[` 一律不转是故意的**：前两个在中文正文里太常见，单独一个 `[` 不闭合任何东西）：**新写模板时，围着成员可写字段的分隔符不许用半角 `()`，也不许把 `[` 当闭合符**——那两种写法这一层封不住；OWN 记忆块头原来正是半角括号，而 `）`→`)` 这条替换会给名字**制造**出一个匹配的闭合符（比不转还糟），已改成全角（ADR-0228 决策 4）。正文那一半由 **正文闸**（下一条）补上；`promptSafe` 另剥 `\p{Cf}`/`\p{Cc}`（U+200B/U+2060/bidi 控制符不在 `\s` 里，标签能对人眼伪装，ADR-0230）（ADR-0226，#957 第二批；`src/shared/promptSafe.ts`）。
- **补跑收口队列**：daemon 重启后决定「上个进程没跑完的哪几条开场白该被当场关掉」的那条规则——把每只 agent 此刻所有未收口的开场白按 seq 升序排一列，从队头起**连续的** `kicked`／`exhausted` 各落一条收口（`readUpToSeq` = 那条自己的 seq），撞上第一条 `runnable`／`unknown`／`skipped` 就停手，后面的留着不落；有 runnable 的 agent 落一条 interrupted 记号，`readUpToSeq` = 此刻队头 seq − 1。判据是「连续前缀」而不是「最早那条 runnable 之前全关」：后者会把排在它前面的 `unknown` 一起静默关掉，而 `unknown` 是「这一刻问不出来」，不是「确认不在籍」。被踢的那条另落一条模型可见的系统发言说「不作数」——日志 append-only，正文删不掉，追一句是天花板（ADR-0228，#957 第四批；`services/runtime/src/sessionService.ts`）。
- **`CloudAck`**：桌面对一次云会话上行调用（say／approve／stop）结果的三态回值 `{ ok: true } | { ok: false; message: string; unknown?: true }`（定义只在 `src/shared/shellBridge.ts` 一处，贯穿主进程→store→渲染层）。第三格是关键：`unknown` = 15 秒 ACK 超时或连接 `:gone`，意思是**不知道有没有生效**，与「确定没发出去」（拒绝／忙／`sendFrame` 失败，都不带 `unknown`）是两件事、该做的动作相反。把 `unknown` 当「没发出去」办（把原文种回输入框）就是让用户再按一次回车，同一句话发两遍、两只 agent 各跑一轮、两笔都记在 owner 账上（ADR-0228，#957 第四批）。

## Key invariants

- `AGENTS.md` is always the single source of rules; `CLAUDE.md` is always just the `@AGENTS.md` empty shell
- No `HANDOFF.md` is created — handoffs happen via issue comments (append-only, timestamped)
- The gate command must be byte-identical in AGENTS.md and ci.yml (CI == Gate contract)
- One agent completes a task from start to finish; handoffs only happen at task boundaries
- **正文闸（promptSafeBody）**：`[label]: text` 框架下，成员正文里一行 `\n[系统]: …` 就是一行干净的伪造说话人行——标签栏位硬化了，正文这条路结构性地封不住，只能让「换行之后行首的 `[`」失去结构意义（换成全角 `［`，替换不是删除）。**只在投影时做**（`deriveMessages` 的 chat_message、带 `fromUid` 的 user_message、压缩兜底重注的那份原文），日志与界面一字不动，本机会话（无 `fromUid`）一个字节不改。第一行不碰：它前面已经是真实的 `[label]: ` 前缀，`[Rick]: [系统]: …` 不是干净的一行——这是天花板（ADR-0230 决策 2，#965；`src/shared/promptSafe.ts`）。
- **接力下界（RelayBounds）**：runtime 每起一个 turn 要算两样日志投影——「这一轮欠着的最大接力 depth」（`openingDepthFor`）与「这条接力链」（`relayChain`），原来各做一次全量 load。两条**保守下界**让它们只读尾段：`closeBound[agent]` = 该 agent 全部 `turn_ended` 的 `max(readUpToSeq ?? seq)`（seq ≤ 它的点名一定已收口），`lastHumanOpening` = 最后一条人话点名的 seq（`relayChain` 的 start 就是它）。装配时从 seed 播种、`notify` 里逐条推进；正确性靠**单调下估**——漏推进只会偏小、多读几条，不会偏大（ADR-0230 决策 4，#958；`src/shared/agentRelay.ts`）。
- **沙箱免审（sandbox_approval）**：工作区一格、owner 可翻的开关（0026，ADR-0231）。`auto` = runtime 在审批门前直接放行容器里的 `bash` / `write_file`（容器是隔离面、凭据不进容器，所以这道闸挡的是空气）；`ask` = 今天的行为，每次弹卡。只管沙箱那两把刀——好友代理连接器与 `create_agent` 不在射程内。放行照样落 `approval_decision`（reason 写明是策略放的），查不到策略一律按 `ask`
- **容器锁（workspaceLock）**：云 runtime 里同一个工作区的容器同一时刻只让一条会话动手（ADR-0232）。按工作区一把、按会话 FIFO 排队；**第一次碰容器才拿**（read_file / write_file / bash），这一轮收口才放，只聊天的 turn 不排队；等待可被停止键打断，排队时群里出一句。与桌面的工作区互斥（ADR-0152，拒绝不排队）是同一个问题在两种进程模型下的两个答案
- **工作区快照（ttlCache）**：runtime 每 turn 起模型前要读的那几样（agent 名单 / 成员名单 / 好友授权）的 60s 记忆化（ADR-0232）。「现读」的纪律没变，变的是读的是一份由本进程写路径失效、由人的动作强刷的快照：`say()` 强刷名单、`create_agent` 落库失效名单。记忆那次故意不缓存
- **型号优先级链（requestedModels）**：agent 的型号白名单按顺序取托管网关供着的第一个，链是 agent 白名单 → 网关第一款（ADR-0232；ADR-0233 之后中间那级「工作区配的那款」随自带 key 路一起删了）
- **云会话只走订阅额度（ADR-0233）**：工作区云会话的模型路由只有 hosted（所有者有活跃订阅，扣所有者，ADR-0217）/ blocked 两态，**没有工作区自带 key**（推翻 ADR-0202 与 ADR-0203 D4 的改道）。额度用完不改道、turn 失败并说人话，网关给的 resetAt 记在会话级 RouteMemo 里省掉窗口内注定 429 的请求。`cs_config` 只剩仓库那一组（协议 7）
- **仓库跟着工作区走（ADR-0234）**：云会话的仓库配置是**工作区**的属性，读写都走控制房的 `workspace` / `config` 帧（协议 8），不以「开着一条这个工作区的云会话」为前提；界面在工作区设置页的「仓库」tab，云会话头部只剩「设置」一颗钮。**没配仓库是正当状态**——文案、运营类的工作区在空目录里干活，不是没配完
- **内容指纹（deploy stamp）**：一个服务端此刻这份代码的 12 位指纹（ADR-0258）。算法是「esbuild 打一遍，把**实际编译进去的每一个文件**按路径排序逐个哈希内容再哈希一遍」——它回答的是「此刻部署上去会不会改变什么」，所以既不是 git sha（app-only 发版不该把 worker 判成陈旧），也不是按 `services/**` 路径猜（协议进位那种提交一个字没碰它）。edge 由 `--var BUILD_STAMP` 注入、`/healthz` 报出；runtime 由 esbuild `define` 进 bundle、daemon 起来打进那行「就绪」。`npm run deploy:check` 拿它比对，回 current / stale / **unknown** 三态
- **点名收件箱（workspace_mentions）**：「谁在工作区里 @ 了我」的那张表（0030，ADR-0256）。一行 = 一次点名，主键 `(uid, session_id, seq)`；**是事件日志的投影不是第二个事实源**——存在的唯一理由是桌面够不着 VPS 上那份权威日志，而角标必须在一条会话都没开的时候画得出来。写方只有 runtime（service key），读方只有被 @ 的本人且要在籍。@ 一个人**不起 turn 不花钱**（`resolveTargets` 照旧只认 agent id），点到的人走 say 帧上另一格 `memberMentions`
- **云会话时间线只画群里发生的事（ADR-0235）**：`session_created` / `agent_briefed`「就位」/ `request_envelope` / **批准了的** `approval_decision` 都不进云会话时间线——它们是机器的内务，不是群里发生的事（本地会话那套 AUDIT 小灰字照旧）。**拒绝仍然画**：它中断了流程。发言人一律左对齐、标签只有「名字 · 时间」，人和 agent 同形；归档在侧栏那条会话行的 ⋮ 里（控制房 `archive` 帧，协议 9），**没有删除**——云会话故意删不掉
- **版本串（记忆 CAS）**：`workspace_memories` 乐观写的前置条件从「此刻 content 仍等于读到的原文」换成「此刻 `updated_at` 仍等于读到的**原串**」——PostgREST 回的 `2026-09-06T01:02:03.123456+00:00` 原样递回 `.eq()`，两边都按 timestamptz 比、精度一个位都不丢（原来否决 updated_at 的理由是 `Date.parse` 砍到毫秒，对原串不适用）；行不存在 = 桌面 `""` / runtime `null`，走 insert、撞主键算冲突。整份正文不再编进 URL（2200 字共享档 ≈ 20 KB）。已知代价：同一毫秒两个写者（两端写的都是客户端 `toISOString()` 的毫秒值）且后者拿的是前者写之前的版本时 CAS 误放行（ADR-0230 决策 7，#962）。
- **磁盘地板 / 用量预算（ADR-0287）**：团队工作卷没有内核配额，**这不是「还没做」是实测的结论**——runtime VPS 是个 Incus/LXC guest，没有 loop 设备、没有 xfs，`--storage-opt size=` 更是被静默收下而不生效；真配额只剩「动 Incus 宿主」那一条，够不着。剩下能做的两层判决**故意不同**：**地板** = 这台机器可用空间低于 2 GiB 时 `ensure()` 硬拒（机器要死了，拒绝写是唯一救得回来的动作；措辞要说清是整台机器不是这个团队），**每团队预算** = 卷用量超过 10 GiB 时群里说一句、**照样交出容器句柄**（`ensure()` 是拿句柄的唯一入口，拒了就连清理用的 `bash` 一起挡住了）。用量是 `ensure()` 里 fire-and-forget 的一次 `du`，所以报的永远是**上一次**的读数
