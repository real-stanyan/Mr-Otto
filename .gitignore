.env
node_modules/
dist/
out/
*.db
.DS_Store
.superpowers/
test-results/
playwright-report/
# supabase CLI 的本地缓存（`supabase --version` 就会生成 cli-latest），不是部署产物
supabase/.temp/

# expo prebuild 的产物（真机 build 才需要；Expo Go 那条路用不到）
mobile/ios/
mobile/android/

# wrangler 本地开发的假 secret 与缓存（services/edge）
.dev.vars
.wrangler/

# 非 .claude 约定位置的 agent lane worktree（另一条 lane 建过一次，见 issue #616）
.worktrees/

# runtime-deploy.mjs 现生成的瘦身依赖清单（版本从根 package.json 现读，
# 是构建产物不是源码，见 scripts/runtime-deploy.mjs，ADR-0199 task-11）
services/runtime/deploy-package.json
