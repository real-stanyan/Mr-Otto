# Mr Otto

Mr Otto（曾用名 otter，仓库目录沿用 Otter）是 macOS 桌面 GUI agent 工具（每个 bot 是一只会用工具、有独立沙箱的水獭）。MVP 完成标准：单 agent + 3 工具（读/写文件、bash）+ event-sourced 会话日志 + replay UI + 危险操作审批 UI + 模型切换 + ExecutionWorld 接口（LocalWorld 实现）。明确不做：通用多 agent 编排框架——工作区群聊里 agent 互相 @ 接力是唯一例外（带周期护栏与棒数上限，ADR-0223；本机子 agent 仍不能再派子 agent，ADR-0047）、插件系统（skill 库是纯提示词注入，不算插件系统，见 docs/adr/0007）。MCP 做 client 那一半（接外部 server 的 tools/resources/prompts，见 docs/adr/0049），不做 server。

架构参考项目（学习/对照用，不引入为依赖）：
- **DeepSeek Harness**：三原则 —— event-sourced 会话日志 / 工具中间件管线 / capability seam（ExecutionWorld）
- **pi**（https://github.com/earendil-works/pi）：极简 agent harness。对照点：append-only AgentMessage 流（"消息完成后不可修改"）、细粒度生命周期事件（turn_start/tool_execution_start…）、工具 execute 抛异常报错 + 返回 `{content, details}`、刻意不做内置权限系统（隔离交给容器层 —— 与 otter 的 ExecutionWorld/v2 Docker 思路互证）

> This file is the single source of truth for ALL AI coding agents, whatever the tool (Claude Code, Z Code, Cursor, etc.).
> Rules live here and only here. Do not duplicate them elsewhere.

## Tech stack

Electron（主进程 = Node agent 核心，ADR-0001）
React + Zustand（渲染进程）
Tailwind CSS + shadcn/ui（渲染进程样式/组件库，ADR-0010；存量 app.css 待 harness 完工后整体迁移，新增 UI 即日遵守）
TypeScript（strict）/ Node.js
SQLite（better-sqlite3，事件日志持久化）
vitest（测试统一放 `tests/`，镜像 `src/` 结构；不与源码同目录）
直连 OpenAI-compatible API（不用 LangChain）；模型 adapter 切换 DeepSeek / Claude / GLM
`@modelcontextprotocol/sdk`（MCP 客户端；只允许 `src/main/mcpClient.ts` import，见 docs/adr/0050）
Swift + SwiftUI + DynamicNotchKit（native/MrOttoIsland；macOS 灵动岛原生 helper，主进程 spawn，stdio NDJSON 桥；ADR-0061）
v2：Docker per bot（dockerode，自托管 VPS）

## Hard rules

- append-only 事件日志是唯一事实来源；先落盘再喂模型（model-visible means logged），任何投影（模型上下文/UI）必须可从日志推导。
- 渲染进程只通过 `ShellBridge` 接口与后端通信，禁止直接触碰 Node API（ADR-0001 的后悔药）。
- 工具实现只依赖 `ExecutionWorld` 接口，禁止直接 import fs / child_process。
- SessionEvent schema 变更必须向后兼容（旧日志必须永远可重放）。

> Any clause in the protocol body marked **Hard rule** (e.g., the hard rule in the "Roles of issues & PRs" section) **counts as part of this section and is protected under L1**: the criterion anchors to the marking itself, not to which section the clause physically lives in (ADR-0018).

## Working agreement (multi-agent)

### On starting a shift (the three start-of-shift steps)

1. **Sync, then read**: `git fetch origin` + fast-forward the local default branch (`git pull --ff-only` while on it) — the repo is the only shared memory, and an unfetched clone is somebody's stale cache of it (a stale clone even means stale *rules*: this very file is version-controlled). Fast-forward impossible = the local default branch has diverged: stop, open an issue, don't build on a forked base (ADR-0046). Then `git log --oneline -10` — see what happened recently
2. Check GitHub Issues — **first look for open handoff issues** (the previous shift's Memory is in there; reading one and closing it = taking over that lane, see ADR-0005. Several open = parallel lanes: take over at most one, leave the rest untouched — see "Parallel shifts" (ADR-0048). If none found → check whether the most recently closed issue has a "no next shift" terminal declaration: if yes = a compliant terminal shift (ADR-0009), start work normally; if no = the previous shift ended out of compliance, open a Protocol gap issue to record it — either way, rebuild context from git log + open issues), then check other open tasks and notes.
   **Before opening a new Task issue, or claiming one from the frontier, search for a collision first — closed issues included** (project ADR-0148): `gh issue list --state all --search "<关键词>"` plus `git branch -a --list '*<关键词>*'`. A hit gets read before anything else: already done → don't redo it (open a new issue referencing it if it still needs changes); half done → continue from there, don't restart. Step 1's `git log --oneline -10` does not cover this — skimming the last ten commits builds a background impression of what happened, it is not a search for one specific need. A compliantly closed issue is exactly the blind spot (issues #611/#612 were the same need done twice)
3. Run the gate command (see below) to confirm the baseline is green — if it's red, fix it first or open an issue; don't start work on a broken baseline
4. Run `npx gearbox-agents version` to self-check the protocol version — if behind, run `npx gearbox-agents update` to backfill (pull-triggered, ADR-0026/0028; this step is the receiving end aligning with upstream, not upstream pushing. If a local Gearbox checkout is installed, you can also run `gearbox-version`/`gearbox-update` directly)

### While working

- **A need in hand gets its Task issue opened before the exploring starts** (project ADR-0148), not after the work is done or half done — one line of the need as stated plus "exploring" is enough at that point. This is what survives an abnormal end: a session killed by an app quit gets no chance to write anything, so the only reliable trace is the one already in the repo while it was still alive — an issue opened up front is still open and unassigned afterwards, and the next shift's step 2 walks into it. Issues opened but not finished are the intended cost: an open empty issue says "somebody touched this and didn't finish", which is the sentence missing at collision time. At shift-end they follow On ending a shift item 3 like any other
- Commit in small steps; the message should spell out the **why**, not just the what
- **Protocol files stay committed — never add them to `.gitignore`**: `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, `docs/gearbox-adr/`, `.gearbox-version`, `.github/workflows/ci.yml`. The repo is the only shared memory between shifts; an ignored protocol file exists locally but never reaches the next agent's clone (ADR-0037)
- One agent sees a task through from start to finish; handoffs only happen at task boundaries (issue closed / PR merged), never mid-task
- Non-trivial changes go through a branch + PR; typo-level tweaks can go straight into main
- **Project-owned** architectural decisions go in `docs/adr/` (one decision per file, starting at 0001); protocol ADRs live in `docs/gearbox-adr/`, managed by the gearbox tooling — don't hand-edit them. **Project ADR numbers are claimed at merge, not at branch time** (project ADR-0074, mirroring what "Parallel shifts" already says for protocol ADRs): before merging, re-fetch; if another PR landed on your number, renumber your ADR to `max + 1` inside your PR, add an `原为 ADR-00XX` line at the top of the file, and update every in-repo reference to it. Commit messages can't be rewritten, so the alias line is what keeps old references resolvable. The gate asserts no two files share a four-digit prefix (`tests/docs/adrNumbers.test.ts`)
- Look up domain-term definitions in `CONTEXT.md` — **two sections: protocol terms and product/technical terms** (ADR-0070); add new terms to the matching section as they come up (product concepts belong there too; no back-filling of historical debt)
- **读到「做不了 / 不在本仓 / 只能维护者做」这类判断时，先花五分钟验前提本身再决定跳过**（`ssh` 能不能连、`ls` 有没有那个目录、`grep` 有没有那个符号）——这类判断读起来像调查结论，实际往往只是上一班没试，写进 handoff 就成了下一班的既定前提，本仓已连错四次（ADR-0134）。验完确实做不了，把**验的方法和结果**写进 issue，让下一班不用再验

### Roles of issues & PRs

Issues and PRs are the timestamped, append-only, non-decaying conversation carriers between agents (and between agents and humans). In this protocol they have **three non-overlapping roles** — every issue/PR should fit into one of these:

| Role | When to use | When to close |
|---|---|---|
| **Task** | There's an actionable thing to do | The task is done and the gate is green |
| **Memory** (handoff memory) | Leave a comment on the **handoff issue** (see On ending a shift) at shift-end, five-part format | The next shift reads it and closes the handoff issue = handoff complete |
| **Protocol gap** | Hit a question the repo can't answer (rule not written, ambiguous, boundary unclear) | The gap gets folded into AGENTS.md / CONTEXT.md / an ADR |

Hard rules:

- **When you hit a question this repo can't answer, you must open an issue (Protocol gap type) — silent judgment calls are not allowed.** This is the only entry point for the protocol's self-repair — it turns gaps from "tacit understanding" into something explicit, discussable, and closeable.
- **Memory five-part format** (the minimum valid format for a handoff comment, ADR-0004): ① what's done ② what's blocked ③ what's next ④ close the issue if the task is complete ⑤ **rationale / trade-offs** — required whenever this shift made a non-default decision (what was chosen, why, and what premise failing would overturn it); if no decision was made, write "none" — don't omit it. Missing any one item means the handoff doesn't count. Across all five parts: content already captured in a durable artifact (ADR / issue / PR / commit / diff) is referenced by number or path, not restated — copies decay, references don't (ADR-0045). Inline belongs only what no artifact carries.
- **Handoff = the moment the issue closes / the PR merges**, not just feeling like things were "explained clearly." Switching agents without closing the issue is a mid-task handoff, which violates the previous section.
- **A PR is the implementation vehicle for a Task, not a separate role**: a PR references the Task issue it implements, and closes that issue on merge. New issues found during PR review get their own issue — don't pile them up in PR comments.

**Task ordering (blocking edges, ADR-0044)**: when one Task depends on another, the dependent issue's body declares each prerequisite with a literal `Blocked by: #N` line (one per blocker). A shift claims only **frontier** tasks — open tasks with no open blockers; when a blocker closes, its dependents join the frontier. Plain text, grep-able, no Projects/labels needed. This is a hygiene convention — a stale edge costs a judgment call at claim time, nothing more.

**Claiming (ADR-0047)**: a claim = assigning yourself on the Task issue (`gh issue edit <N> --add-assignee @me` — the GitHub account the agent acts under); first assignment wins, visible and timestamped. No triage permission → a "claiming this" comment instead. An open frontier task with no assignee and no claim comment is free. A shift ending with the task unfinished states in its progress comment whether the claim is released (unassign) or carried; a dangling assignment from a shift that left no comment is stale, not binding. Single-human repos may skip claiming — with one queue reader it informs nobody; its value begins at the second human.

> Why use an issue comment instead of a standalone handoff file: see `docs/gearbox-adr/0003-issue-roles.md`. Why Memory lives in an open handoff issue rather than a closed Task issue: see `docs/gearbox-adr/0005-handoff-lives-in-an-open-issue.md`.

### PR disposition (merge rules)

Four rules (ADR-0007):

- **Always merge via merge commit** — never squash, never rebase: the why behind small-step commits is a protocol asset (the repo is the only shared memory between sessions), and squashing is equivalent to deleting memory; locking in one style keeps history predictable.
- **Who merges**: the PR's author agent merges it themself once CI is green. Protocol changes follow the tier system (see "Changing the protocol itself"): L1 waits for `stanyan` agreement, L2 is autonomous.
- **A second agent's review is not mandatory**: in serial repos only one shift is present at a time, and forcing mutual review would block at handoff boundaries; parallel lanes (ADR-0048) don't change this — review stays optional, because the quality backstop never depended on serialization: the CI gate + the `stanyan`'s after-the-fact veto (revert + reopen the issue), plus branch protection where configured (ADR-0042).
- **Don't take over someone else's open PR** — that's a mid-task handoff (see While working). Exception: the handoff issue explicitly transfers it, or the `stanyan` directs it.

If a PR is still hanging open at shift-end, the task isn't done: per item 3 of On ending a shift, write progress into the Task issue's comment and leave the PR open.

### Changing the protocol itself (rules for changing this file)

Agents can modify AGENTS.md, but **the change is tiered by its content** (ADR-0006):

| Tier | Content | Process |
|---|---|---|
| **L1 strict tier** | Hard rules / Gate command / Tech stack / this section itself | issue + ADR + PR, **and the agent may only merge after the `stanyan` explicitly agrees, in the session or in a PR comment** |
| **L2 autonomous tier** | Working agreement (except Gate) / the index (Where to find things) | issue + ADR + PR, agent may merge autonomously |


The boundary of "Gate command" (ADR-0010): the command line itself, and **loosening/deleting/rewriting an existing gate-script assertion** = L1; **adding a new, stricter assertion** = L2, riding along with its own PR. Pure refactors (behavior unchanged) count as L2, with the burden of proof on the agent making the change.

**Test-type gates** (vitest / tsc / lint, ADR-0020): the config layer follows the rule above directly (tightening = L2 / loosening = L1 / the command line = L1); the test-content layer is tiered by **motive** — tests added/removed/changed in the same PR as the product code they follow = L2 routine development; **deleting to go green** (deleting / `.skip`-ing / weakening a test with no corresponding product-code change in the diff) = L1, and a silent skip is a violation. Deleting or skipping a test must state its motive in the commit message or PR body.

General rules (apply to both tiers):

- **All three pieces are required, none optional**: a matching issue (usually Protocol gap type) + an ADR (recording the decision and its rationale) + a branch PR (CI must be green to merge; closes the issue on merge).
- **A protocol change without an issue + ADR is out of compliance** and should be reverted, regardless of which tier it belongs to.
- **Protocol changes carry more weight than code changes**: code only needs an ADR for architectural decisions, but protocol changes always need one.
- **Humans retain an after-the-fact veto**: reverting the corresponding PR + reopening the issue undoes the change — even if it wasn't caught at the time.

**L1/L2 boundary criterion** (ADR-0012, **mechanism reference takes priority**): any new content that **references the L1/L2 tiering / Hard rules / Working agreement mechanism** (regardless of whether it's "optional" or touches an existing file) is treated as **L1**. Objective criterion — the text contains mechanism keywords like `L1` / `L2` / `Hard rule` / `Working agreement` / "tiered authorization", or semantically depends on these mechanisms to function (e.g., subagent routing that depends on L1/L2 to decide who gets assigned).

| Scenario | Classification | Basis |
|---|---|---|
| New template/subsystem that **references** a protocol mechanism | **L1** | ADR-0012 |
| New purely informational document (e.g. "how to contribute") that **references** no protocol mechanism | L2 | ADR-0012 |
| Modifying an existing protocol file (Hard rules / Gate / Tech stack / Working agreement content) | **L1** | ADR-0006 |
| Modifying the index (Where to find things) | L2 | ADR-0005 |
| A CONTEXT.md entry **defines** an existing mechanism (changes only CONTEXT.md + cites its source ADR + adds no new obligation/changes no process boundary — all three conditions required) | L2 | ADR-0019 |

**Definition exemption** (ADR-0019): the criterion targets **legislating** (adding/changing mechanism semantics), not **describing** (writing an already-legislated rule into the glossary). If any of the three conditions isn't met, or you're unsure → default to L1; don't grant yourself the exemption. Changing semantics under the guise of a definition is a violation — revert + reopen the issue.

> Why so strict: agents easily use "optional + purely additive" as an L2 channel to expand the protocol's boundaries (see the PR #21 retrospective — subagent-system referenced L1/L2 but self-merged as L2). This criterion closes off that path.

L1's "explicit agreement" is a weak-b form: it's enough for the `stanyan` to say "agreed" in the session or write "agreed" in a PR comment, and the agent presses the merge button itself. **For the PR-comment path, only a comment authored by the GitHub account `stanyan` names counts (ADR-0034)** — anyone else's "agreed" is not L1 approval. **In a repo with more than one human collaborator, only the PR-comment path is valid L1 approval (ADR-0042)** — in-session agreement stops counting (including in the maintainer's own session): in-session approval leaves no verifiable trace, so a merged L1 PR without the maintainer's comment would be indistinguishable from an impersonated approval. Single-human repos keep both paths. **GitHub's Approve button is not required** — the cost is that the `stanyan` becomes the L1 bottleneck, and that cost is accepted.

> This repo is downstream of Gearbox. Backfill follows the pull model (ADR-0026): step 4 of the three start-of-shift steps runs `gearbox-version` to self-check the protocol version, and `gearbox-update` to backfill if it's behind — it doesn't depend on upstream pushing, it proactively aligns with upstream.

> This repo doesn't publish protocol versions; the root `.gearbox-version` records the upstream Gearbox version at onboarding time, maintained by gearbox-install / gearbox-update, and read/compared by `gearbox-version` (upstream ADR-0023).

### Gate (the hard gate — must be all-green before shift-end)

```bash
npm test
```

> Fill in the gate to match your actual project — any command that can automatically assert "nothing's broken" works (rationale: ADR-0002). See ADR-0020 for how test-type gates (vitest/tsc/lint) map onto the L1/L2 tiers. The command must be byte-identical to `.github/workflows/ci.yml` (CI == Gate contract).

> `npm test` 断言两件事：`tsc --noEmit` 通过，且 `vitest run` 通过（项目 ADR-0053）。
> vitest 走 esbuild，只剥类型不校验类型——没有前半段，一个 TS strict 错误可以顶着全绿的门禁进 main。
> 写代码时的内循环用 `npx vitest --watch`（只跑测试）；`npm test` 是提交前和 CI 上跑的那一次。

CI (`.github/workflows/ci.yml`) runs the same set of commands; if it's red, merging is not allowed.

### On ending a shift (shift-end rules)

1. The gate is all-green
2. commit + push
3. Close finished Task issues as usual; for half-finished ones, write progress into that issue's comment
4. **Open a handoff issue for the next shift** (Task type, kept open, ADR-0005): the body states the current state and suggestions for next steps, and this shift's Memory comment (five-part format, ADR-0004) goes here. In multi-human repos the body also lists the Task issues this lane still owns (takeover = claiming exactly those), or marks itself **"context only"** when nothing transfers (ADR-0048). **This is the only entry point the next shift is guaranteed to encounter** — Memory no longer gets buried in a casually closed Task issue. **The sole exception — a terminal shift** (ADR-0009): when archiving / confirming there's no next shift, you may skip opening one, but you must explicitly declare "no next shift" + the reason in a comment on the last closed issue. A silent terminal doesn't count as terminal. Terminal is repo-level: with another lane still live (someone else's open handoff or claimed task), a terminal declaration is invalid — that's just a lane end (ADR-0048).
   **Closing someone else's "context only" handoff does NOT count as taking over a lane** (ADR-0069): that hands you context, not the baton. The shift that closed one still owes this rule — open its own handoff issue, or declare terminal. Two options, no third.

### Parallel shifts (multi-human repos, ADR-0048)

Serial single-human repos need none of this — with one live shift, the rules above already suffice and every rule below degenerates to them.

- **A lane = one shift + its claimed tasks.** Parallel shifts are allowed iff each works only on frontier tasks it has claimed (ADR-0044/0047). Disjoint claims = disjoint lanes; no other lock exists or is needed — task-level overlap is prevented at claim time, file-level overlap resolves in the PR merge like any concurrent development.
- **Handoff issues are per-lane**: shift-end rule 4 unchanged in shape, but a starting shift reads **all** open handoff issues, takes over **at most one** lane (claim its listed tasks, close its handoff), and leaves other lanes' handoffs open — closing another live lane's handoff is stealing its baton. A **"context only"** handoff (lane finished, nothing transfers) is closed by its first reader after reading.
- **Terminal declarations (ADR-0009) are repo-level, not lane-level** — see On ending a shift.
- **Protocol changes serialize at merge time**: two lanes may each open a protocol PR, but ADR numbers and the version bump are claimed at merge, not at branch time. Before merging: re-fetch; if a competing protocol PR landed first, renumber your ADR and recompute the version (latest tag + segment, ADR-0028) inside your PR, then merge.
- A stalled lane is released by the `stanyan`: unassign its tasks, close its handoff (the stale-claim rule in ADR-0047 already makes dangling assignments non-binding).

### Worktree discipline (project ADR-0149)

The main checkout is **frozen on the default branch and read-only**. Every shift works inside its own worktree.

- **Open one to start work**: `npm run lane -- <任务名>` (ADR-0150). It first searches same-topic issues (**closed included**) and branches and prints what it finds — that is the collision check of start-of-shift step 2, moved onto an action you already have to take (ADR-0154); it reports, it does not block. Then it fetches, opens `.claude/worktrees/<slug>-<随机>` off `origin/<default>`, and gives the branch a random suffix so two lanes on the same task name cannot collide; an existing directory or branch is refused rather than reused. Repo-local, no Gearbox involved.
- **A worktree is single-use**: one worktree serves one task, thrown away when done, and **never `git checkout`s to a different branch**. `npm run lane` stamps the branch it opened into the worktree's admin dir, and `pre-commit` refuses a commit made on a different branch (ADR-0154) — a worktree opened by hand carries no stamp and is not checked. A long-lived worktree that switches branches *is* a second main checkout — the branch-switching is back, just in another directory. Isolation comes from the single-use part, not from the word "worktree".
- **Why this one rule suffices**: under worktrees, having a branch pulled out from under you is *physically impossible* — git refuses to check the same branch out twice (`fatal: 'main' is already used by worktree at ...`). Nobody working in the main checkout ⇒ the branch switch never happens ⇒ no clause is needed to forbid it.
- **`npm run wip` instead of stashing** (ADR-0154): in your own worktree nothing can touch uncommitted work, so a WIP commit does the job and lives on your own branch, where nobody else's `pop` can reach it. Undo with `git reset --soft HEAD~1`.
- **Never bare `git stash` / `git stash pop`**: worktrees isolate files and HEAD, but `.git` is shared — **so is the stash stack**, and that is the one leak in the model (issue #543 symptom 2 was another lane's `pop` walking off with somebody else's work). Use `git stash push -u -m "<唯一标签>"`, capture your entry's SHA via `git stash list --format='%H %gs'`, restore with `git stash apply <sha>` (never `pop`), then re-find `stash@{n}` by tag and drop it. Better still: in your own worktree nothing can touch uncommitted work, so a WIP commit beats stashing.
- **Clean up at shift-end**: `npm run lane:prune` (dry-run; `-- --apply` to act) removes merged+clean worktrees and merged local branches. It never force-deletes, never touches a dirty worktree, and **never deletes a branch nobody has committed on** — that shape is a freshly opened lane, not a leftover twig (#449). Repo-local; this is why the repo no longer depends on `gearbox-agents prune` (ADR-0150).
- **Mechanical backstop**: `.githooks/pre-commit` refuses commits made in the main checkout on a non-default branch. Installed automatically by `npm install` (the `prepare` script, ADR-0150 — a setup command you must remember to run fails the same way a rule you must remember to follow does); to do it by hand: `git config core.hooksPath .githooks`. Its ceiling is stated in the hook itself — git has no pre-checkout hook, so the branch switch itself cannot be blocked; `--no-verify` is a deliberate escape hatch.

### Branch hygiene (optional)

> **This repo uses its own `npm run lane:prune` instead** (ADR-0150) — it carries the zero-work-branch protection this tool lacks (#449 / upstream gearbox#141). The paragraph below is the upstream description, kept for the other three things the upstream tool covers.

Before shift-end (or when you hit stale refs at shift-start), run `npx gearbox-agents prune`. It cleans up four things (ADR-0030/0043):

- Leftover linked worktrees from agent sessions (`--apply-worktrees`, `git worktree remove` on merged + clean ones only — dirty or locked worktrees are reported, never removed; runs before the branch pass because a worktree checkout blocks `git branch -d`)
- Locally merged branches (`git branch -d` safe-deletes, fails loudly)
- stale remote-tracking refs (`git fetch --prune`)
- Remote merged branches (`--apply-remote`, prints the list + asks for confirmation before deleting)

Dry-run by default — deletes nothing; a whitelist protects the current branch / the default branch / `gearbox-backfill-*` / the main worktree and the worktree you run from; never force-deletes (`-D`, `worktree remove --force`). This doesn't replace GitHub's `delete_branch_on_merge` setting — turning that on is the recommended root fix for repo owners; the tool is a backstop (`--check-settings` checks it and prints the command to enable it, without changing it automatically).

### Division of labor (optional, fill in as needed)

Division of labor is a project-level property; the template doesn't presume one (ADR-0008). Pick one of three:

1. **Fill it in**: which kind of task goes to which agent (split by capability, not tied to a specific tool). <Unvalidated candidate example: mechanical bulk edits, filling in tests → an agent good at bulk execution; architectural design, hard bugs → an agent good at deep reasoning>
2. **Leave it blank**: the default rule = **Task-issue claim-based ownership** — whoever claims a task sees it through start to finish (see While working); tasks aren't routed by agent specialty.
3. **Single-agent project**: delete this whole section (it's not a gate anchor, so deleting it won't break the gate).

## Where to find things

- `CONTEXT.md` — domain glossary (protocol terms + product/technical terms, two sections, ADR-0070)
- `docs/gearbox-adr/` — protocol ADRs (copied from Gearbox, managed by tooling — don't hand-edit)
- `docs/adr/` — this project's own architectural decisions (starting at 0001, human-authored)
- `docs/distribution-macos.md` — macOS 打包 / 签名 / 分发
- `docs/dev-two-accounts.md` — 本机同时跑两个账号（好友功能联调）
- `tests/architecture.test.ts` — Hard rules 的可执行版（越界 import 在这里红，错误信息带修法，ADR-0058）
- `tests/docs/adrNumbers.test.ts` — `docs/adr/` 编号唯一 + 不跳号的可执行版（撞号在这里红，ADR-0074）
- `tests/architecture.caseCollision.test.ts` — 同目录 TS 文件名去扩展名后不许只差大小写的可执行版。macOS 的文件系统大小写不敏感，撞名的那对里 `.tsx` 会被 tsc **静默丢出类型检查**，于是本机门禁与 CI 给出不同答案（vitest 照跑，所以没有任何症状）。光改名不够，断言才是改名的保鲜期（ADR-0173，#687）
- `scripts/check-node.mjs` / `tests/architecture.testDiscovery.test.ts` — 「这一跑到底跑了没有」的两道闸（ADR-0213，#897）。前者挂在 `pretest` 上：node 20 的 better-sqlite3 prebuild 在 `new Database()` 时段错误，于是 50 个建 `EventStore` 的文件一条断言都没跑——**退出码确实是 1，门禁不是绿的**，但摘要行写 `Test Files 370 passed (403)`，读起来像几个用例挂了。这道闸不重复那次失败，是把**归因**从代码挪回环境（门槛 `>=22` 取实测跑通的最低版本，不是 `.nvmrc` 的 24）。后者管真正安静的那种：文件名没落进 vitest 的 `include` = 一次都不跑而门禁全绿（`.test.tsx` 那次就是），判据是「import 了 vitest 却不在 include 里」，`include` 从 `vitest.config.ts` 现读不抄
- `scripts/release.mjs` / `tests/scripts/releasePushOrder.test.ts` — 本地发版一条龙 + 它的步骤顺序（ADR-0215，#679）。顺序里有一条不是随手排的：**升版之后立刻推 main 占位，然后才开始构建**。原来是构建完再 `push --follow-tags`，而那两步要十分钟——期间别条 lane 合了 PR，最后那次 push 被拒，且推 tag 与推分支不是原子的：**tag 先上去了、分支被拒**，落地成远端一个悬空 tag 加没有 release（发 v1.1.0 时真踩过）。末尾**只推 tag 不推分支**：被超车时本地 main 是落后的，推分支在 git 眼里是把远端往回退、直接拒。代价是构建失败会在 main 上留一个没有 release 的版本提交（版本号空一个洞，没有消费方假设连号）。可执行版真起临时仓真跑脚本，判据是构建脚本有没有留下脚印——读源码断言那行在这行之前，会对着「末尾还推分支」的错误实现全绿
- `scripts/deploy-stamp.mjs` / `edge-deploy.mjs` / `deploy-check.mjs` / `release.mjs` 里那两步 — **服务端部署跟着发版走，且「部署成功」的判据是线上亲口报出的指纹**（ADR-0258，#791）。#790 的形状：代码合了、门禁绿了、客户端发版了，线上 worker 还是旧的，而桌面报的是「云端无响应」（方向指向 VPS 宕机）；2026-09-08 又踩了另一面——`CS_PROTOCOL_VERSION` 12→13 而 VPS 上停在 12，从 main 构建的桌面连不上任何云会话。**有害的偏差方向只有一个：客户端比服务端新**，而 `npm run release` 正是客户端出门那一刻，所以两个服务端的部署接进它、**排在升版之前**（这一步失败时没有版本提交、没有 tag、没推过任何东西，收拾成本是零，同 ADR-0215 把失败点往便宜处挪）、**edge 先于 runtime**（runtime 的 `EDGE_BASE`/`RELAY_BASE` 指着 edge）。**缺凭据一律拒绝发版，没有跳过开关** —— 一个用过一次的逃生门下次就是默认，而这条 issue 修的正是「忘了部署」。判据不是退出码是**自检**：edge 部署后轮询 `/healthz` 直到它回出这次的戳，runtime 把戳 `define` 进 bundle、daemon 起来打进那行「就绪」（连 `CS_PROTOCOL_VERSION` 一起报），部署脚本从 journal 读回来——**落在跑着的那个进程上不落在磁盘那个文件上**（rsync 成功而 systemd 起不来时，文件说的话是假的）。指纹取**真实模块图的内容**（esbuild `write:false` + metafile，逐文件哈希）不取 git sha 也不按路径猜：sha 会把一次 app-only 发版判成服务端陈旧，按 `services/**` 猜则漏掉协议进位那种「一个字没碰 services/」的提交，而放宽到 `src/**` 就天天误报——**会误报的警报用不了几次就没人看了**。`npm run deploy:check` 回**三态**，`unknown`（没登录/没配 `RUNTIME_SSH`/打不通/线上那份还不会报戳）两边都不并：并进 `current` 就是「没有证据」被读成「没有问题」（本条 issue 的病根），并进 `stale` 则是把一次网络抖动说成一次漏掉的部署。要核的地址从 `src/shared/edgeConfig.ts` 的 `DEFAULT_EDGE_BASE_URL` **现读**（抄第二份那天没有任何一处会报错，只会让自检对着空气地址报「通过」）。否决了 issue 里另一条候选「GitHub Actions 按路径自动部署」，三条理由任一单独成立：盖不住 runtime（要 ssh + docker build）、要往 repo secrets 放部署 token 而本仓是单人本机发版、它关掉的是无害的那个方向。已知代价七条在 ADR-0258，最要紧的两条：发版从此要 Cloudflare 登录 + `RUNTIME_SSH`（有意的——那台机器不该能发出一个它没法同时更新服务端的版本）、**每次发版都重新部署一遍**（没做「指纹一致就跳过」，那等于给这条链路加回一个判断，而判断错的那一次就是 #790）
- `scripts/wip.mjs` — `npm run wip [说明]`：把手上的活落成一个提交而不是 stash（多 worktree 共享同一个 stash 栈，#543 踩过；撤销 `git reset --soft HEAD~1`，ADR-0154）
- `scripts/lane.mjs` / `scripts/lane-prune.mjs` / `scripts/install-hooks.mjs` — 开一条 lane / 收工清理 / 自动挂钩子（`npm run lane -- <任务名>`、`npm run lane:prune`；零工作量分支为什么不删见 ADR-0150 与 #449）
- `.githooks/pre-commit` / `tests/hooks/preCommitWorktree.test.ts` — 「主 checkout 只读、开工用一次性 worktree」的机制兜底 + 它的可执行版（装一次：`git config core.hooksPath .githooks`；天花板与逃生门写在钩子文件头，ADR-0149）
- `src/shared/fileRefs.ts` / `src/renderer/src/lib/rehypeFileRefs.ts` / `src/renderer/src/lib/codeLines.ts` — 正文里的「文件:行号」认成可点 chip，点了跳到 Files 面板的那一行（ADR-0110；rehype 插件的装配顺序有两条不会报错的坑，见该 ADR 第二节）
- `src/shared/askUser.ts` / `src/renderer/src/lib/askUserCard.ts` — 问卷答卷的编解码（`formatAnswers` 的逆函数在这儿）+ 时间线上那张「已作答」卡的纯逻辑（ADR-0111）
- `src/main/filesService.ts` / `src/shared/files.ts` — Files 面板的主进程数据源与纯逻辑层（只读；三条安全边界钉在 `tests/main/filesService.test.ts`，ADR-0092）
- `tests/e2e/` — Playwright-electron 端到端（`npm run e2e`，不在 gate 里；跑不跑随你，不是 PR 的义务，ADR-0138 取消了 ADR-0058 那条）。既是冒烟，也是四张真机验收清单的落地处：`harness.ts` 换 `HOME` 做隔离、`fakeModel.ts` 是本机假模型，见 ADR-0076
- `src/main/islandBridge.ts` / `src/main/islandProjection.ts` — macOS 灵动岛：主进程 stdio 桥 + 事件投影器，接一个原生 Swift helper 进程（ADR-0059 推翻版）
- `native/MrOttoIsland/` — macOS 灵动岛原生 Swift helper（ADR-0061，推翻 0059；ADR-0063 演进为多会话 fleet 列表）
- `src/main/simulatorHub.ts` / `src/main/simInputBridge.ts` — iOS 模拟器：simctl 台账 + 画面轮询 + 输入桥（ADR-0092）
- `native/MrOttoSimInput/` — iOS 模拟器输入/无障碍 helper（Swift，CGEvent + AXUIElement；ADR-0092）
- `src/shared/remote/` / `src/main/remoteBridge.ts` — 手机端远程投影与审批：帧协议、握手与密封流（`src/shared/remote/` 是纯层，手机端 import 同一份）+ 与 islandBridge 平级的桌面侧装配（ADR-0094 / 0095 / 0096 / 0097 / 0100 / 0106）
- `services/runtime/src/frameHandler.ts` 的 `serialize` — 每个 cid 一条串行链（#915）。桌面的 `create()` 在**同一个 tick 里**连发 `hello` + `create`，而 daemon 的接线是「来一帧起一个 promise」；hello 那条要 await 验签**再** await `labelOf`（一次真 Supabase 往返），create 在这个窗口里被处理时 `cids` 还是空的，于是落进「第一帧不是 hello」那条分支回 `not_authorized`——**`labelOf` 是网络调用，所以这不是偶发竞态是近乎必然**，云会话大概从来没建成过。粒度是 **cid 不是全局**（全局串行会把一个慢查询变成所有人的队头阻塞）；`.then(fn, fn)` 让前一条抛了也接着跑下一条（只接成功路径的话一次抖动会把这条连接永久卡死）；`onGone` 要连链一起删。否决的两条写在 ADR/issue 里：await 之前先登记 cid（把竞态换成安全洞——验签没完就当已验籍）、桌面等一拍（控制房**故意没有 welcome**，没有可等的信号）。`FrameHandlerDeps.log` 是**必需**的（同 rateLimit）：拒绝是这一层唯一的失败出口，写成可选的话忘接线那天它安静地什么都不记，而那正是最需要它的那天
- `services/runtime/src/daemon.ts` 的控制房看门狗 / `wsTransport` 的 `isOpen()` — 「连不上中继」这个状态本身没有任何人在看（#913）。退避重连是**无限**的，所以一条永远握不上手的连接不会以任何方式结束、也不会积累出症状：真机上它安静失败了七个多小时，服务器日志里只有启动那行「就绪」——因为 `createWsTransport` 的 `log` 缺省是空函数，而 daemon 建两条 transport 时都没传。桌面那侧唯一的信号是建云会话超时后的「云端无响应」，**它把「握手被拒」说成「对面没回话」**，方向指向 VPS 宕机，真实原因是 `RUNTIME_SECRET` 两边不一致（同一把共享口令抄两处，`docs/runtime-vps.md` 里就是这么写的，没有任何机制保证一致）。看门狗的判据是 `isOpen()` 这个**状态**不是日志文案（文案一改看门狗就静默失效，而这条 issue 修的正是「失败无声」）；`isOpen()` 要求 socket OPEN **且**中继发过号——只看 readyState 会把「握手过了却没进房」算成连上。`isOpen` 加在 `createWsTransport` 的**返回类型**上不是 `RemoteTransport` 接口里：接口有别的实现，加必填会全打红、加可选则忘了实现那天看门狗安静变 no-op
- `src/model/openaiCompatible.ts` 的 `readSSE`（`terminated` / `streamError` / `bytes`）+ `parseToolArgs` — **流没收尾要说清是断流，不是拿半截 JSON 报语法错**（#1131）。真机：云会话里「开发」这一轮 2.3 秒后红条 `Unterminated string in JSON at position 21`，VPS 上的栈指向 `if (buf) feedLine(buf)` 那一行——上游在一块的 id 串写到一半时关了连接，「流关了缓冲还有货 = 服务器没带尾换行」这条假设把半行喂给了 `JSON.parse`。收尾的凭据有两个（`data: [DONE]` 或带 `finish_reason` 的块），有一个就算收尾，**两个都没有才算断流**（不是每家都发 `[DONE]`，只认它会把正常流判成断流）；上游塞在流里的 `{"error":…}` 单独认，不然那句话会被「断流」盖掉。重不重发的判据是 **`played`（真交给过 onDelta 一个碎片）不是 `consumed`（收到过字节）**：第一块往往只是 role 块或半截 id，什么都没播出去，按字节判会把一次完全安全的重发也拒掉；播过了照旧不重发（半条消息续不上），但话要说清。工具参数解析失败按 `finish_reason` 分两句（`length` = 撞了长度上限被截断，其余 = 模型写错了），都带工具名。改的是 `src/model/`，**云会话要重新部署 runtime 才生效**（#791）
- `src/session/deriveMessages.ts` 的 `AssistantChatMessage.reasoning` / `src/model/openaiCompatible.ts` 的 `reasoningPassback` / `src/shared/modelCatalog.ts` 的 `REASONING_PASSBACK` — **自己的 reasoning 随投影带出，回不回传按厂商门控**（#1151，ADR-0274）。「API 禁止思考回流」是 R1 时代的规矩，DeepSeek 自 V3.2 起反过来：thinking 模式 + 带 tools 时最后一条 user 之后的每条 assistant 都必须带 `reasoning_content`，自己的工具回合能过是靠它按 tool_call id 在服务端存的缓存——等一次长审批、daemon 重启补跑、对方缩 TTL，缓存没了就 turn 中途 400。修法三层：投影把日志里的 `reasoning` 带上 `ChatMessage`（没有该字段的旧日志逐字节不变）；adapter 默认**剥掉不发**（多数 API 拒陌生字段，这是保命闸），开着的厂商发 `reasoning_content`（有原文发原文、没有发空串，探针验过空串也收）；名单在目录按厂商派生，**逐家真接口验过才准进**（2026-09-09：DeepSeek 要求所以开，GLM/Kimi 接受但不要求不开，Qwen 没验成不开——网关只收有效 JWT 而本机 token 过期，脚本刷新会顶掉在跑的 app 会话）。开启后不做位置/tools/档位判断：探针证明任何位置、thinking 关、空串全收，「最后一条 user 之后」那份位置逻辑是会漂移的假设。接线两处：桌面 `makeAdapter` 按 `choice.reasoningPassback`，runtime 按 `findModel(route.model)` 现查。**云会话要重新部署 runtime 才生效**（#791）
- `src/shared/remote/wire.ts` / `wsTransport.ts` — 中继的线上约定（按 cid 寻址的帧、`:peer`/`:gone` 等控制消息、子协议、帧上限，**三方共用一份**）+ 桌面与手机**共用的那一份** WebSocket 传输（两个运行时都有原生 WebSocket，ADR-0129 / 0130）。`send` 回 boolean（ADR-0201 第四节）：四条丢帧路径都不抛异常，其中一条正是「正在自动重连」这个正常窗口；**不是送达确认**，只是把「压根没交给 socket」从成功里择出来——多数调用方照旧忽略它，在意的是「用户点完就不再看」的那几个（config/create）
- `services/edge/` — 边缘服务：OAuth 落地页 + 中继。`src/relay.ts` 是配对的纯逻辑（跑在根门禁里），`src/worker.ts` 是 Cloudflare Worker 入口 + Durable Object（唯一依赖运行时的文件，单独一份 tsconfig）。运行时那一层由 `checks/relay.mjs` 打真 workerd 验（ADR-0129）。连接数上限**按人分桶不按房间总数**（ADR-0201 第一节，#824）：`MAX_CONNS_PER_USER` 这个名字一直在撒谎——自远程的房间键就是 userId 才碰巧相等，而 `cs-ctl` 是全平台一个固定房。分桶键 `ck` 由 edge.ts 算好递给 DO（`sha256(房间键+uid)` 前 64 bit，**不递 uid**：好友代理房间的前提是中继不懂参与者是谁）
- `services/edge/src/px.ts` / `src/shared/remote/pxEscrow.ts` / `src/main/pxEscrowSync.ts` — 好友代理云端执行面（ADR-0197，推翻 ADR-0151「凭证不出门」）：edge 按 hostUid 一户一箱（Escrow DO，AES-GCM 密封），三道闸序云端复刻。托管文档的形状**两方共用一份**（pxEscrow，纪律同 wire.ts）；A 侧上传编排在 pxEscrowSync——四个触发源（授权变更 / mcpHub.onChange / mcp-auth 重写 / 登录恢复）汇成一次防抖上传，**零授权 = DELETE 整箱**（撤销级联的后半），内容指纹没变不打网络；进箱只收 live 的 https http-transport server，一台 http://localhost 不毒死全箱。B 侧云借用在 `src/main/pxCloudClient.ts`（grants 拉取 + call，content 转换与 mcpClient 共用 `toMcpContent`）+ proxyManager 的 `routedBorrowMcp`：每条借用是一把**每次调用现选路**的刀（live 通道 ready 走通道，否则打云端），`activeProxies` 的底本从「活着的通道」换成 usableBorrows——「配过但没连上」正是云借用要接住的形态；grants 查询失败保留旧缓存（「拿不到」≠「被清空」，真撤销有 proxy_revoked 帧与云端 403 双保险）。切片 4 审计回流在 `src/main/pxAuditSync.ts`：云端替 A 执行的账（Escrow DO 环形 500）按游标（proxyStore.cloudAuditCursor）增量拉回并入本地台账（`mergeCloudAudits`，映射与去重钉在那里），触发点两个——登录恢复 + 用户翻调用记录（proxyAudit IPC），不做定时轮询；A 侧借出页的「云端可用」= escrowSync.hostedServerIds（上次成功 PUT 的箱内清单）∩ 该好友的授权非空，断线但箱在时状态行说「云端可用」不说「没连上」（后者暗示对方用不了，实际正相反）；登出先 `escrowSync.purge()` 再 signOut（supabase 登出默认吊销所有设备的 session，云端却还托管着能刷新的凭证——清不掉不阻塞登出，断网也得能登出）
- `src/main/proxy*.ts` / `src/shared/remote/proxyInvite.ts` / `proxyProtocol.ts` / `src/renderer/src/lib/proxyShare.ts` — 好友代理：@好友接替你的身份操作已接通的 MCP 服务（Shopify/Google Ads…），**凭证不出你的机器**（ADR-0151）。信任根是邀请码里那把**一次性 secret 的持有证明**，不是频道号——`proxyConnection` 的 `tryPair`，与自远程共用 `pairing.ts`；长期信任落 `proxyStore` 的 pin，撤销把授权/pin/频道一起清（ADR-0162）。执行侧三道闸、顺序固定：**身份**（帧里自称的 `fromUid` 只用来核对，查授权一律按通道绑定的那个）→ **关系**（删好友 = 代理权限跟着死；名单三态，`null`=还没同步好也拒）→ 白名单（ADR-0164）。取消是一条要真发出去的帧，且**取消 ≠ 没发生**——照样记审计并说清「可能已经动过」（ADR-0165）。B 侧借来的服务**按好友短标签加前缀**合并进会话（`proxyNamespace.ts`），不换掉本地那份；前缀取 uid 不取昵称（昵称过不了 `safe()`、又会变，而审批记忆按完整工具名记），通道没了**抛错不回落本地**（ADR-0166）。线上单帧有 256 KiB 上限而 relay 超限是**关掉发送方连接**，所以 `sendSealed` 回 boolean、超限不发；执行器最前面还有一道并发+令牌桶（第四道防线，保护的不只是凭证还有审计账），被限流的一个时段只记一笔（ADR-0167）。「借进来的」和「借出去的」记在同一本台账的两栏，断线的那条**仍然留在列表里**（「配过但没连上」≠「从没配过」，ADR-0168）；同理**撤销要说出口**——`proxy_revoked` 是一帧真发出去的话，靠「清单空了 + 连接断了」推断的话，它和「对方关机了」产生的观测一模一样，而这两件事该做的动作相反（标记不删除，`usableBorrows` 不再连它但 `allBorrows` 仍然列它）。A 侧那块表（`hostStatus`）与 B 侧严格对称，`inflight > 0` = **此刻**正在用我的凭证——审计是结果出来才写的，所以这一格另有 `onActivity` 一条信号源；改授权走 `proxyUpdateGrant`**不重发邀请码**（重发 = 重新配对，而调权限不是），ADR-0169。`hostStatus` 的 `pairing` 三档给「A 发码后退出 app」那个死锁**取了个名字**——secret 只活在内存里，重启后 `resume()` 因为没 pin 而跳过，房间再也不开；判据是既有数据的一个函数（有 pin / 无 pin+房间开着+没过期 / 其余），**落盘 secret 和无证明配对两条路都否决了**（ADR-0170）。线上白名单里 `tools: []` = **整服务放行**，勾选表的换算钉在 `proxyShare.ts`
- `src/shared/shareGrant.ts` / `src/renderer/src/components/ShareGrantDialog.tsx` — **分享会话时连带把它用到的服务借给对方**（ADR-0177，#694）：`serversUsedInSession` 反查「这个会话调过哪几台**此刻还连着**的 server」（日志里只有模型可见名，靠 `assignMcpToolNames` 重算反查，所以是尽力而为不是精确重建），确认框默认全勾、可减。**不做自动全授** —— 白名单内没有逐次审批（ADR-0151），「给你看我干了啥」和「你可以用我的身份下单」差得太远，不能靠隐式默认糊过去；代价只在真有服务可借时付（没用到 MCP 的会话不弹框）。随分享发的邀请活 24 小时（`PROXY_SHARE_INVITE_TTL_MS`）而不是 10 分钟 —— 分享是异步的；这不是把闸门拆了，**secret 仍然不落盘**，A 一重启码就作废（ADR-0162/0170），真实窗口由前者封顶。邀请码走信封的新字段（`v` 保持 1，旧客户端照常渲染卡片、只是没那个按钮），B 侧一键握手在 `SessionShareCard`：**接不上服务时不顺手把对话导进去**再报错。`@好友` 那条正文**不发给模型**（ADR-0179，#705）—— 它是发送侧的动作信号，照样 dispatch 的话本地水獭会去做你刚委托出去的同一件事，而 B 用的是**你的**凭证，两边动的是同一份真实数据。代价是消息什么痕迹都不留，所以分享成功后主进程追一条 `session_shared`（log-only、`ignorable`、进隐私闸——发给 B 的包里剥掉，否则等于告诉 B「我还把这个给了小红」）
- `mobile/src/friends.tsx` / `mobile/src/friendsApi.ts` / `src/shared/friendsQuery.ts` — 手机端好友：加好友 / 收发请求 / 私信（直连 Supabase，不经中继；纯逻辑与桌面共用 `friendsQuery.ts`，ADR-0114）
- `src/shared/remote/pairing.ts` / `src/renderer/src/lib/qr.ts` — 扫码配对：一次性二维码 + 持有证明的纯逻辑（三边共用），和把它画出来的模块矩阵。**目录仍然不是信任来源**，桌面验的是「谁能对 secret 签出名」（ADR-0142，推进 ADR-0095 后果表第三行）
- `src/shared/remote/stats.ts` / `src/shared/sessionActivity.ts` — 手机端设置页那两块：会话热力图 + 各模型用量。**拉取不订阅**（`stats` 帧对），`trim.ts` 那道闸门不动（ADR-0115）
- `src/shared/sessionWorktree.ts` / `src/main/sessionWorktreeService.ts` — **每只**水獭都拿一份独立工作副本（git worktree，落在 userData 不进用户的仓库；判据一条：工作区在 git 仓里，ADR-0157 取消了 0156 的「第一只不隔离」）。水獭从系统提示里知道它在副本上、**动文件前要先告诉用户项目目录不会变**、合回去先问一句合到哪；**提示里不写死分支名**（自动标题出来后分支会改名，日志里那份就成了历史记录，ADR-0158）。会话活着时副本被 `git worktree lock` 锁住，原因串带 sessionId + pid，给回收程序判活用。 合回项目与回收在 `merge` / `recycle`：**合到项目目录此刻所在的那条分支**、脏了拒绝、冲突 `--abort` 回滚（绝不把用户的 checkout 留在冲突态）；回收只收「干净且已合并」的，其余留着（ADR-0159）。UI 是头部那枚常驻 chip + 「更多」菜单里的一项。推翻了 ADR-0152 对候选 1 的否决，那条互斥与 ADR-0155 的跨进程锁留作兜底（ADR-0156 / 0157）
- `src/shared/coworkLog.ts` / `src/main/coworkLogFile.ts` / `src/main/coworkMiddleware.ts` — 非 git 文件夹里多只水獭同时干活：工作区根目录一本明文协作记录（谁/何时/动了哪个文件/为什么，`write_file` 的 `reason` 参数供料）+ 按需注入 + **文件级**的闸（撞同一个文件拦一次要求重读，不同文件一律放行）。追加走 `node:fs` 的 `O_APPEND`——读改写会把「两个进程同时写」这个问题原样重现。天花板：`bash` 里的 `mv`/`rm`/重定向管不着（ADR-0161）
- `src/shared/workspaceExclusion.ts` — 同一个文件夹同一时刻只跑一条 turn（纯逻辑；接线在 `index.ts` 的 turn 起跑处，紧挨着 `runningSessions` 那条自检）。沙箱围的是路径，围不住共享的 `.git`；同家族（子会话/SideChat）不互斥（ADR-0152）。**只在 git 仓里还这么拦**——非 git 目录改走 `coworkLog` 那条文件级的路（ADR-0161）
- `src/shared/workspaceLock.ts` / `src/main/workspaceLockFile.ts` — 工作区互斥的**跨进程**那一半（两个 app 实例指同一个文件夹）。锁在机器级临时目录、按路径哈希取名——不进用户的仓库；陈旧锁靠心跳过期 + pid 探活自愈，**自愈比挡住更重要**（一次崩溃不该把文件夹永久锁死，ADR-0155）
- `src/shared/gitSafety.ts` / `src/main/dirtyTreeApprover.ts` — 破坏性 git（`reset --hard` / `clean -f` / `checkout -- 路径` / `restore` / `stash drop`…）+ 工作区脏 = 越过 bypass 模式问人一次。**裸 `git checkout <分支>` 不在名单里**——git 自己会拒绝覆盖，拦它只会训练用户闭眼点批准（ADR-0153）
- `src/main/workspaceLens.ts` — 岛的分组镜头：workspace →「哪个项目 + 是不是副本、副本在哪条分支」（30s TTL 记忆化，因为 `pushFleet` 跟着每条事件跑）。**组头回答「这是哪个项目」，副本身份下沉成行上的 chip**；投影层靠注入这只镜头保持纯函数，默认镜头 = 不折叠 = 旧行为（ADR-0172）
- `src/main/projectRoot.ts` / `src/main/projectMemoryMigrate.ts` — 记忆的项目作用域解析：workspace 向上第一个 `.git` = 项目根，纯读文件不起 `git` 子进程。**作用域键是 `origin` 的 remote URL 归一化后的 `host/path`，不是路径哈希**（ADR-0211，#886）——路径哈希在每台机器上都不同，项目档因此从来没跨过机器；没有 remote 的仓退回路径（等于保持改动前的行为）。归一化去 userinfo（URL 里那段 token 每台机器都不一样）、去端口、host 小写而**路径原样**（合并两个不同仓比拆开同一个仓更糟）。旧目录迁移三条判断都在 ADR-0211：只搬本机解析得出 remote 的那份、搬是**并集不是覆盖**、搬完留墓碑 `merged.txt` 而**不删目录**——云同步没有墓碑机制，删了会被还揣着旧副本的机器推回来（删了又回的无限来回），而墓碑同时保证用户手删的条目不会下次开机又被合回来。键与本机路径分开带（`{id, root, dir}`）：写盘按 id，点名检测按 root。`resolveWorkspaceOrigin` 一次爬升同时得出根与 worktree 分支（分支从 `<主仓>/.git/worktrees/<名>/HEAD` 读，**现查不读日志**——日志那份会陈旧，ADR-0158/0172），`resolveProjectRoot` 是它的投影。**worktree 折叠回主仓**（取舍：worktree 是一次性的，不折叠的话项目记忆跟着每次换班出生死亡；代价是 worktree 里读不到 `.git` 时不折叠）——与 `projectInstructions.ts` 的爬升同源但结论相反，两边不共用函数（ADR-0116）
- `src/main/accountScope.ts` / `src/main/authStorage.ts` 的 `sessionIdentity` — 本机数据按登录账号分抽屉（`<userData>/accounts/<sha256(uid) 前16位>/` 与 `~/.mr-otto/accounts/<同名>/`，ADR-0187、#749）。**`auth.json` 留在 userData 根**：抽屉名从它算出来，uid 不必等 `restore()` 的网络往返 —— supabase 落的 session 自带 `user.id`，和进门闸（ADR-0183）读同一个文件、同样同步离线；形状判据抽成 `sessionValues` 一份，`hasSession` 与它共用（这个判断已经错过一次，#729）。**换号靠重启不做热切换**（`needsRelaunch`，登出不触发）：抽屉在装配那一刻钉死，二十来处 store 各持一条绝对路径，重启换得干净、没有「这个换了那个没换」的中间态；重启**先于** `send(accountChanged)` 且直接 return，否则渲染层会在上一个账号的数据上多活一帧。存量**能归属才归属**：开机时有 session 就归给它，没有就整堆停进 `accounts/_unclaimed/` 且**从此不自动归给任何人**（`parkUnclaimed`，ADR-0188 修正 0187 第四节）—— 第一版少了这个 else，语义变成「归给升级后第一个登录的人」，于是「登出→升级→登录另一个号」把 A 的全部数据交给了 B（#755，#749 换条路重演）。挪进 `_unclaimed` 本身就是修复的一半：根下空了，后来者扫不走。`~/.mr-otto/` **整个**搬而不是挑私密的搬 —— 挑就要每加一个文件重判一次，而这个 bug 本身就是没人做那次判断的结果；不可读的代价用 `who.txt` 抵掉，界面上「去哪手改」的文案一律现取 `configRoot`（写死的话用户会去改一个对本账号不生效的文件）
- `src/renderer/src/components/SignInScreen.tsx` / `SignInCard.tsx` / `lib/identity.ts` 的 `needsSignIn` — 进门那道闸：没有**登录记录**时 app 只画这一屏（背景与 `Splash` 同一份 dither —— 冷启动是它在底下等着 Splash 淡出，两层不同就成了换皮闪动）。闸门判据是 `auth.json` 里**有没有一份 session**（ADR-0183 收紧了 0182 的「有任意 key 就算」—— PKCE 的 `-code-verifier` 是「点过一次 OAuth 然后放弃」，被它骗过一次，#729），**不是** `account.signedIn`：后者要等主进程 `restore()` 的网络往返，照它判会让已登录用户每次开 app 闪一屏登录页、断网时更是把人锁在自己的软件外面。早退写在 `App()` 的**返回值**里而不是包在 `main.tsx` 外层（`boot()` 挂在 App 的 effect 上，不挂载就永远取不到记录），于是下面那棵树一个都不挂 —— 没登录的人按 ⌘K 不会有搜索框浮在登录卡上。登录卡两个渲染点共用同一张（账号页那个入口留着：有记录但没验上的人是闸门故意放进来的）。e2e 靠 `launchOtto({ authRecord })` 过闸，默认 true（ADR-0182 / 0183，#723 / #729）。**闸门还有第二条按住它的理由**（`showsSignInScreen` = `needsSignIn` **或** `holdGateForPasswordReset`，ADR-0194）：recovery OTP 换到的是真 session，验完那一刻闸门自己就抬了，于是一个还没设新密码的人背后铺开整个 app、而「以后再说」等于让他带着**旧密码原封不动**的状态进去 —— 他刚才证明的只是「能收这个邮箱的信」。修法不是让弹窗跨过闸门，是让闸门先别抬。`atGate` 由**发起方**给不从 `resetPending` 反推（账号页那张同款登录卡上的人本来就在 app 里），那笔记号**落 localStorage 不落内存**（点邮件链接那条路要经过浏览器、可能隔着一次冷启动）。忘记密码走**验证码**不走链接（每一跳都是一个人会走丢的地方；链接没拆，同一封信里两样都有），`OTP_LENGTH` 取 Supabase 项目的 `mailer_otp_length`（本项目 8，GoTrue 默认才 6）
- `src/main/mcpOAuth.ts` / `src/main/mcpAuthStore.ts` — MCP 的 OAuth 授权：loopback 回调 + 0600 凭据落点（ADR-0121）
- `src/main/mcpHub.ts` 的 `ready()` / `index.ts` 装配末尾那句 `void mcpHub.ready()` — **MCP 握手不挡会话装配**（ADR-0276，#1187）。这个函数挂在每一次会话装配上（`resumeSession` 里那句 `await`），所以它等谁就是「点开一条不在内存里的会话要卡多久」——过去它对**每一台没连上的** server 都重试一遍并等到底（封顶 10 秒），而连接又只由它发起（开机不连），于是两笔静默的账：app 起来后第一个点会话的人替所有人付握手；没授权/连不上的那几台每次冷 resume 再付一遍。真机实测三台未授权的远程 server 让一条 **7 条事件**的会话等 879ms —— 这段成本跟日志大小毫无关系（同一条会话的读+IPC 才 39ms/2555 条）。判据收成一条：**这台问过了没有**。没问过的这次等它（工具表挂载一次定终身，ADR-0054，不等就是替用户决定这条会话没有这几台的刀）；问过而没连上的后台重试 + 30 秒冷却窗（重试没错，错的是频率——`ready()` 挂在高频动作上）；**`needs-auth` 一次都不重试**（拿同一份凭据再打一遍答案不会变，那是「要人点授权」；重连/authorize 两条路直接调 `connectOne`，不看冷却窗）。开机预热排在 `mcpHub.onChange` 接线**之后**（预热每连上一台就 `emit()`，接线在后面这几发推送没人收）。「同一个 id 不并发连两次」从单飞的 `readying` 换成按 id 的 `inflight`——那道闸原来是整体单飞顺带给的，重试挪到后台之后前提没了，而撞上在 stdio 下就是两个孤儿子进程；注意 `triedAt === undefined` 与「此刻正在连」**不是**一回事（超时返回但连接还挂着同时满足两者，那时 `attempt()` 交回同一个 promise，接着等而不是再连一次）
- `src/tools/mcpConfigure.ts` — agent 自助配置 MCP，过审批门（ADR-0118）
- `src/renderer/src/lib/runtimeHydration.ts` / `src/main/index.ts` 的 `sessionRuntime` handler — 推送之外那扇查询窗口：重载后补回 turn 状态/压缩标记/挂起的审批问卷。**只填空不覆盖**（为什么这条规则就够，见 ADR-0133）
- `src/renderer/src/lib/fileTree.ts` / `src/renderer/src/components/elements/file-tree.tsx` — 「这一组工具动了哪些文件」那棵树：独生目录链压一行、工作区外的文件不进树、同一文件写两次行数相加（纯逻辑）+ 挂在工具组折叠头底下、点一行在 Files 面板打开。取代原来每个写入一张可下载的文件卡（ADR-0140）。行末那对 `+N/−M` 来自 `tool_result.diffStat`（写盘那一刻由 `main/turnDiff.ts` 的 `writeStat` 算好落盘，**不是**渲染层现算，也不是 turn 级聚合那一份，ADR-0141）
- `src/shared/billing.ts` 的 `SSE_COST_COMMENT` / `src/renderer/src/components/CostPanel.tsx` — 「这次调用花了多少 credit」（ADR-0211，#857/#895）：非流式走响应头，**流式放不进头**（settle 发生在响应头早已发出之后），改成流末尾一行 **SSE 注释**追在 `data: [DONE]` 之后——注释行合规解析器一律跳过，换成自造的 `data:` 帧就得赌对方解析器够宽容。只在 `flush` 那条路贴（中断时 controller 已不收新块），且**尾注的字节不计进 `info.bytes`**（那是「结算还是 release」的判据）。面板上托管行记到了写 credit、没记到写「托管」而不是破折号（后者说的是「查不到价」，是另一回事）；合计只有**清一色且每一笔都有数**才报得出来，混着就退回 token 总数。**第三种不显示 $ 的理由是「订阅」**（ADR-0241，#1025）：`modelPricing.ts` 的 `UNPRICED` 把「没有价」拆成 `flat_rate`（订阅制第三方，钱在月费里 —— 不是查不到，是不存在）与 `unknown`（有按量价、只是查不到公开数），面板据此分别画「订阅」与「—」。#1025 报上来时以为是笔误（`k3` vs `kimi-k3`），实际 `kimicode` 与 `moonshot` 是两个 ProviderId 两套账号体系（ADR-0117），**把按量价抄过去就是编一个用户没花过的数**。合计**照旧退回 token 不把包月那几笔当 0 加**：边际是 0 没错，但那句话会变成「这个会话花了 $0.03」，对一个每月付着钱的人说比不报更坏。保鲜期是一条断言：目录里每一款必须有价、或是本机那一族、或在 `UNPRICED` 里说明理由 —— 「两张表对不上」是静默失败，界面上只多一个破折号，而破折号本身是合法状态，肉眼分不出抄错一个字符和真的没有价
- `services/edge/src/billing.ts` 的 `checkoutParams` / `src/renderer/src/lib/billingError.ts` — **我们自己是 merchant of record**（ADR-0216，#910）：Stripe 把 Managed Payments 设成账号默认，而开着就要求每个 Product 有 `tax_code`——没填的后果不是少收税，是 `checkout/sessions` 直接 400，订阅按钮点了什么都不发生（真机上三个档位全 400）。`managed_payments[enabled]=false` 写在**每次会话的参数上**不是只关后台那一格：后台那格是 Stripe 的开关、test/live 还是两份，写死才让行为不取决于「那一格此刻是什么」。两种 mode 都要带（加购走 payment，过同一道校验）。翻回去的路：删那一行 + 后台填 `tax_code`，代价与选码依据在 ADR-0216。`humanizeBillingError` 是配套的另一半——Stripe 原文是说给维护者听的（后台链接 + 账号 id），照 `humanizeMcpError` 的规矩只翻认得出的；**卡被拒不算配置类**，那是用户换张卡就能解决的事，翻成「我们这边没配好」会让他干等
- `src/shared/sessionTitle.ts` — 会话没标题时清单里显示什么（#925）。标题是第一条 `user_message` 首行的**投影**，所以「还没发过话」的会话天然没有标题（刚点 ＋ 建的、开局卡上还没打字的、云端刚落库那条），真机上长成**一格空白**——一行看不出是什么，也看不出能不能点。兜底统一成「新会话」（和用户刚按的那颗钮同一个词），**不再由调用方传**：原来侧栏传文件夹名、任务栏传「任务」、⌘K 传组名，同一条会话在三处三个名字，人得先意识到「这三行是同一条」。判据是 `trim()` 后比空串不是 `??`——云会话/已发布会话那两张表的 `title` 是 `string` 不是 `string | null`，没标题时落库的就是空串。**故意不套这层的两处**：`src/tools/sessionSearch.ts` 的「(无标题)」是给模型读的（写成「新会话」它会当成真标题），侧栏那摞史前会话退回 `sessionId`（那是一堆打不开的记录，id 是它们唯一的把手，全叫「新会话」就彼此分不开了）
- `src/renderer/src/lib/workspaceAccess.ts` / `components/WorkspacesSidebarSection.tsx` / `NewWorkspaceDialog.tsx` / `supabase/migrations/0020_*.sql` — 工作区从页脚抽屉搬进侧栏**项目区** + 订阅闸（ADR-0217，#917；**#1087 又把它抬成切换器上独立的第三档，见下条**）。页脚那枚 `Boxes` 图标**撤掉**：入口留两个只会让人以为它们是两样东西。头部那颗「新工作区」**只在它生出来的东西画得出来的那一栏**（#923：留在任务栏就是点完看不见结果；那一栏 #1087 之后是工作区栏，判据没变）。任务栏只剩「新会话」一颗时改成通栏**左对齐**（`SOLO_BUTTON`），跟正下方的「已归档会话」对齐——两条通栏按钮叠着一个居中一个靠左就是没对齐。「分开显示」当时落在三处（段头 / 行的样子 / 段尾一道**内缩** 14px 的细线——通栏的横线读作「界面构件之间的分隔」，内缩的才读作「同一份清单里的两组」），**那道线 #1087 删了**：分开这件事改由切换器承担；同样翻过来的还有空态——原来「一条都没有时整节不出」（项目栏底下已经有一段空态，顶上再压一句就得读两段），独占一栏之后必须画（空列表 + 有错仍然照出，那是「读不到」不是「没有」，且两句话不同时出现）。拉取挂在 `AppSidebar` 不挂在这一节——它 return null 时 effect 永远等不到第一次拉取。**订阅闸卡在创建这一侧不卡参与**：规则「工作区走创建者的额度」在「成员也要各自订阅」的读法下没有意义。两道闸缺一不可——界面那道是为了**把话说清楚**（为什么 + 一条去订阅的路），RLS 那道是为了**真的拦住**（界面判据跑在用户机器上）。`workspaceAccess` 回**四态**：`unknown`（billing 还没查过）不能并进 `no_subscription`，否则冷启动点一下会劝一个已经付过钱的人再付一次。`past_due` 不算活跃——判据与真正花钱那层（`decideRuntimeRoute` 只认 `active`）一致，否则建得出一个跑不动任何 turn 的工作区。弹窗还单列了「建成功了但列表没刷出来」一态（#843 症状 1：它和「没建成」长得一样，而两者该做的动作相反）。**#1024 把这道闸从「有活跃订阅」收窄成「这一档带工作区」**（ADR-0242）：判据是服务端下发的 `plan.capabilities.workspace`，**不写死 `{pro,max}`** —— 写死的代价是这句话有两份（客户端一份、RLS 一份），只会在改档那天分家且分家不报错。`plan_too_low` 是**独立一态不许并进 `no_subscription`**：没订阅的人走 checkout，已经订着的人走 Portal 换档（ADR-0203 决定 18：对已有订阅的人再开一张 checkout 会变成第二条订阅、两笔一起扣，网关回 409），合并成一句「去订阅」= 给 Lite 用户一颗点了必然失败的钮 —— 与 `unknown` 单列一态是同一条纪律。**服务端一格都没下发时退回改动前的行为**（判据挂在整张价目表上，不挂在单字段默认值上）：RLS 那道闸在同一时刻同样还是旧的，两道闸任何时刻都说同一句话；反过来（缺席按拦）才是真事故 —— 客户端一发版，所有 Pro/Max 用户在数据落地之前建不了工作区。migration `0028_workspace_requires_plan_capability.sql` 另起 `can_create_workspace()` 不改 `has_active_subscription`（让后者顺带判能力就是个名不副实的函数），**只收窄 insert 存量一行不碰**（降档不该让一个群消失）。「存量 Lite 用户怎么办」那个原本的阻塞项**是查真库化解的**：Lite 订阅一条都没有过（ADR-0134 的又一例，数据在 #1024 的评论里）
- `src/shared/workspaceError.ts` / `src/main/workspaceManager.ts` 的 `list()` + `unreadableSnapshot` — 工作区列表**一格挂了不等于整份没有**（#843 ②③）：`fetchWorkspace` 按工作区 `allSettled`，拉不下来的那格降级成带 `loadError` 的占位快照（列表行有的字段照抄、明细全空，侧栏画组头 + 一句红字原因、**不给 ⚙/＋**——设置页会把空名册当事实），host 缓存在部分失败时并上上一次的（喂的是借用路径，「拿不到」≠「被清空」，同 ADR-0197 grants 缓存的规矩）。错误原文过 `humanizeWorkspaceError`：判据先看 PostgREST 的 `code`（42703/42P01/PGRST204 = 客户端比库新、23505、42501），文案正则只是 code 缺席时的兜底；缺 migration 那档**把原文带上**（缺的是哪一列是说给维护者听的），认不出的原样留（ADR-0189 的规矩）
- `src/renderer/src/components/WorkspacesSidebarSection.tsx` / `CloudWelcome.tsx` / `CloudSessionMain.tsx` — 工作区里开会话和本地一个开法（ADR-0218，#919）。每个工作区是一个 `SidebarGroup`，**和本地工程组同一套组件同一组类名**（像是会漂移的，共用骨架不会）：可折叠组头 + 组头 `SidebarGroupAction` + 组内会话行挂同一道竖脊。组头两颗动作——**＋ 只开开局卡不建任何东西**（这是「一致」的全部内容，本地那颗 ＋ 也只是把主区换成 composer），⚙ 进工作区设置。开局卡少了文件夹/型号/挡位**不是漏做**：那三样是**工作区**的属性不是这条会话的（ADR-0202），摆上来就是三个点了不生效的控件。第一句话**排队等 ready**（主进程 `say()` 要 `status==="ready"`，而进房结束时 welcome 还在路上）：落 `cloudPendingFirstMessage`，主区那块的 effect 在 ready 那刻补发，`take` **先摘后发**——effect 会因状态变化重跑，清晚一步发两遍；建/进房失败要连它一起清，否则下次进别的云会话时那句话自己冒出来。主区那条分支排在 `phase === "welcome"` **之前**（进云会话时本地 phase 还停在上一条会话上）；`newSession`/`resume` 各自先 `closeCloudSession()`，所以本地那条路抢得回主区。归档的云会话不进侧栏，去工作区设置页底部（同本地「已归档会话」）
- `src/renderer/src/App.tsx` 的 `AppSidebar`（三档切换器 + `switchTab`）/ `store.ts` 的 `SidebarTab` / `WorkspacesSidebarSection.tsx` 的空态 — **工作区独占侧栏第三档**（ADR-0259，#1087）：切换器从「任务 / 项目」变成「任务 / 项目 / 工作区」，推翻的是 ADR-0217 的**位置**结论（工作区不是项目的子类，是「我在哪儿干活」并列的第三个答案），它的闸与语义一个字不动。跟着重判的是原来那一节里三样「因为和项目挤在同一栏」才存在的东西：段尾那道内缩分组线**删掉**（这一栏底下没有第二组要分开）、空态从「故意不画」翻成**必须画**（原来的理由「项目栏底下已经有一段空态」没了，不画就是切过来一片空白，人分不出「还没建过」和「读不到」）、「新工作区」跟着它生出来的东西搬进这一栏（#923 判据不变），`TWIN_BUTTON` 与 #921 的「两颗要长一样」一起没了消费方、删掉。**工作区栏没有「新会话」**（云会话必须落在某一个工作区里，这一栏顶上没有「哪一个」可言）、**也没有「已归档会话」**（归档的云会话在 ⚙ 那一页底部，摆一颗永远报 0 的钮是撒谎，`archivedCount` 在这一档显式回 0）。**ADR-0256 那两枚角标被这道 tab 挡住了**——人站在任务/项目栏时组头与会话行一处都不在屏幕上，所以切换器那一格右上角亮一枚点，判据是 `unreadMentionCounts(...).total`（`total` 加在纯函数里不在渲染层 filter：「哪一条算未读」只能有一份判据），**画点不画数**（这一格只有 78px，量级在下一层那两枚带数的角标上，同 ADR-0255 的取舍），话进 `aria-label` 不进 `title`。切档顺手退出归档屏收在 `switchTab` 这个**入口**上（那棵树的分支顺序上 `archivedView` 排在 `tab` 之前，不收就会画着项目的归档而切换器写着「工作区」）。三档在 16rem 里的排版是**真机量出来的**：234px 三等分每颗 78，「工作区」三个全角字 42 + 图标 16 + 默认 `px-2 gap-1.5` 的 22 = 80 > 78，而失败形态和事先猜的不一样——flex 项 `min-width:auto`，它不截断也不撑破侧栏，是**自己长到 82 再把两个邻居各挤成 76**（分段控件从此不等宽，切档时白药丸的宽度还会跳）；收窄成 `px-1.5 gap-1` 需要 74，三颗都稳在 78。几何**故意没写成断言**（同 ADR-0236 第 1 条），保鲜期是紧挨类名那段注释
- `src/renderer/src/components/ContextRingTrigger.tsx` / `billingView.ts` 的 `quotaAlert` — **额度吃紧时，输入框那枚环右上角亮一枚点**（ADR-0255，#1073，还的是 ADR-0254「代价与已知未做」第三条）。那枚环只反映**上下文**，额度只画在悬停之后（ADR-0209 的已知代价），而额度是唯一会真正把人拦住的那一个——上下文满了还能压缩。**不是 issue 标题里那道「环外的弧」是一枚点**：两道弧同心同宽，而上下文 >75% 走的也是 warn，最坏形态「橙内环 + 橙外弧」里人分不出哪个在告警，而这个记号存在的全部理由就是让人一眼知道**是额度出事了**（否则他会去按压缩）；点的形状与位置都不同，橙环 + 橙点仍分得开，代价是**不报量级**（量级由悬停后那两只表回答）。判据抽成纯函数 `quotaAlert(billing, now)` 因为有两个消费方（点的颜色 + 钮的 `aria-label`）：`null`（还没查到）与「没订阅」都不画但**记成两条**（前者同 ADR-0240 不许退成 Free，后者是「他没有额度可言」不是「额度充足」）；**`exhausted` 排在百分比前面**——它是网关亲口说的「拦住你了」，而只走 429 那条路时 `windows` 不更新、光看百分比会漏掉 issue 标题说的那一刻，且渲染层这份快照不会自己到点过期所以跟 `liveExhausted` 一样**现算**；色档走 `bindingWindow` + `quotaTone` 与浮层那两只表共用（分家那天点亮着而卡里全是灰的，人会先怀疑这个点坏了）。**话进 `aria-label` 不进 `title`**（这枚钮当初就是为了不跟富浮层抢同一次悬停才不给 title 的，点自己 `aria-hidden`）；光环取 `paper` 那两个 token（`ring-background dark:ring-popover`）不取单一 `ring-background`——深色下那张纸是 popover，抄错的后果是常年挂着一圈浅边；表 60 秒一跳且**作用域圈在这一枚钮里**（钮是常驻的，浮层关着时整棵子树不挂载，不走表则睡一觉回来对着一个早就恢复的红点，而**常年挂着的假警报比不报更坏**）。**不做入场动效**：它是挂一整天的状态记号不是一次事件，而 `@starting-style`/keyframes 会在每次挂载时都放一遍。已知代价：`ctxWindow === null` 时整枚钮不画（#193）于是告警无处可挂、几何没写成断言（同 ADR-0236 第 1 条）、`remainingPercent` 的浮点毛刺（`80/100` 已用报 `19.9%` 不是 `20.0%`）不是本条引入的，另开 issue
- `src/renderer/src/lib/cloudContext.ts` / `components/CloudContextRing.tsx` / `components/CtxBreakdownSection.tsx` — **云会话输入框右下角也有那枚上下文用量环**（ADR-0267，#1138；#985 当年把它连同型号 / thinking 一起判成「团队的属性」少掉了，前两样确实是、环不是）。三条判据全是「云会话是群聊」带来的：**视野按 agent 分**（`projectForAgent`，与 runtime 的 agentView 同一个函数——整份日志当一只算的话，A 报 50K 的账后 B 回一句 8K，环从 40% 掉到 6% 而谁都没压缩）、**工具表从这只自己最后一条 `request_envelope` 取**（桌面没有云 runtime 的 toolDefs，信封是日志里唯一的快照；倒着扫要按 agentId 过滤——别人说出口的那半在我的视野里还带着 model）、**窗口按这只最后一次真跑的型号查目录**（不认识一律 null：不画、不比，#193；一只都没露面时退回 welcome 的团队默认款，所以第一轮就画得出来）。环画**最吃紧**那只（同 `bindingWindow`），浮层逐只列、段头写清是谁的。**额度那半（告警点 + 套餐额度）只给 owner**：读的是 `store.billing` = 我的订阅，而云会话烧的是 owner 的（ADR-0233），`ContextRingTrigger` 为此多了 `quotaApplies`；**钱那段永远不画**（云会话没有 direct 这一档，`showsCost` 在这里只可能说假话）。卡的中段（壳 + hero / 分段条 / 图例）抽成 `CtxBreakdownSection` 两张卡共用，App.tsx 的 `CtxDetails` 只剩本地独有的上下两段。真机一次没跑过
- `src/renderer/src/components/PlanQuotaSection.tsx` / `ModelFootnote.tsx` / `ProviderIcon.tsx` / `src/renderer/src/lib/modelFootnote.ts` / `providerMark.ts` / `fmtTokens.ts` — **上下文浮层给订阅用户重做**（ADR-0254，#1071）：钱**整段删掉**，卡片收成两段——上半「套餐额度」两扇窗画成并排两只表，下半「会话上下文」。判据是**日志里有没有一笔走自己的 key**（`showsCost`）不是 `isSubscribed`：这张卡报的是已经发生的事，而 billing 是此刻的状态（退订过的人那些 hosted 行确实由额度承担过，订阅前那几笔 direct 是真金白银）。**不给替身**——草案里那行「本会话用掉 5 小时额度的 1.3%」被维护者砍掉：换了单位仍然在回答「这一次花了多少」，把按次计费的心智从后门请回来；那一格就是空的。两扇窗**平级**（ADR-0209 的主/次两条条子撤掉），谁先拦住人**由颜色说不由大小说**；环填**剩余**、色档按**已用**（同 ADR-0239），充足时中性灰不上品牌蓝；**清零的窗不画倒计时**（原来「100.0% 可用」和「已恢复」两行自相矛盾）。hero 改用 K（`865,481` → `865.5K`，这张卡上别的数本来就是这把尺子），为此 `NumberTicker` 加 `format?` 与可选 `label`、`fmtCtx` 从 `App.tsx` 搬进 `lib/fmtTokens.ts`。图例两列、**按大小降序**（行序本身就是「谁吃掉了它」的答案）、零值行压弱色不删（删了卡片会在类别出现/消失时抖动）。型号脚注**一行到底**：右边那串定长事实 `shrink-0`、挤不下时只让型号名 `truncate`（截数字等于把唯一的量化信息弄丢），为此**省掉 `tokens` 这个词**（最长的型号名配完整右串要 304px，可用只有 274px），完整那份进 `title`。厂商 logo 是**打进包的本地 SVG**（simple-icons CC0，mask + currentColor，画法同 `McpEntryIcon` 的 mono 档，永远不接远程地址）；`PROVIDER_MARK` 是穷举 `Record`，`null` 不是「以后再补」——**自己描一个「差不多像」的比画首字母更糟**；**认不出的型号不画标**，不走 `resolveModel` 的 DeepSeek 兜底（那条兜底是为了「发得出请求」，画上去就成了给陌生型号安一家厂商）。徽章复用 `PlanBadge`（ADR-0240）。已知张力：**触发器是上下文那枚环，悬停后主语却在卡片下半**（维护者看过两种顺序后选的额度在上），`--warn` 在这张卡上同时是 Max 的徽章色和额度告警色，触发器上仍然只有上下文（额度用完时静息界面一个字都不说，另开 issue）
- `src/renderer/src/components/PlanQuotaSection.tsx` / `src/renderer/src/lib/billingView.ts` — 订阅额度那两扇固定窗**挤在上下文浮层卡的底部**，不另给常驻控件（ADR-0209，#892）：额度是全账号的、上下文是这个会话的，但问它们的时机是同一个。代价明写在组件头注里——**不悬停就看不见**，真用完那一下靠时间线上那条 `route_changed`。主数字取**吃紧**的那扇（`bindingWindow`，并列取 5h），另一扇走中性灰；色档与上下文环共用同一组阈值。过了 `resetAt` 的窗按清零画（`liveWindow`，与设置页共用一份——同一扇窗两个界面不能给出两个数），倒计时只在当主那扇底下单独一行（三样挤一行会顶出这张 300px 的卡）。**两扇窗报的是「还剩百分之几」不是「已用 / 上限」**（#1026 把 ADR-0239 决定 1 末段那笔债还了）：与设置页逐字同一口径 —— 用户问这个数就是想知道「我还能干多久」，而 `0.1 / 311.5` 要人当场做减法；条按**剩余**填（闲着时是满的，原来那个填了 0.03% 的形态在屏幕上和「组件坏了」一样），精确 credit 进 `title`。色档仍按**已用**判、画出来是剩余（同一件事两个说法，判据只能有一份），且**充足时主条也是中性灰不是品牌蓝** —— 一根几乎满格的蓝条会把「一切正常」画得比「快没了」还响，颜色在这两处只用来说「出事了」，当主/非当主的分别改由标签字色 + 那行倒计时承担
- `src/renderer/src/components/ModelPicker.tsx` / `src/renderer/src/lib/modelMenu.ts` / `src/shared/autoModel.ts` — 输入框右下角那枚模型选择器：**列哪几款**，以及 Auto（ADR-0244，#1042）。原来的分组过滤判的是「配没配自己的 key」，而 `routeModel` 里**托管优先于自带 key**（ADR-0176 决定二）—— 一个付了钱、一把 key 都没配的用户打开它**一组都看不到**，他此刻真能跑的那几款界面上一个都不列。失败模式是安静的（渲染不出错、只是少几行），而本机三家 key 都配着把它完全掩盖了（同 #1040 那一族的盲区）。修法是分两组，分工一句话：**上面那组不额外花钱，下面那些烧自己的 key**；订阅那几款从厂商组里**摘掉**不并排出现两次（同一个型号在两处点下去跑的是同一条路，列两遍只会让人以为有得选），顺序原样照抄网关那份（从便宜到贵，ADR-0237 的排序键）。判据从组件的 `useMemo` 搬进 `modelMenu.ts` —— 留在那儿就没有保鲜期。`billing` 拿不到与「没订阅」在这里给**同一个答案：空**（整块退回改动前的样子），方向与 ADR-0217 的 `unknown` 相反而理由也相反：那里并进「没订阅」会劝一个付过钱的人再付一次，这里按「都能用」画会给一个没订阅的人列一排点了必然 blocked 的型号。**Auto 是这一格的一个取值不是第二个控件**：只画不接线 = #722 那个撒谎的勾，所以 ADR-0237 的 Auto 一并移植到本机 —— 分类提示词/「拿不准算 hard」/「认不出来一律 null」三条判据搬进 `src/shared/autoModel.ts` 两条路共用，**两边唯一的差别是怎么向网关证明身份**（runtime 带 `x-runtime-secret` + on-behalf，桌面带用户自己的 JWT），所以那一层收的是一份算好的 headers。不到两款不画 Auto（那时 `pickAutoModel` 一律回 null）；只有主会话那两个入口给 `allowAuto`。**「Auto 开着没有」是 `model_changed` 的投影**（新增可选 `auto?: true`，旧日志照常重放），与型号、lane 读同一条事件、resume 自动回来，不加第二种持久化 —— 三条推论各是一个坑：`nextAuto` **必须**进 `switchModel` 的相等性比较（漏了就是点 Auto 那一下静默什么都不发生，而界面已经显示 Auto）、Auto 自己挑出来的那条**照旧带** `auto`（挑了一款 ≠ 关掉 Auto）、手动挑一款的那条**不带**（那一条本身就是关掉的动作）。判一手排在代读员那道判断**之前**（它会换掉型号，而代读判据问的正是 `supportsVision`），也排在 `image_described` 的 append 之前（`barrenTurns` 按 `events[i-1]` 认领它，中间夹一条 `model_changed` 就断了）。**打开那一眼看不看得见 Auto 是另一件事**（#1049）：cmdk 在挂载时（Radix 还没量完位、列表高度不是最终值）就对选中项 `scrollIntoView({block:"nearest"})` 了一次，于是选中项被顶到列表最上方、Auto 与「订阅」组头一起滚出视野 —— 人人都有一个选中的型号，所以人人都看不见它。修法是浮层打开后等一帧**先回顶再 `nearest`**（`scrollTop = 0` 一句是二选一：要么 Auto 可见、要么找得到当前那款，两件事都要）。这条**故意没写成断言**：jsdom 没有布局（`scrollIntoView` 是空实现、`scrollTop` 恒为 0），写出来只钉得住「调了这个函数」而真正会坏的是时序，保鲜期在紧挨代码那段注释里（同 ADR-0236 第 1 条 `pb-14` 那笔账）。**#1058 又收窄了三处**（ADR-0249，维护者对着真机点的）：订阅那一组**不画组头**（`heading` 放宽成 `string | null`，判据是「这一组是不是唯一一组」—— 厂商那几组照旧写，那边真的有好几组）、**Auto 与其余各款同一个排版**（撤掉 ADR-0244「一行两层」那条：论证没错但代价估小了，一行两层会打断这一列的基线，而这一列每天要扫很多遍、Auto 只需读懂一次；那句话降级成 `title`，与 ADR-0243 点名的「危险状态常驻警示退成 hover」不是一回事）、**不画「视觉」记号**（它在 `GLM-5.3 Flash（视觉）` 那行是同一件事说两遍、在别的行又是唯一信号，两头不靠；代价是选单里看不出哪款看得见图，而真会撞上的路径有代读员和 `routeModel` 的话接着）。`ModelMenuItem.vision` 随之删掉——留着跑不到的数据，下一个人会以为它还在被谁读
- `src/main/modelRoute.ts` 的 `subscribed` 分叉 / `src/renderer/src/lib/billingView.ts` 的 `isSubscribed` / `settingsShell.tsx` 的 `settingsSections` — **订阅用户不许自带 API key**（ADR-0248，#1051；维护者定的产品口径，ADR-0233 已经对云会话定过同一条）。**真正生效的是 `routeModel` 那两条 `return direct`，不是选单**：存量会话里选着的老型号、代读员、后台小模型、子智能体各有各的取模型的路，一条都不经过那枚选单 —— 只藏界面等于 ADR-0233 点名不许的静默失败模式（「额度用完悄悄改烧你自己的账号」）。订阅这一侧从三态收成 hosted / blocked 两态；判据挂在 `hosted.subscribed` 上，所以没装配托管的那些装配（探针 / 测试 / 裸装配）行为一字不变。**Ollama 也在射程内** —— 它不要 key 也不花钱，但留着就是一条「选单里没有、路由却通」的路，而这条规矩的全部意义是让两边说同一句话。blocked 的措辞里**不许再出现「填自己的 key」**（一句无法执行的建议比不给建议更糟，#1040 那一族），四种情形分开说，其中「拿不到 JWT」那条**以前是悄悄退回自带 key 的**、现在必须说出口且不许写成「你没订阅」。`route_changed` 对订阅用户不再落 —— 不落恰恰是对的，落一条「本次起用的是你自己的 key」就是宣布一件没发生的事。设置里收起「模型配置」与「子智能体」，守卫放在 `openSettings` 这个**入口**上（深链 / 齿轮落点 / 会话 ⋮ 都从这条路进来，只滤导航等于「列表里没有、按钮却跳得进去」）；**收起来 ≠ 关掉功能** —— 为此 `subagentRunner` 与 `createChildAgent` 补上了 `hosted`，不补的话订阅用户派一次活必然 blocked。另两条「不是主模型」的调用同样接上：代读员原来调 `routeModel` 但不喂 `hosted`（不修 = **每条带图消息整个 turn 失败**），`createCheapAdapter` 干脆直接读 `process.env` 的 key、整条 `routeModel` 都绕过去（顺带补上一个存量的洞：没配 key 的订阅用户三个外挂**从来没跑起来过**）；两者的出厂默认款（`glm-4.6v-flash` / `glm-4.7-flash`）网关都不供，所以另有 `visionModelFor` / `helperModelFor` 两个纯函数取订阅供着的最便宜那款（代读员那条额外要求带眼睛），**挑不出来就原样返回**让 `routeModel` 去说人话。ADR-0244 的两组从此**不并存**，当初为并存写的那道去重没了消费方、一并删掉
- `src/renderer/src/components/PlanBadge.tsx` / `billingView.ts` 的 `planBadge` — 侧栏名字右边那枚档位徽章（ADR-0240，#1030）。判据一条：**`null`（还没查到）不许退成 `"free"`** —— `billing` 是主进程 push 上来的，冷启动那一瞬间渲染层手上必然是 null，把它并进 Free 就是对着一个正在付 Max 的人说他没订阅（同 ADR-0217 的 `workspaceAccess` 为什么必须有 `unknown` 一态）；这枚徽章的两个失败方向不对称，少画一格没人损失什么。有订阅/没订阅的三条（`plan===null` / `none` / `canceled`，后者是 #865）**与账号页共用这一份**，`me===null` 的分叉留在调用方（账号页两种情形都画价目卡，侧栏必须分开）。`past_due` **仍报原来的档** —— 扣款失败不改变「你订的是 Pro」，「出事了」由账号页那条带按钮的 warn 横幅说（ADR-0239 决定 2），一枚 24px 的徽章带不动出路。四色取现成的语义色 token（灰/`--ok`/`--brand`/`--warn`），代价是 **`--warn` 在本仓别处的意思是「出事了」，Max 借形不借义** —— 哪天要调警告橙，先想起这里挂着一个最贵的档。同一个组件两处用，因为「Lite 是什么颜色」的答案只能有一份（两张色表改一处忘一处不报错，只会让同一个账号在两块屏幕上是两个档）；账号页原来那套「past_due 橙 / 其余蓝」因此撤掉 —— Max 本身就是橙的，两个含义压在同一个像素上都说不清。侧栏另补一次 `loadBilling(false)` 种子：冷启动那一发 push 可能早于渲染层订阅，丢了就永远不出现
- `src/renderer/src/components/BillingSettings.tsx` / `AccountUsageCard.tsx` / `src/shared/modelShare.ts` / `src/renderer/src/lib/usageWindow.ts` — 账号页重设计（ADR-0239，#1022）。**额度两扇窗报「还剩百分之几」不报 credit**，条按**剩余**填充（原来那条填 0.03%，在屏幕上和「组件坏了」长得一模一样；反过来画之后闲着时它是满的），**一位小数向下取整** —— `0.1 / 311.5` 真值 99.9679%，四舍五入写出来是 `100.0%`，把「刚烧了一点」和「一次没动」说成同一件事；`remainingPercent` 在 `limit<=0` 时回 100，与 `windowPercent` 的「已用 0%」互为补角。**色档仍按已用判**（`quotaTone`，与浮层共用阈值，ADR-0209）画出来的却是剩余，**充足时中性灰不上品牌蓝**（一根横贯整卡的蓝条会把「一切正常」画得比「快没了」还响）。浮层那半这次没跟着改，两处报的是同一事实的两种说法不是两个数。Free 卡**不是** plan 表里的一行，是「没有订阅」这个状态本身的名字（$0 由定义得出，不受 ADR-0203「价目服务端下发」约束），虚线边框 + 不给主行动按钮 —— 它不是可买的东西。**「工作区」那枚 chip 故意没做**：`capabilities` 要加字段、ADR-0217 的闸要从「有订阅」收窄成「档位 ∈ {pro,max}」且界面与 RLS 都得改，只画 chip 不改闸 = #722「撒谎的勾」的一般形式。**使用情况是一张卡两半**（左热力图 / 右本周各模型 token 占比），两半口径不同各自说出口不硬对齐（左只数你自己开的主会话 #141、云会话不写本机日志；右算所有计费调用含子会话含归档）。`modelShares` 按**型号 id** 归并不按厂商：`usageStats` 那条路 `describeModel` 认不出就整行 `continue`，按厂商归并时那是对的（硬塞进某一家会让那家数字凭空变大），代价是本机真实调用**静默消失**（真库里 `glm-4.5-flash` 那批就是这么没的）；按型号没有归属问题，`provider` 为 null 画中性方块。只报占比不报 token 数也不报钱（四种钱的写法一次都不用出现，也绕开 `modelPricing` 缺 `k3` 键），代价「按 token 不按钱」写在脚注；前 5 之外合成「其余 N 款」不静默截断。`weeklyUsageWindow` 对不齐额度窗时**标签跟着换**成「近 7 天用量」。热力图元件只画图不画卡，月份刻度**跨 3 列 = 与最小间距同一个数**（跨度大于间距时撞上的会被 grid 挤到第二行，而那一行定高、第二行整个溢出压在格子上），格子 `w-fit` 收住（上游写死 `repeat(N,1fr)`，占满宽度时会把格子摊成长方块）+ 按 `cell.column/row` 显式落位不赖 DOM 顺序
- `src/renderer/src/lib/sidePanel.ts` / `src/renderer/src/lib/useBackgroundWatch.ts` / `src/renderer/src/components/BackgroundTasksPanel.tsx` — 右侧槽位那一族面板的单一开关（`panelKeyOf` / `panelFlags`，互斥由构造保证，不再每个 action 手抄一遍）+ 后台任务的常驻盯梢：轮询 live 名单、0→非 0 时自己把面板掀开。**只在槽位空着时掀**（ADR-0139）。面板每行画成一台终端（`elements/terminal-block`），为此 `execDetached` 补了可选 `onOutput`：**两条起点各有各的接法**——显式 `run_in_background` 直接递进去，前台跑满 30 秒自动转的那条得在命令起跑那刻就挂 tee（进程已经在跑，回调加不进去），漏掉后者就是「面板上一片空白」的那半（ADR-0195）。尾巴**按会话分格**（`bg-N` 每个会话各数各的，平铺会串台）、轮询回来的名单整份替换而尾巴只合并（跑完没贴回的那张卡不该每 5 秒被清屏）
- `src/world/liveGroups.ts` / `src/world/residueLocal.ts` / `src/shared/residue.ts` / `src/session/residueProjection.ts` / `src/renderer/src/components/ResiduePanel.tsx` — 水獭留下的东西谁来收（ADR-0193，#759）：kill 的单位从进程换成**进程组**（`detached: true` + `killGroup(-pgid)`，三条 kill 路径统一），`close` 事件不等于死亡——注销前探一次活，有活口就进 escaped，这是残留清单第一个零误报的数据源。simctl / lsof 拍的快照与 baseline 做**差集**，**没有 baseline 就不出清单**（兜底成空快照 = 宣称「开机时零端口零模拟器」，整机的东西全被算成本会话的残留，还默认勾上）。「算不算清完了」的判据是 `CleanupResult.kind` 这个**事实**，不是 `ok + note` 那组线索——三处消费方各自猜的时候，「信号发了但进程还活着」因为带了 note 被一致当成成功抹掉，三处一致地错比一处错更难发现；`failed` 留在清单上，`cleaned` 只在探活确认之后才允许回。残留是 **app 级**事实（进程组/模拟器/端口不属于哪个会话），所以直播那条**故意不按 sessionId 分流**：归档同时把 currentSessionId 清成 null，而归档那一刻的全量 diff 恰恰是 ports/simulators 类残留的唯一来源
- `src/renderer/src/components/SideChatWindow.tsx` 的 caret / `tests/e2e/streamingCaret.e2e.ts` — 流式光标只归**此刻还在长字的那一段**（ADR-0214，#894）：光标 = `caret && isAnimating` 同时成立的 `<Streamdown>` 实例数。主聊天那条路本来就对（投影把一个 turn 合成一条消息，只有 live 那条 running，`isAnimating` 取 **part 自己**的状态），**浮窗**有——它不走 aui，自己手写两个实例且都传了 `caret`。修法是让「只有一个」**由构造保证**（整个浮窗只有流式缓冲那一个拿得到 caret），不是把两处条件对齐；`animated` 留在原处（入场动效 ≠ 还在长）。`app.css` 的 `.streaming > :last-child::after` 是从没挂上过的死 CSS，已删——它骗掉过一只水獭两小时（#891）。e2e 的采样跑在页面内、判据取内联 `--streamdown-caret`，两条踩过的坑写在 ADR 第四节
- `src/renderer/src/lib/agentPhase.ts` — 运行指示条那枚药丸写什么、配哪个 orb。六档，审批最优先；调用方保证 turn 在跑（ADR-0133 / issue #549）
- `src/renderer/src/lib/liquidGlass.ts` / `src/renderer/src/components/LiquidGlass.tsx` — 液态玻璃卡片：位移贴图（纯逻辑）+ 挂滤镜的壳，材质配方在 `app.css` 的 `.liquid-glass`。失败模式是**静默的**（整条 backdrop-filter 被丢掉），所以 e2e 里有一条专门盯它（ADR-0132）
- `src/main/imageIntake.ts` / `src/renderer/src/components/elements/image-generation.tsx` — 工具产出的图：字节落附件库、日志只记 ref 的那道中间件 + 显示它的卡。**上游那张卡没有 `<img>`**（完成态画的是一坨写死的渐变），本仓改动一览写在文件头（ADR-0144）
- `src/shared/imageFit.ts` / `src/main/imageCodec.ts` — 超上限的图**先缩再入库**，缩不动才拒（ADR-0208，#882）。阶梯（长边/画质一级级往下试，每级量真实字节数）**与手机端共用一份**，干活的编解码器各是各的（桌面 Electron `nativeImage`，手机 expo-image-manipulator）；纯逻辑在 shared、编解码器注入，所以 `intakeFile` 那一层照旧不碰 electron，`encode` **必填无默认**（忘接线该编译不过）。目标 4MB 不是 10MB —— 入库成功 ≠ 送得到模型（Anthropic 单图 5MB）。天花板实测：`nativeImage` 只解 png/jpeg，webp/gif 走 `undecodable` 原样交给 10MB 那道闸
- `src/renderer/src/lib/mcpInstalled.ts` / `src/renderer/src/components/McpServerEditor.tsx` — 已装的那几台也是卡片，
  「已接通」/「待接通」两组置顶（一张写着「连不上」的卡挂在「已接通」下面是自相矛盾；藏起来更糟——那台就再也点不到了）。
  管理面（开关/改地址/重连/删除）住在详情页里，页面下半那份等宽 id + 裸 URL + `0 资源 · 0 prompt` 的清单已删。
  连不上的那句错误过 `humanizeMcpError`：只翻认得出的，认不出的原样留着，原文进 `title`（ADR-0189）
- `src/renderer/src/lib/mcpDetail.ts` / `src/renderer/src/components/McpConnectorPage.tsx` — 目录卡点开的那一页：
  事实清单（先说「代码跑在谁的机器上」）+ 要填的参数 + 已装那台的工具清单。**换页不是弹窗**，主行动那一格
  复用 `installSlot`（判据分叉就是 #722 那个撒谎的勾的一般形式）。组件长出第二条 return 路径时，挂在旧路径
  末尾的对话框要跟着一起挂——它们看起来挂在组件上，其实挂在某一支上（ADR-0185）
- `src/shared/mcpCatalog.ts` — 常见 MCP server 的目录数据（人手维护、会过时；字段与占位符自洽由 `tests/shared/mcpCatalog.test.ts` 钉住，ADR-0118）
- `src/shared/defaultWorkspace.ts` / `src/main/taskWorkspace.ts` / `src/main/taskFolderPrune.ts` — 内置 Default 按会话分格（ADR-0206，#851）：任务会话的判据是一个纯函数（等于 Default 根 **或** 父目录是它，主进程与渲染层共用）、先铸 sessionId 再 mkdir 子目录再建会话（`presetSessionId`）、设置页清理空壳只删「名字像 id + 空 + 不是活着的会话」
- `src/main/projectPackager.ts` / `src/tools/packageProject.ts` — 「打包为项目」：把 Default 工作区的产出搬进 `文档区/Mr Otto/<项目名>`（唯一故意越出围栏的工具能力，两道闸与被否掉的路见 ADR-0135）；`src/main/workspaceSettingsStore.ts` 是默认工作文件夹/内置 Default 的落盘与解析（#559）
- `services/runtime/` — 工作区群聊会话的云 runtime daemon（ADR-0199）：装配现有 agent 核心接进 VPS。`sessionService.ts` 是主装配点（engine + turnCoordinator + approvalRouter + pxTools 接线；中途插话只 `store.append` 一条 `chat_message`，不碰 engine 半个字，靠 engine 每轮从日志重新投影天然生效）；`frameHandler.ts` 是 cid 世界的纯协调层（hello 验籍 → 会话帧分发；踢人「关门」= `requireStillMember` 在 say/backlog/approve/stop/config/archive 六处复查在籍 + `dropCid` 摘广播席位，只堵写路径会留一条无限期只读订阅）；`config.ts` 缺任何一个必需 env 就 fail fast；`sandbox.ts` 管每工作区一容器一卷的建/停/孤儿回收（空闲 30 分钟停、孤儿两阶段 7 天宽限期才真删）。**clone 那一段的三条判断都在 ADR-0200**：① 要不要 clone 是一张说得出理由的决策表（`probeWorkState` 五态 → `decideCloneAction`），不是一个布尔——清空只发生在「能证明可丢弃」的两种情形，其余 refuse 并说清，`ahead` 拿不准按最坏情况算，**每条分支都有回执**（含 skip；`CloneOutcome` 是 kind 不是 ok+reason，同 ADR-0193）；② **凭据不进水獭那台容器**——clone 跑在一次性旁路容器里（挂同一个卷，跑完整台删掉，`mrotto.clone` 标签给 reconcile 收残骸），推翻了 #821 的「同居 + 用完即焚」，那套 cleanup 机制跟着删了；③ 云会话的 system 提示词来自建会话时 append 的 `session_created`（`cloud` 字段，投影可从日志推导），不是 runtime 现场拼。仓库配置这条线有**读路径**：`welcome.repo` 带当前配置 + 最近一次 clone 结局（token 不下行，只回 hasPat），`config` 有 `config_result` 回执，`validateRepoUrl` 两端共用一份结构化白名单且服务端自己也校验一次。**二期终审那批遗留在 ADR-0201**：`rateLimit.ts` 四档令牌桶（say/turn/create/stop，第四档 `STOP_BUCKET` 是 ADR-0227 加的：停止不起模型调用、看着免费，但每次成功的 stop 都往日志里落一条系统发言；桶按 uid 不按 cid，一帧只记一个桶，被限的一个时段只记一笔；`FrameHandlerDeps.rateLimit` 故意是必需的——忘接线该编译不过而不是静默拆闸），拒绝在**两个房间用两种回执**（控制房 `denied: rate_limited`，会话房 `error` 帧——客户端把 denied 当终态会直接断连接，而限速是「待会儿再来」）；归档三层分工（`sessionService.archive` 只碰日志、daemon 写库+延一拍收房、frameHandler 判「owner 或建的人」），**成功不回执**——`session_archived` 广播给所有人那份就是回执，渲染层的判据是日志里那条事件不是「我刚点了归档」；clone 是 `--depth 1`（卷没有磁盘配额，历史往往比工作树大一个量级；这台 VPS 是 overlayfs + ext4，`--storage-opt size=` 用不了，验法记在 ADR-0201 第六节）。**模型 key 跟着工作区走**（ADR-0202，#844，推翻 ADR-0199 决策⑥；**ADR-0233 又推翻了它**：云会话统一走所有者订阅额度，没有自带 key 那一级，`config` 帧只剩仓库，下面这段留作历史）：`config.ts` 的必需项里没有 MODEL_*，`adapterFor(workspaceId)` **每次 chat() 现读一次**工作区配置（会话房是长命的，定死意味着改完 key 要重启 daemon），没配就抛一条**给人看的**错走 turn 失败路径落日志——**刻意不做 env 兜底**：兜底 = 忘了配的工作区默默烧维护者的钱，而那个失败模式是静默的（只有账单会说话）。`config` 帧的两组字段各自可选，服务端按 patch 合并——只改模型时把仓库地址一起发过去，等于替 owner 声明「那一格我也确认是这个值」。云会话有自动压缩与护栏硬停（ADR-0225，#957）：`contextWindowOf` 按路由后的实际型号现读、`loopGuardMaxNudges: 5` 命中即抛错走既有 `turn_ended{error}` 路径收口，都是可选字段、缺席 = 桌面那条路的现状不变。**停止 + 归档顺带停**（ADR-0227，#957 第三批）：`CloudSession.stop(byUid, byLabel)` 三态 `"ok"|"idle"|"not_allowed"`（判据与审批逐字同一条，只停当前这一轮不清队列），`archive()` 顺带 `abortCurrent()`，daemon 的 `closeRoom` 从固定 2 秒改成先等 `settled()` 或封顶 10 秒——三条动作各自的回执（`say_result`/`approve_result`/`stop_result`）在 `frameHandler.ts` 里转译，不复用 `error`
- `src/world/dockerWorld.ts` — `ExecutionWorld` 的容器实现（v2，ADR-0199）：工具经 `docker exec` 进工作区容器，loop 留在宿主进程——模型 key 与 MCP 凭据永不进容器（只有 http fetch 走宿主侧）。`docker exec` 没有 kill 原语，运行中中断只能 reject 让包在命令外层的 `timeout(1)` 自然到期兜底收口——已知天花板，不是漏做
- `src/shared/remote/cloudSession.ts` / `src/main/cloudSessionClient.ts` — cs 帧协议（`cs_hello`/`cs_say`/`cs_event`/`cs_backlog`/`cs_approve`/`cs_config` 等，三端共用一份，ADR-0199）+ 桌面主进程的云会话客户端。桌面是**显示器**不是执行者：真正跑 turn 的是 VPS runtime，桌面订阅直播事件 + welcome 后 `backlog(afterSeq:-1)` 拉全量补历史。直播事件先于 backlog 到达时先进 `liveBuffer`，backlog **最后一片**（`done:true`）才与它合并按 seq 排序统一转发（复审实测复现过乱序）；云会话从不进本地 `store`，靠 `cloudSessionFleetRow` 合成一条虚拟 `SessionSummary` 才挂得上 fleet/手机（`pushFleet` 以本地 `store.sessions()` 为遍历主体，虚拟行只是追加）。协议 5→6（ADR-0227，#957 第三批）：`CsUp` 加 `stop`，`CsDown` 加 `say_result`/`approve_result`/`stop_result` 三条回执——这是桌面得知一次 say/approve/stop 被业务拒绝（限速、权限、无在跑 turn）的唯一途径，`cloudSessionClient.ts` 按 `pendingSay`/`pendingApprove: Map<callId,…>`/`pendingStop` 等它们，15 秒未到算"不确定"。协议 15→16（ADR-0263，#1107）：`CsDown` 加 `delta` 帧——**云会话的助手输出流式下行**。碎片是帧不是事件（不落日志、不带 seq、不进 backlog、不去重，新事件类型的十一处穷举表一处都不用碰），`text` 是**累计快照**不是增量（中继掉帧/中途 join/重连都不会在预览上咬出洞）。runtime 的 `services/runtime/src/deltaStream.ts` 按 agent 50ms 合帧（与本机 deltaCoalescer 共享契约不共享代码），`sessionService.notify()` 开头无条件 flush（任何事件出门前先放完碎片，同本机 `send` 包装那处纪律），终态落盘后 `clearAgent`。桌面 `sendDelta` 绕开 liveBuffer/seenSeqs 直转渲染层；渲染层 `lib/cloudStreaming.ts` 整槽替换（不拼接），`assistant_message`/`turn_ended` 清槽，UI 落在 `PendingTurnLines` 的 running 气泡上（三点换成正在长的文字）。reasoning 不过线（终态气泡不画它，预览也不画）
- `services/edge/src/llmGateway.ts` / `quota.ts` / `billing.ts` / `billingQueries.ts` / `src/shared/billing.ts` / `src/main/hostedQuota.ts` / `services/runtime/src/hostedRoute.ts` — 订阅制计费（ADR-0174/0175/0176 定形，ADR-0203 落地，#696）：网关在 edge Worker，一户一个 `Quota` DO 做**双固定窗**（5h + 周，累计数不是环形桶，惰性清零）的 hold/settle；`usage_event` 是钱的唯一事实、DO 是投影，冷启动从事实重建。三端共用 `src/shared/billing.ts`（同 wire.ts 的纪律）。桌面 `routeModel` 三条出路：hosted（订阅优先于自带 key）→ direct → blocked（措辞分三种）；网关 429 `quota_exhausted` 是 adapter 里单列的 `reroute` 类——不退避、立刻重解析端点一次，改道落 `route_changed`。runtime 以 `x-runtime-secret` + `x-otto-on-behalf-of` 代表**工作区所有者**调网关（ADR-0217 推翻了 ADR-0203 决定 9 的「代表发起人」，#917），仍不持有任何模型 key。Stripe 裸 REST（新旧两代事件形状都收：period 在订阅对象上还是在**条目**上、invoice 的订阅 id 在 `subscription` 还是 `parent.subscription_details` —— webhook 的 API 版本跟着账号走不跟着代码走），订阅投影只认 `customer.subscription.*`；**换档只走 Customer Portal**，对已有订阅的人 `checkout` 回 409（再开一张 = 第二条订阅、两笔一起扣，ADR-0203 决定 18）。**中断 ≠ 没花钱**（决定 17，推翻 spec）：只要有字节转发出去过，中断的流按预扣估算结算而不是 release —— 「收到内容之后断线」否则就是一个能无限重复的白嫖洞；`tapSseUsage` 的 finalizer 因此报事实（usage + 转发字节数）不报结论。部署五步 + 手验九条在 `services/edge/README.md`
- `src/tools/generateImage.ts` / `src/main/modelRoute.ts` 的 `routeImage` + `imageBlocked` / `src/session/latestImage.ts` / `services/edge/src/billingQueries.ts` 的 `modelsForMe` — 会话里出图（ADR-0257，#1081）：路是**桌面 → edge 网关 → OpenRouter**，官方 key 只活在 Worker secret 里、客户端一个字节都拿不到。写进桌面包那条否掉了 —— app bundle 是用户机器上的可读文件，key 挖得出来 = 无限花维护者的钱，而「额度记在用户头上」这件事在客户端**结构性地做不到**（客户端自报花了多少钱 = 把钱那条链交给它）。网关几乎不用改：转发体是 `{...body}` 展开的，所以 `modalities` 原样上去、`message.images` 原样回来 —— **透传补了一条断言**，它今天只是实现的副产品，加一层字段白名单会让出图安静退化成纯文本回复（症状是「模型说画好了但一张图都没有」）。计价**按输出 token 不按张**：真机实测一张图 completion 1120、账单 $0.0672055，`price_out` 填 $60/M 之后现成的 `costMicro()` 逐 micro 相等，于是出图就是一次输出特别贵的普通调用，网关不用另开计价路径。`model_route.kind` 不是洁癖 —— `me.models` 是输入框那枚选单的数据源，而 ADR-0237 的 Auto 拿 `models.at(-1)` 当「最贵 = 最强」，$60/M 会让纳米香蕉变成 hard 档主模型（一次正常提问得到一张图）；缺席/认不出一律按 `chat`，迁移跑之前行为一字不变。判断从 `worker.ts` 搬进 `modelsForMe`（那个文件不进 vitest，同 `usageAttribution` 的教训），`imageModels` 加在 `meFromParts` **最末**（七个位置参数，插中间会让既有调用把 `modelPlatforms` 喂给新参数，两者都是「数组或对象」tsc 拦不住）。**出图没有 `direct` 这一档**，所以 ADR-0233 点名的「额度用完悄悄改烧你自己的账号」在这条路上不存在；四种 blocked 分开措辞，「网关不供出图」与「连不上网关」都不许写成「你没订阅」。`imageBlocked` 与 `routeImage` 拆两半是因为 `available()` 必须同步而拿 JWT 是异步 —— 两个消费方共用一份，各写一遍就是「工具表里有这把刀、点下去说你没订阅」。**没订阅 = 工具表里根本没有它**：挂着它意味着模型先说「我给你画一张」再失败。图生图靠 `edit_last` 不靠 id —— 模型看得见图却看不见附件 id（`deriveMessages` 把附件折成 `image_ref` part），`latestImageRef` 倒着扫日志，用户贴的和工具产出的合成一条时间线（「把刚画的这张改成夜景」是最常走的那条）。UI **零改动**：`generatedImagesOf` 认的就是 `prompt` 这个参数名（ADR-0144 决定五「只认这一个名字」）。`HttpPostOptions.timeoutMs` 是为它加的（实测 10.4s / 44.5s，30s 写死会超时；不放宽全局默认）。已知代价：这笔钱进 `usage_event` 但 `CostPanel` 看不到（那块读的是本机 `assistant_message.credit`）、云会话那侧不挂、**要跑 migration 0031 + `wrangler secret put OPENROUTER_API_KEY` + 部署 worker 才生效**（顺序上 migration 先于 worker，`routesQuery` 的 select 带了 `kind`）
- `src/shared/imageModel.ts` / `services/edge/src/llmGateway.ts` 的 `upstreamPathFor` / `supabase/migrations/0032_*.sql` — 出图型号选得了了，且七款一起（ADR-0261，#1086）。三件事咬在一起：① **整条出图路从 `/chat/completions` 换到 OpenRouter 的 `/api/v1/images`** —— 纯出图模型（Seedream 一族、GPT Image 2，`output_modalities` 只有 `["image"]`）在前者上一律 404（上游原话 `No endpoints found that support the requested output modalities`），而 `/images/models` 的清单里**连 Gemini 那三款也在**，所以它是统一入口不是特例，修法是换过去而不是按模型分两种形状；判据挂在**路由行的 `kind`** 不挂在客户端敲的路径上（照后者判 = 「这款模型该怎么调」有两份事实、其中一份在用户机器上），`usage` 形状两条端点逐字相同所以 `parseUsage` / `costMicro` / hold-settle 一个字没动。顺带修掉一笔超收：`gemini-3-pro-image` 走 chat 那条会回 2 张图 + 251 个 reasoning token 而 `costMicro` 只有一个输出单价，按出图价算那 251 个 = 超收 20%。② **价按两族两种算法填，都是真机打出来的**：Gemini 一族 `completion_tokens` 是真实出图 token 数、单价取 `pricing.image_output`；**Seedream 一族 OpenRouter 按张收费而回包里 `completion_tokens` 恒等于 16384**（一个常数），照前者填会超收 2~4 倍（第一版就是这么错的，实测 3.9x/3.9x/2.0x），改成 `每张价 ÷ 16384` 之后 `costMicro` 在这一族上退化成按张定价、算式不用改 —— **这一族的保鲜期是脆的**（16384 是 OpenRouter 的记账约定，不是我们能断言的东西），所以有一条纪律：**加一款出图模型之前先打一次真调用对账单**。默认款跟着最便宜那款变、**不给谁钉 `priority`**（那会让这一列同时承担排序和钉默认两件事，而 ADR-0237「最便宜那款是个承诺」正是靠它只说一件事才成立）。③ 选择本身是**新事件类型 `image_model_changed`**，与 `model_changed` 分开两条不是洁癖 —— 合成一条的话每次换文字模型都得把出图那格一并带上，否则「缺席」是「没变」还是「清空了」说不清，而换文字模型是每天很多次的动作。新事件类型的检查清单**实际是十一处**（上一条索引列的十处 + `Timeline.tsx` 的 `EventRow`），而第十一处不是靠人记住的，是 `tests/renderer/timelineLists.test.ts` 那条「两份名单一致」当场翻红抓出来的。选中的那款网关下架了就**回落最便宜那款不报错**（`pickImageModel`，主进程解路与选单画勾共用一份）；**turn 进行中允许换**（那把刀每次调用现解一次路，不会把正在跑的一轮劈成两半）
- `src/renderer/src/components/ModelPicker.tsx` 顶上那枚 `Tabs` — 「文字 / 图像」（ADR-0261，#1086）：与侧栏「任务 / 项目」同一套组件，钉在 `ModelSelectorList` **外面**（列表可滚，开关跟着滚走就不知道自己在哪一格）。四条判据：清单空（没订阅 / 还没查到 / 网关不供出图）或没给回调 = **整枚不画**，下面一切退回改动前的样子（同 `modelMenu` 对 `hosted` 的处置）；图像那一格**不走 `ModelSelectorItem`** —— 它的选中态与 `setValue` 都挂在 Root 的 `value` 上，而那是文字模型，借它画出图清单会让触发器那行字跟着变成出图型号，也就是那颗按钮开始说假话；勾落在**会被用到的那一款**（`pickImageModel` 的结果）不落在 `imageModel` 上（没选过时该显示网关会替他挑的那款，选过但下架了时该显示真会跑的那款）；**每次打开回到「文字」格**（触发器写的是文字模型）。图像那一格的**厂商标另记一张表**（`IMAGE_MODEL_VENDORS`，与名字表键逐个对上、有断言钉着）——不从 `model_route.platform` 推（那对七行出图路由全是 `openrouter`，说的是上游是谁不是牌子是谁），也不按 id 前缀猜（陌生前缀的新模型会被安错一家，同 ADR-0254）；`ImageVendor` 与 `ProviderId` 是两套东西不合并（ByteDance 压根不是本仓的模型提供商，塞进 `providerCatalog` 是让那张表撒谎）。名字走 `IMAGE_MODEL_LABELS` 这张人手维护会过时的表，认不出的**原样显示 id**。**Auto 那一档与文字那格不是同一套机制**（文字那边真打一次分类调用判难度，这边只是「由系统挑」= 最便宜那款），但它顺手还掉一笔账：**「没选过」与「显式选 Auto」合成同一档**（`isImageAuto`），于是勾在 Auto 上还是在某一款上，恰好回答了「这是我选的还是默认就是它」；口令复用 `AUTO_MODEL`，它不在 `imageModels` 里所以路由那侧天然回落，`pickImageModel` 一条分支都不用加
- `src/shared/sessionPackage.ts` 的 `PRIVACY_VERDICTS` — 分享会话的隐私闸是**每个事件类型都必须表态**的 `Record<type, "strip"|"keep">`，不是 denylist（#850）：新加事件类型不来这里写一笔，tsc 直接红。表态的问题只有一个——这条事件说的是「这段对话」还是「发送方这台机器 / 这个人的私事」，后者 strip。它是新事件类型检查清单的第九处（前八处：events.ts union + `KNOWN_EVENT_TYPES_MAP`、persistencePolicy、deriveMessages、deriveSections、`toThreadMessages.isAuditEvent`（原来这里写的是 `threadGroups.isInvisible`——那是死代码，`groupThread` 在 `src/` 里零调用，`App.tsx` 自己的注释说「已经没有消费者」，真正决定「这条事件上不上时间线」的是 `isAuditEvent`，issue #929，ADR-0219 顺手改对这条索引）、deriveUsage、`src/shared/contextEstimate.ts` 的 `pendingAfter` switch（这条不是 `Record`，加新类型不会报错，只是圆环静默少算——ADR-0219 那次 `agent_briefed` 就先漏进了它的 `default` 分支）、`src/session/agentView.ts` 的 `OTHER_AGENT_VERDICTS`（工作区多智能体的上下文隔离表，同一族的第二份穷举 `Record`，ADR-0219））
- `src/session/eventLog.ts` / `src/session/agentView.ts` / `services/runtime/src/sessionService.ts` — 工作区多智能体的上下文隔离（ADR-0219，#928）：`agentView(store, agentId)` 是**变换不是过滤**——别人的 `assistant_message` 要剥掉 `toolCalls` 才能进我的上下文，留着它、丢掉配对的 `tool_result` 的话，`deriveMessages` 的悬空工具调用自愈（ADR-0005）会替它造一条「没执行」的假 tool 消息，比崩溃更难查。**ADR-0268（#1146）把「变换成什么」改了：别人的 assistant_message 投影成带名字的 chat_message（`[名字]: 内容`，user 角色）**——原来剥完字段仍是 assistant 角色，每只 agent 把别人的话读成自己说过的（真机「开发」答「收到接力，管理员这棒我来收个尾」），且 DeepSeek thinking 模式 + 请求带 tools 时**最后一条 user 之后的每条 assistant 消息都要带 reasoning_content**，接力开场白后面跟着别人两句话 = 开口前 400；名字从那只 agent 的 agent_briefed 按日志顺序现取（发言那一刻的名字），没有就退回 agentId。自己的工具回合能过不是因为传了 reasoning_content（本仓从没传过），是 DeepSeek 按它发的 tool_call id 在服务端缓存了思考——缓存没了一样 400，见 #1151。这些都是用真接口对着从日志重建的请求验的（`auth.json` 里的用户 JWT 直接打 edge 的 `/llm/v1/chat/completions`）。`context_compacted` 必须按 `agentId` 过滤、不能无条件转发：压缩摘要是按视角生成的（ADR-0003），漏一条别人的压缩事件 = 我的整段真实历史被换成别人视角的摘要，不崩不报错。`OTHER_AGENT_VERDICTS` 的初稿是一张丢弃名单，漏掉的正是 `context_compacted`——换成穷举 `Record` 之后加新事件类型不表态 `tsc` 直接红，形状同上面那条 `PRIVACY_VERDICTS`。`sessionService.ts` 的 `engineFor` 按 `agentId` 缓存 `LoopEngine`、命中即返回：**adapter 只在这只 agent 第一次开口时定死**，`daemon.ts` 注释承诺的「改 agent 下一 turn 生效」对改提示词成立（`briefIfNeeded` 会补一条新 brief）、对改型号不成立且静默——同 ADR-0202「每次 `chat()` 现读」的教训在 agent 粒度上又踩一遍——1a 走不到（型号白名单还没有界面能改），**1b 接上界面后已修**（ADR-0220，#932）：命中缓存也 `setAdapter`，**不比对「变没变」**（判据一漏就是安静地继续用旧型号，而 `setAdapter` 是纯赋值，白设一次不花钱）。1b 另外改了这个文件里最要紧的一条：**发言先落盘再排队**——`say()` 解出非空名单就当场 `append` 一条 `user_message{fromUid, mentions}`，`runJob` 改用 `engine.runLoggedTurn(job.opening)` 对它起 turn，「排队中 / 正在回复」因此是 `openTurns` 的投影（`src/shared/turnLedger.ts`）而不是内存队列的状态，装配末尾按同一份推导补跑上一个进程没跑完的。排队仍然纯内存、重启仍然丢，兜住的只是「开场白在日志里」。**「答两次」堵在不变量上不堵在日志推断上**：`unseenUserTail` 对带 `mentions` 的 `user_message` 一律不重采样——每一条这样的消息在落盘那一刻就已经有一个 turn 归它；按日志推断「已答过就跳过」的那版被否了（`turn_ended` 不只有 `completed`、`concludesTurn` 那条 return 不查尾，两个洞都静默丢消息）。客户端给了 `mentions`（含 `[]`）以它为准、服务端不再重解析正文（坑 ④）；排队期间 agent 被删落一条它自己的 `turn_ended{error}`——`chat_message` 不带 `agentId`，收不了 `openTurns` 的口，界面上就永远转圈
- `src/shared/turnLedger.ts` / `src/renderer/src/lib/agentMentionInput.ts` / `src/renderer/src/lib/cloudTimeline.ts` / `src/renderer/src/components/WorkspaceAgentsTab.tsx` — 工作区多智能体点亮桌面那一半（ADR-0220，#932 切片 1b）。`turnLedger` 的 `openTurns` 是「谁欠谁一个回答」的日志投影（点名的 `user_message` → 那只 agent 之后有没有动静 → 有没有它的 `turn_ended`），**runtime 的重启补跑与渲染层的状态行共用这一份**——两处各写一遍迟早分家（同 `agentMention.ts` 的纪律）。收口**要看 `turn_ended.readUpToSeq`**（engine 在起跑那一刻记的日志末条 seq，只在配了 `agentId` 的 engine 上写）：turn 跑到一半才到的那条点名这一轮**从头到尾没看见过**（协调器只对**排队中**的 job 去重，而 `unseenUserTail` 已经不重采样带 mentions 的消息），拿「任意 `turn_ended` 都收口」判就是提前收口——这个窗口里 daemon 一重启，`openTurns(seed)` 再也找不到它，没人答它，永远。缺席 = 旧日志/本机会话，按老规则。`agentMentionInput` 只回答「光标此刻是不是停在一个没打完的 @ 后面」，**不是 @ 解析**：这句话最后点了谁一律由 `parseMentions` 说了算，边界判据抄它的口径（不然 `rick@x` 会弹出选人）。发送带上算好的 `mentions`，chip 行与发送用的是同一次结果，界面说给谁和服务端跑谁不可能分家。选人弹层走 Radix `Popover` 不自己 `absolute bottom-full`——这一页装在 `overflow-y-auto` 里，新会话时 footer 离容器上沿不到 90px，往上画的列表被裁掉**且滚不到**；**故意没有 `onBlur`**（切窗口不表达「这个 @ 我不要了」，误关一次人就只能换行躲开），指针出界交给 `onInteractOutside`（点回 textarea 本身要 return，它是锚）。进出场 keyframes 手写在 `app.css` 按 `[data-slot="popover-content"]` 挂：`animate-in` 一族在本仓是死类名（同 `ui/dialog.tsx` 顶部的警告），挂 slot 所以三个 Popover 消费方一起吃到。`cloudTimeline` 是署名与归属的纯逻辑——`fromUid` 在场按 uid 判「是不是我」（同名两人分得开），缺席才退回 1a 的 label 前缀比对。`WorkspaceAgentsTab` 是 agent 的建改删 + 型号白名单，权限矩阵钉在 `workspaceView.ts` 的 `agentRows`（种子管理员没有删除钮）。两处 placeholder 的不对称是**保留的**：开局卡不带 `mentions` 走老语义（「不 @ 谁的话由管理员接」），composer 总是带数组（「不 @ 就只是群里说一句」）——统一成一句必然有一处撒谎。已知未修：#933 补跑无上限、#934 增量快照仍可能多答一次、#935 全角 ＠、#936 `origin` 全仓没换皮
- `src/renderer/src/lib/workspaceMentionItems.ts` / `CloudSessionPage.tsx` 的 `MentionAvatar` — 云会话 @ 选人弹层里的**人类成员**与那一行的版式（ADR-0252，#1059）。名单原来只有 `ws.agents`，而云会话是**群聊**：成员的头像与署名时间线上一直画着（#971），唯独打 @ 的那一刻他们不存在——后果不只是少几行，`resolveSendMentions` 会把「一个候选都解析不出来」判成打错字**整句拦下**，于是 @ 一个真实存在的同事得到一句「没有叫「张三」的智能体」。**服务端零改动**：`resolveTargets` 按 agent id 过滤，人类 uid 放进去只会被静默丢掉，所以客户端只把 agent 那几个 id 填进 `mentions`，只 @ 人时发权威的 `[]` → 服务端 `targets.length === 0` 只落一条 `chat_message` 不起 turn（这推翻了 ADR-0228 那句「永远不会在正文写了 @ 时返回 `[]`」，新规则：有且只有「全点在人身上」这一种情形）。「认不认得」复用 `parseMentions` 不拿 `mentionTokens` 比字符串（token 贪婪吃到空白，`@Mingxuan Zhang` 只切出 `Mingxuan`，两套切词判据分家 = 弹层里点得中、发送时说不认识），且**排在 `fresh` 之后**——成员显示名来自 `profiles.name`，从没过 `agentNameConflict` 那套前缀检查，撞名时归 agent 接，与本地名单健康时 `parsed` 先命中的答案一致。撞名要**在列表上说出口**（`memberShadowedBy`：成员名以某个 agent 名开头就会被最长匹配抢走，中间那格改画「@ 会点到智能体「X」」）——不标就是点了写着「成员」的那行、回话的却是一只 agent，#722 的一般形式；同名成员补 uid 前缀。agent 排在成员前面（默认高亮落在会接话的那一族），**自己也在名单里**（摘掉就要维护第二份名单，而「认不认得」那份必须含自己）。右边那格标注**也搜得出来**（同 `friendMentionItems` 的纪律），头像 20px 不是气泡旁的 24px，高亮**不给 transition**（方向键连按，补间会拖在手指后面）。那一行**真渲染一遍**在 `tests/renderer/mentionOptionRow.test.tsx`（#1068）——纯逻辑那份钉的是每一格的值，钉不到「有没有被画出来」；桩要打在 `complete`/`naturalWidth` 上不是 `onload` 上（Radix 判加载成功读的是前两个，jsdom 里 `naturalWidth` 恒为 0，光派 load 事件仍然判成 error）。**@ 一个人会不会惊动他**——真通知在 ADR-0256（#1064）
- `src/shared/workspaceMentions.ts` / `services/runtime/src/mentionInbox.ts` / `supabase/migrations/0030_workspace_mentions.sql` — **被 @ 的人类成员真的收到提醒**（ADR-0256，#1064，协议 12→13；还的是 ADR-0252「代价」第一条）。一张 `workspace_mentions` 表，一行 = 一次点名，主键 `(uid, session_id, seq)`（daemon 重启补跑天然幂等，所以是 `upsert` 不是 `insert`——23505 走 throw 会把一次正常重放记成告警）。**它是日志的投影不是第二个事实源**：权威那份在 VPS 上而桌面够不着（要先开一条会话房才读得到 backlog），而「你不在的时候有人喊你」恰恰只发生在你没开着它的时候——角标必须在一条会话都没开时就画得出来。写方只有 runtime（service key），表上**不给 authenticated 的 insert / delete**：给 insert 就是让任何在籍成员替别人伪造一条「有人 @ 了你」，给 delete 则让「读过了」与「从没发生过」变成同一件事；读要**在籍**（退了群不该继续读得到群里的正文摘要，同 ADR-0199 的「踢人 = 关门」）。`from_label`/`excerpt` 是写入那一刻的**快照**不现查 profiles——与 `usage_event.agent_id` 记 id 不记名（ADR-0221）方向相反而理由对称：那边要「改名不断账」，这边要「当时的措辞不被改写」。`resolveTargets` **一个字不改**：它答的是「起几条 turn」（花钱的那一格），人类 uid 在那儿一直被静默过滤、也应该继续是；点到的人走 say 帧上另一格 `memberMentions`，**分开带**是因为合成一格再让服务端去分，等于要求服务端认得出哪个 id 是人。服务端不自己从正文里认（认人名要先有一份 `profiles.name` 名单，于是「@小红算不算点到小红」就有第二份判据，而客户端那份是用户**看得见**的，同 ADR-0220 坑 ④），但**不信客户端**——按 `hostUids()` 复核在籍 + 剔掉发言人自己（选人名单里仍然有自己，那份答「认不认得这个名字」，这里答「要不要惊动他」）。撞名归 agent 的判据是**同一个 `@` 的位置**（`parseMentionSpans`/`parseMemberMentions`）不是名字前缀：`memberShadowedBy` 只覆盖一个方向，反过来（agent「小红助手」+ 成员「小红」，正文 `@小红助手`）会给一个根本没被点到的人发提醒。送达三条路——realtime `postgres_changes` 第四条通道（**状态不并进好友那份健康度**：0030 没跑的那段时间它会一直报错，并进去等于让一次没跑的 migration 表现成「好友功能连不上」，ADR-0223 那条教训）、开机 + **窗口重新聚焦**时各拉一次（不做定时轮询——会看到角标的那一刻必然是人回到窗前）、`workspaceMentionNotification` 走 ADR-0027 那套聚焦判定与点击落点（带 Ping 音：被点名是「有人在等你」）。角标画组头 + 会话行两处，**组头那枚收不收起来都画**（旁边那个条数展开就数得出来所以只在收起时报，未读数展开也数不出来），色取 `--brand` 不取 `--warn`（被 @ 不是「出事了」，ADR-0240 已经让 Max 借走一次形）；`unreadMentionCounts` 在 section 那层算一次往下传——写进 zustand 的 selector 是一个真的死循环（每次都造新对象 → `useSyncExternalStore` → Maximum update depth exceeded）。进房 = 已读，本地先灭再发（等回执再灭会让断网时一条读过的会话永远顶着角标），`markMentionsRead` 的 `is("read_at", null)` 不是优化——没有它，重开一条早就读过的会话会把当初的 `read_at` 改写成此刻。已知代价六条在 ADR-0256，最要紧的三条：**只做桌面**（手机没有推送凭据那一层）、**agent 写的 @ 不通知人**（`relayAfterTurn` 不解析成员名字，那是另一个能力）、**realtime 与系统通知两跳一次都没在真机上跑过**
- `src/shared/agentToolAllow.ts` / `src/renderer/src/lib/agentToolsForm.ts` / `services/edge/src/usageAttribution.ts` / `src/renderer/src/lib/workspaceUsageView.ts` / `src/renderer/src/lib/cloudModelStatus.ts` — 工作区多智能体的连接器白名单与用量归因（ADR-0221，#941 切片 2 / #946 切片 3 / #945）。白名单两层都是「空 = 全给」（顶层 `[]` = 整池放行、条目 `tools: []` = 整台放行），与 `workspace_connectors` / `proxyShare.ts` 逐字同口径——同一个字面量在相邻两张表里意思相反是最贵的那种不一致，而这两张表并排住在同一个设置页里；匹配**只看 `serverId` 不看 `hostUid`**（界面上按 host 拆两行会让人以为能分开授权、实际做不到，何况贡献者换台机器 `hostUid` 就变，存量授权会因为对方重装 app 而静默失效），过滤落在 runtime 的 `cachedPxTools`。「一台都不给」在这个编码里**表达不了**，所以 `toolsDraftError` 不许存出「只用勾选的 + 零勾选」——存出去就是把用户的「都不要」翻译成「都要」；否决了引入 `null`/哨兵，那个状态本身没有真实需求（删了那只 agent 更诚实）。工具名只在**每个**贡献者都点了名时才列得出来（有人整台放行时那台此刻有哪些工具在 edge 的托管箱里、本机快照没有），只能整台勾；**已撤回连接器的残留勾选要画出来标「已撤回」不静默丢**——静默丢等于替用户把一份他没碰过的授权收窄了，不画则是 #722「撒谎的勾」的一般形式。用量那半：`usage_event.agent_id` 带 id 不带名（改名不断账，空串 = 未归因），周用量端点在 **edge 不做 Supabase RPC**（周窗起点要 owner 的订阅 + `weekStartFor`，那份逻辑只在 edge，且同一扇窗两个界面不能给出两个数，ADR-0209；桌面直连 usage_event 走不通——RLS 只让本人读，而窗是 **owner 的**，ADR-0217），在籍闸的四步编排抽成 `fetchWorkspaceUsage(get, …)` 这个注入 `get` 的纯函数、worker 只剩一行——`worker.ts` 不进 vitest，留在那边等于**这个端点唯一的权限判断零执行覆盖**；`not_found` 排在 `not_member` 前面（合成一个 404 的话，被踢出去的人看到的是「这个工作区没了」）。tokens 列 = `prompt + completion` 不加 `cached`（后者是前者的子集，`llmGateway.ts` 计价用 `Math.min(cached, prompt)`，加上就是重复计数）。#945 那格：`welcome`/`config_result` 带 `modelRoute`、协议 4→5，判据是 runtime 用**与 turn 同一份** `decideRuntimeRoute` 算好下发而不是桌面拿 `billing.me` 自判（那是**我的**订阅，工作区走 owner 的）；`route` 为 `null` = **runtime 探测本身抛错**，**不是 edge 抖了**（`hostedProbe` 把失败缓存成「没订阅」不抛，一次 edge 故障在这一格上表现为 `blocked`），所以文案不往「探不到/连不上」写。已知未修：整台放行的连接器挑不了工具、两个 host 同名 server 无法只授其一、用量表按 agent 不按会话、本地 `model_usage` 事件仍无 agentId、`route.kind` 无穷举检查
- `src/shared/workspaceMemory.ts` / `services/runtime/src/workspaceMemory.ts` / `services/runtime/src/workspaceMemoryTool.ts` / `src/renderer/src/lib/workspaceMemoryView.ts` — 工作区多智能体的记忆（ADR-0222，#949 切片 4）：一张 `workspace_memories` 表**一档一行**（`agent_id` 空串 = 工作区共享档，非空 = 那只 agent 的私有档；成员在籍即可写，对称 ADR-0198 决策③），条目切分/上限/原子批量复用 `src/shared/memoryStore.ts`。快照事件 `workspace_memory_loaded` **缺席或内容变了才落**、投影**最新一条胜出**：不是每 turn 都落（日志堆满同一段文字、前缀缓存每轮打穿），也不是只判「有没有」（别人在本会话中途改了共享档，我下一 turn 就看不见）——这是它与本机 `memory_loaded`「一会话一条」的根本差别，因为**共享档是多写者可变状态**；投影侧因此**不许 `+=`**，两条快照叠起来就是两个 SHARED 块摆在 system 里，模型读到新旧两套口径而不报任何错。两档都空时也落一条基线快照（事件本身是「这套装配有记忆」的凭据，同本机无条件渲染那条），压缩幸存靠 `modelContextScan` 多捞一类。云侧档位枚举 `"shared" | "own"` 是**自己的一份，不动 `MemoryTarget`**（那个类型手机端在用，收窄它把桌面四档一起打红），共用的只有抽出来的 `applyEntryOps`——ADR-0116 那条「超限且没变小才拒」的教训不能有第二份实现。共享档的写入者前缀 `[名字] ` 由**写入路径**拼、名字现取不定死（同 ADR-0202「每次 `chat()` 现读」），**桌面手改不拼**（人写的不是 agent 写的，伪造署名比缺前缀更糟）。读失败只 warn 不阻塞 turn；写互斥是**进程内**的 `withMemoryFileLock`（单 daemon 前提，多 daemon 那天要换 DB 级 `updated_at` 乐观锁）。设置页那半：单块保存**只 patch 那一行**（`replaceRow`，归一化与主进程落库前同一步纯函数），整份重拉会把别的块没保存的草稿悄悄冲掉，所以「刷新」在有草稿时先 `confirm`。**新事件类型的检查清单实际是十处**——上一条索引列的九处之外还有 `tests/session/persistencePolicy.test.ts` 里手写的 `DURABLE` 数组（`Exclude` 做类型级穷举，漏了 tsc 直接红）；ADR-0222 写的「十一处」里那第十一处 `src/renderer/src/lib/threadGroups.ts` 的 `isInvisible` 是死代码，已按 #929 删掉，`tests/renderer/timelineLists.test.ts` 从三份名单对表收成两份。已知代价：桌面手改**不落审计事件**（工作区没有「当前会话」可挂，`updated_at` 是唯一痕迹）、工具成功行不发 `formatMemoryResultLine`（它的类型绑着 `MemoryTarget`）所以云会话时间线上没有记忆 chip、手工冒烟一次都没跑
- `src/shared/agentRelay.ts` / `services/runtime/src/sessionService.ts` 的 `relayAfterTurn` — agent 互相 @ 接力（ADR-0223、刹车重排见 ADR-0238，#950 切片 5；**收窄了本文件开篇「明确不做多 agent 编排」那半句**——开的只有群聊接力这一条窄路，ADR-0047 决定 5「本机子 agent 不能再派子 agent」原样成立，两条路的三点差别在 ADR 决策 1 那张表）：**载体是两条事件不是一条**——`agent_relay`（群事实，`ignorable`，模型不可见，时间线画线与护栏/上限的判据都从它重放）+ 一条带 `relay` 的 `user_message`（起 turn 的载体），后者让 ADR-0220 的 `openTurns` 重启补跑、`unseenUserTail` 不重采样、渲染层「排队中 / 正在回复」全部免费拿到；造第二种起 turn 的机制才贵。开场白的 `fromUid` 是**点火的那个人**（不给 agent 发伪 uid，spec §4.2；代价：B 动手时用的是点火者的代理授权，不是「自己的」）。**三道刹车，且钱只能往下压不能往上抬**（ADR-0238 重排，#1017；`workspaces.relay_max_depth` 那个可配的分支闸已撤）：① `RELAY_MAX_HOPS_PER_IGNITION`（24）**无条件**，绝对天花板——三者里唯一与档位/型号/价目全都无关的量，owner 换个档不该改变群聊行为；② 预算闸：这次点火已花掉的钱 >= 所有者 5h 窗口**剩余**的一半（分母取剩余不取 limit，否则窗口用掉 95% 时拿到的预算与全空时一样、刹车对「快没额度了」完全无感，而窗口触底后 `hold()` 会退到用户买的加购桶）；③ 周期护栏两档：`minRepeats` 注一条话**不停**（ADR-0212），`RELAY_SPIN_STOP_REPEATS = 3` 硬停（同 engine 的 `loopGuardMaxNudges`，ADR-0225——ADR-0212「只注话不停」的前提是人就坐在屏幕前，云会话没有那个人；取 3 是因为周期 p 的链停在第 3p 棒，p≤8 时全部落在 24 之内，取 4 则 p=6 恰好与天花板同时命中、p≥7 永远轮不到）。参数 `RELAY_GUARD = {maxPeriod:8, minRepeats:2}`（#957 F2 修订自 3/2）——3 只 agent 全互 @ 时每轮 6 跳才闭合一个周期，maxPeriod 3 是永久盲区、护栏一次都不会喊。**降级 = 改动前逐字不变**：问不出剩余额度（探针把网络失败也缓存 60s）时补回 `DEFAULT_RELAY_MAX_DEPTH` 那道分支闸——不补的话，读不到钱的那条路反而比今天松，正是 ADR-0237 点名不许的方向。`spentMicro` 是**下界**（`relayStateSince` 一次扫描同时给出链与花费，两者共用同一个链首判据），少算只会让 ② 晚命中而 ① 无条件，所以下界是安全的。两句话都走 `chat_message`（`fromUid: "system"`）——群里所有人可见，`agentView` 里是 `keep` 所以正在打转的那两只自己读得到。**只有 `completed` 才接力**（`aborted` 是人按了停止），**归档后一律不接力**——接力是 turn 收口后唯一会自己长出新 turn 的路径，而 `drain` 不看 `archived`（ADR-0201）。扫描窗口按**这一轮起跑前的日志尾**（`scanFrom`，与 engine 的 `readUpToSeq` 同一个量）划界，**不是 `job.opening.seq`**：同一只 agent 排两个 job 时后者的开场白 seq 早于前者产出的 assistant_message，用它划界会把早被处理过的话重新扫进来——同一次 @ 落两条 hop，还把护栏诓成「打转」（回归用例在 `tests/runtime/sessionService.test.ts`）。时间线上**只画接力线不画开场白**（`cloudTimeline.ts` 的 `relayLineText` / `hiddenFromCloudTimeline`；那条正文是写给模型的措辞，两条都画是同一件事说两遍）。已知代价：hop 与开场白两次 append 不原子（孤儿 hop 只让护栏偏一格）、护栏命中后这条链每一棒继续喊（默认上限下最多 3 句）、同一轮 @ 了 N 只时 `spend` 是进循环前的一张快照不在循环里推进（钱的判据天生事后——这一棒还没跑，花多少不知道；兜住这次扇出的是 `chain.push(hop)` 让无条件的 `cap_hops` 逐个收紧）、Auto 的分类调用扣了钱但一条事件都不落所以 `spentMicro` 永远漏掉它（量级约 200 输入 token）、接力开场白在本机 `Timeline.tsx` 会画成匿名 user 气泡（#936 同族，今天走不到）。接力棒上的连接器要点火者批一次（`openingDepth > 0` 时 `requiresApproval: true`，人自己 @ 起的 turn 照旧不问）、depth 判据从日志重放推导（否决内存 `pendingDepth`）落在 ADR-0225（#957 一批）
- `src/shared/createAgentDraft.ts` / `services/runtime/src/createAgentTool.ts` / `services/runtime/src/agentRegistry.ts` — 管理员替用户建 agent（ADR-0224，#954 切片 6）：`create_agent` 工具**只挂在 `agentId === 'admin'` 那台 engine 上**（判据是稳定键不是名字），必过审批门（ADR-0118 同款），审批卡经 `approvalRouter.summarizeArgs` 钩子逐字段、**提示词全文不截断**（默认那 200 字 JSON 对它不够——截断的卡等于让人批一段没看见的提示词）；`created_by` = 点火的那个人（`currentInitiator`，spec §4.2），查不到就拒绝不伪造；参数校验**不 fail-open**（`tools` 形状不对抛错，不走 `normalizeAgentTools` 的「整份回 []」——`[]` 在这张表里是整池放行）；桌面刷新名册**不新增事件类型**，`cloudTimeline.createAgentLanded` 从 `tool_result{ok}` 反查配对的 `assistant_message.toolCalls`。已知代价：重名在审批之后才报、别的 agent 的花名册不因新成员重发（`briefIfNeeded` 只看 instructions）。migration 0025 给 `workspace_agents` 补的形状约束（agent_id/name 换行）与每工作区 32 只上限是这条能力的 DB 兜底，见 ADR-0225。第二批（ADR-0226，#957）加了 `argsFields`：`createAgentApprovalFields` 派生出的五个字段（名字/职责/型号/连接器/提示词，提示词全文不截断）供审批卡逐字段渲染，与整块字符串 `argsSummary` 并存（旧客户端/旧日志读后者，不是二选一）。
- `src/shared/promptSafe.ts` / `src/shared/createAgentDraft.ts` / `src/shared/workspaceAgents.ts` — 工作区多智能体的发言人身份与字段防伪是两道独立的闸（ADR-0226，#957 第二批）：**写入时校验**（`createAgentDraft` 的 `noNewline`/`collapseWhitespace`、`workspaceAgents` 的 `validateAgentName` 三条新规则）挡的是"新建/改名这一刻能不能落库"；**投影时的结构性转义**（`promptSafe`：折叠空白 + `]` → `］`）挡的是"旧日志里已经躺着的字段、或 `profiles.name` 这类从没走过写入校验的字段，拼进 `[label]: text` 这类结构时还能不能撑破结构"——后者不是前者的替代，是没有前者时唯一还剩的兜底，两道各自独立成立、缺一不可。发言人标签（`safeSpeakerLabel`）在此之上加了第三层：保留名「系统」只有 `uid === "system"` 才用得了，别人撞上就退回 uid 前 8 位；这一层要在 `daemon.labelOf` / `sessionService.say`+`logChat` / `deriveMessages` 投影三处各自幂等地跑一遍——不幂等就会因为跑了几次而给出不同的名字。拼名字进结构的地方**不止发言人前缀**——共享记忆条目的写入者前缀（`workspaceMemory.withWriterPrefix`）与 OWN 记忆块头（`deriveMessages.renderWorkspaceMemoryPrompt`）也各过一次 `promptSafe`：判据是"这段字面量靠 `[` `]` `「」` 撑起结构吗"，是就得过。旁边那条"这条 `user_message` 是不是 engine 自己注的旁白"的判据在 `src/renderer/src/lib/systemNote.ts` 的 `isSystemNote`/`systemNoteBody`——**云与本机两条时间线共用这一份**（各写一份迟早分家），且它**不是** `isAuditEvent`（后者答的是"上不上时间线"，是另一个问题）。正文那一半（`\n[系统]: …` 伪造说话人行）由第五批的 `promptSafeBody` 在投影时补上——只碰换行之后行首的 `[`，第一行不碰是天花板（ADR-0230，#965）
- `src/main/memoryFiles.ts` / `src/main/memorySync.ts` / `src/shared/memoryReconcile.ts` / `supabase/migrations/0019_memory_docs.sql` — 记忆跟账号走（ADR-0207，#852）：`memoryFiles` 是 accountConfig 下 `memories/` 的**唯一**读写口（架构断言：碰记忆路径符号的文件不许 import node:fs），写完回调汇进 `memorySync`（工具那条路经 `LocalWorld.onConfigWrite`）；对账是纯函数 `planReconcile`（后写胜、内容相同不动）；触发点只有本地写 + 登录恢复，不轮询；从云端写本地时 `muted`，否则死循环。项目档跨机那条天花板已由 ADR-0211 收掉
- `src/shared/memoryTopics.ts` / `src/main/memoryTopics.ts` / `src/main/sessionTopic.ts` — 记忆第四档 TOPIC（主题桶）+ 会话主题分类：slug/种子/索引的纯逻辑、主进程读桶目录、合并调用的任务四（ADR-0204，#846）
- `src/shared/toolLoopGuard.ts` / `src/loop/engine.ts` 的 `loopFingerprints` — 退化循环护栏（ADR-0212，#891）：判据是**周期重复**不是「连续相同」——真机那条轨迹里相邻两圈从来不相等（同一组 14 条命令循环了 14 遍、190 次调用、两小时零进展、零信号），只认 `last === prev` 的护栏会全程沉默。命中**注一条话不停 turn**（ADR-0006 的无步数天花板原样成立），载体沿用 `user_message` + `origin`（`"loop_guard"`，同 ADR-0205 那条注入路径，所以不新增事件类型）。话注在这一圈 `tool_result` 全部落盘之后——插在 `assistant(tool_calls)` 与答复之间投影出来就是「有 tool_call 没答复」（ADR-0005 的教训）。喊完清空历史，判据作用域是一个 turn
- `src/main/backgroundTasks.ts` / `src/tools/waitTask.ts` / `src/loop/engine.ts` 的 `appendBackground` — 后台任务结果怎么进对话（ADR-0205，#871，修订 ADR-0088）：turn 在跑就**当场尾部追加**（纯 append，前缀不变；0088 拿 ADR-0073 反对的是中段重写，不是这个形状），idle 才另开一轮；采样窗口里到的攒到那条 assistant_message 落盘之后再落，否则投影像模型已经答过它没看见的东西；模型这步没要工具时 engine 多看一眼日志尾——有没答的用户消息就再采样一圈。`wait_task` 让模型在同一 turn 里等（Claude Code TaskOutput 对照），等到的走 tool_result 回、完成回调带 `claimed` 不再追加。重试回注消息走 `resendMessage(seq)`：身份由主进程从日志取，`sendMessage` 入口继续不透传 background
- `src/renderer/src/lib/messageQueue.ts` / `src/renderer/src/components/elements/message-queue.tsx` / `App.tsx` 的 `QueuePanel` — turn 跑着时敲的回车 = **排队**（ADR-0247，#1048，推翻 #344 的插话 steer——那条链路整链拆除了，引擎里没有第二种把话送进 turn 的入口）：队列是渲染层状态（"待发出的意图"不落事件日志，发出去那一刻才照常落 user_message），turn 变 idle 时 `drainQueue` 自动发队首（中断/暴死也走到这条，排着的活不会卡死），发失败回队首不丢话。每条排队项的「顶替」= `promoteTask` 提到队首 + `stopTurn` 中止当前 turn，收口后由同一个 drain 发出——纯渲染层编排两条现成机制，主进程零新接口；竞态良性（turn 恰好自己收口时 stopTurn 是 no-op，该条仍在队首下一轮第一个发）。`deriveMessages` 的中途落消息顺序修复**不随 steer 拆**——后台任务回注照样会在工具组开着时落 user_message
- `services/runtime/src/sessionService.ts` 的补跑队列规则 / `turnBoundary` / `SayRejectedError` + `budget` / `src/renderer/src/lib/cloudTimeline.ts` 的 `stopButtonRows` / `agentMentionInput.ts` 的 `resolveSendMentions` / `src/shared/shellBridge.ts` 的 `CloudAck` / `systemNote.ts` 的 `systemNoteDetail` — 工作区多智能体第二轮复审回流（ADR-0228，#957 第四批）：判据一律挂在**自己算得出的事实**上，不挂在别人递过来的字段或另一格状态的巧合上。**补跑收口按每只 agent 的 seq 队列落"连续前缀"**（队头起连续的 kicked/exhausted 各落一条 `readUpToSeq=自己的 seq`，撞上第一条 runnable/unknown/skipped 就停手，记号退到队头 seq−1）——原来"按最早那条 runnable 划界"会把排在它前面的 `unknown` 一起静默关掉，而那是"这一刻问不出来"不是"确认不在籍"；被踢的开场白另落一条**模型可见**的 `chat_message{fromUid:"system"}`「不作数」，因为日志 append-only、正文删不掉，追一句是这条修法的天花板（在 `agentView` 按在籍过滤那条被否了）。`abortCurrent` 里 `stopRequested` **无条件**置位：engine 只在每圈开头查一次信号，一条**没有工具调用**的回复直接 `completed`，"按了停止"就此蒸发、接力照点火。`stop{seq}` 与 `turnBoundary`（起跑那刻的日志尾）比对回 `not_current`，界面那半是 `stopButtonRows`（每只 agent 只画 **seq 最小**那行 running；不用"遍历首见"——那是把正确性押在 `openTurns` 恰好按 seq 升序上，前提一变不会红只会把钮画错行）。**限速下沉到 `say()` 的 `resolveTargets` 之后按真实点名数扣**（`budget` 回调 + `SayRejectedError`，拒绝走 `say_result` 不进 `deps.log`）：原来按客户端自报的 `mentions` 长度扣，**省掉那个字段就是一个 say 令牌换 N 条 turn**；超容量**拒绝不夹价**（夹价 = 第 11 只起免费）、名单降级且点了名一律拒收不落盘（降级时说「没找到智能体 X」是确凿的假话）。`promptSafe` 扩到五个替身（`]（）「」`），**半角 `()` 与 `[` 故意不转**，于是有一条纪律：新写模板时围着成员可写字段的分隔符不许用半角 `()`、不许把 `[` 当闭合符。`CloudAck = {ok:true} | {ok:false; message; unknown?:true}` 贯穿主进程→store→渲染层：15 秒 ACK 超时是**"不知道"**不是"没发出去"，`unknown` 时清输入框、另画一行绑了 sessionId 的「重新发送」（把原文种回输入框 = 用户再按一次回车 = 同一句话发两遍、两笔都记在 owner 账上）；三个 store 动作从此不碰 `workspaceGroupsError`；审批卡的两条启发式兜底删掉（前提是协议 6 的 `approve_result` 已是权威，那两条在 `ok===true` 分支上只可能说假话）。`systemNoteDetail` 让后台任务旁白带首行 + exit code、正文 `<details>` 可展开。`resolveSendMentions` 把「名单读不出来」与「你名字打错了」分成两句话说——旧判据在正文写了 @ 却一个都没解析出来时**发一个权威的空数组**，那句话点谁都没点上而界面完全无声
- `src/shared/workspaceAgents.ts` 的 `SandboxApproval` / `services/runtime/src/sessionService.ts` 的 `policyApprover` / `agentRelay.ts` 的 `RELAY_MAX_HOPS_PER_IGNITION` / `briefIfNeeded` 的三样判据 — 全量复审选做的三条（ADR-0231，#977）。**云端每一次 `bash`/`write_file` 都要人批**是这套东西真机跑不起来的第一道墙：`approvalGate` 只看 `requiresApproval`，runtime 没有桌面那套 `approvalMode`，串行 `drain` + 10 分钟超时 = 一只 agent 跑 10 条命令冻群 10 次，而容器本来就是隔离面（ADR-0199/0200）。修成**工作区一格** `workspaces.sandbox_approval`（0026，默认 `ask` = 今天的行为一字不变；**控件已搬进云会话输入框那一行，见 ADR-0243**），runtime 用**包在 router 外面的 approver** 放行沙箱那两把刀（判据按工具身份不按名字；放行也落 `approval_decision` 带 reason；每个 job 第一次撞门时现查、这一轮复用；查不到回落 `ask`），连接器与 `create_agent` 照批；桌面读那一列走**单独一条容错查询**（ADR-0223 部署顺序那条教训）。`briefIfNeeded` 原来只比对 `instructions`，别人新建/改名/改职责对这只永远不可见——现在 name + roster 指纹（按名字排序）一起比。`relay_max_depth` 封的是分支不是总量（最坏 `N^depth`，自带 key 路无额度兜底），`decideRelay` 加一道 `relayChain.length >= 24` 的总量闸，排在分支闸之后。**ADR-0238 保留了这道闸并把它升成唯一的绝对天花板**（回执改名 `cap_total` → `cap_hops`）：分支闸退成降级专用、预算闸只可能更早命中，于是这个 24 成了「一次点火最多长出几条 turn」的唯一硬答案——它必须与档位/型号/价目全都无关，那三样一变就是同一群人在同一个工作区里看到不同的群聊行为
- `services/runtime/src/workspaceLock.ts` / `ttlCache.ts` / `sessionService.ts` 的 `gateContainer` + `grantedFor` / `hostedRoute.ts` 的 `requestedModels` — 复审选做的第二批（ADR-0232，#979）。**同工作区多条会话共用一容器一卷、零互斥**：锁按工作区一把、按会话 FIFO 排队（与桌面 ADR-0152「只拒绝」故意不同——云端所有会话在同一个 daemon 进程里，`try/finally` 就是全部失败模式，而排队制的 turn 本来就有「排队中」那盏灯）、**第一次碰容器才拿**（只聊天的 turn 不排队）、这一轮收口才放、等待可被停止键打断（engine 的信号打不到一次还没起 exec 的等待）、排队时群里说一声。**每 turn 起模型前的五次网络**收成 60s 内一次（记忆那次故意不缓存——共享档是多写者可变状态）：agent 名单走 `ttlCache`（`say()` 递 `{fresh:true}` 强刷、`create_agent` 落库 invalidate、接力链内复用），成员名单从 `membershipCache.members()` 读（与在籍判断本来就是同一条 SQL），授权拉取按（发起人，成员名单）记 60s 快照（撤销不受影响：edge 的 `pxGate` 每次调用都重判）。**型号白名单改成优先级链**：`requestedModels` 按顺序取网关供着的第一个，链是 agent 白名单 → 工作区配的那款 → 网关第一款；只喂 hosted 分支（#957 D2 原样成立），spec §3 的「会话级选择」这次不做，表单文案改成说实话
- `services/runtime/src/hostedRoute.ts` 的 `decideRuntimeRoute`（两态）/ `src/shared/remote/cloudSession.ts` 协议 7 / `src/renderer/src/lib/cloudModelStatus.ts` — **云会话不可以自带 key，统一走所有者订阅额度**（ADR-0233，#981；推翻 ADR-0202 与 ADR-0203 D4 的改道）。维护者定的产品口径；技术上自带 key 那条路存在一天，「额度用完悄悄改烧所有者自己的 key」这个静默失败模式就存在一天。路由只剩 hosted / blocked：所有者没订阅、探不到、额度用完三种 blocked 各说各的话（措辞分开——一次 edge 抖动被写成「你没订阅」，人会去点续费解决一个不存在的问题）；额度用完不改道，网关给的 `resetAt` 记进会话级 `RouteMemo`，窗口内的 turn 一次网关都不打；`route_changed` 事件类型留在 schema 给旧日志重放、不再落。`config` 帧去掉 `model`、welcome/config_result 去掉 `model` 一格、`CsModelRoute` 去掉 `workspace`——**减字段也进位**（7），已装桌面是协议 4 本来就连不上。daemon 的 `workspaceConfigStore.loadAll` 读到存量记录里的 `model` 当场剥掉回写（别人的凭据，没用了不该继续躺在 VPS 上）。桌面「云会话配置」卡只剩仓库；头部模型那一格只读路由——**#1052 又把它收窄成「只在起不了 turn 的时候出现」**（ADR-0246）：`hosted` 与「探不到」两态一格都不画，理由是三分之二的时间它在用一个常驻标签说「正常」，而那句 `型号 · 托管` 还不保真（探测递空白名单 = 工作区默认款，真跑一轮按 agent 白名单/Auto 现取，两者可以不是同一款）；留下的 `blocked` 是唯一会挡住干活、且有出路的那句。同一判据落在两个消费方（头部 + 设置页「文件」tab，#1056 之后它挪到那一页顶部整句写，见 ADR-0251 决策 9）
- `services/runtime/src/frameHandler.ts` 的 `applyConfig` / `src/main/cloudSessionClient.ts` 的 `ctlRequest` / `src/renderer/src/components/WorkspaceFilesTab.tsx` — **仓库配置从云会话头部搬进工作区设置**（ADR-0234，#991，协议 7→8；**这一页 #1056 重新定义成「文件」，见下一条**）。判据是**这个东西属于谁**：仓库是工作区的属性（同 ADR-0202 的模型 key、ADR-0231 的 `sandbox_approval`），不是某一条会话的——挂在会话房的 `config` 帧上，等于给一个工作区级设置强加「先开一条这个工作区的云会话」这个前置条件，而它在界面上的样子就是「设置项藏在会话头部 + 常驻一格「未配仓库」」。`config` 帧搬进控制房并带 `workspaceId`，新增 `workspace`/`workspace_state` 一对读帧，会话房里出现这两种帧一律 `denied not_authorized`（同 `create`）。桌面三条控制房 RPC（create / workspace / config）共用 `ctlRequest` 一副骨架，#829「没发出去当场失败不白等超时」与终审 C1「JWT 只发第一个 host 通告」各留一份、不再各写一遍；会话房那套 `pendingConfig` 挂起态（含三条终态收口）整个删掉——控制房每次开一条新连接，连接自己的 `onGone`/`onClose` 就是收口。**「不用仓库」是正当状态不是没配完**：文案从「还没有配仓库」改成「没有配仓库……不是每个工作区都需要仓库」（做文案、运营的活在空目录里干）。`openWorkspaceId` 从 `AppSidebar` 本地 state 搬进 store——侧栏 ⚙ 与云会话头部那颗「设置」打开同一扇抽屉，两个消费方不在同一棵子树上
- `src/shared/remote/workPath.ts` / `services/runtime/src/workFiles.ts` / `src/renderer/src/components/WorkspaceFilesTab.tsx` — **工作区设置页那一 tab 从「仓库」重定义成「文件」**（ADR-0251，#1056，协议 10→11）。维护者对着一个奶茶店的工作区问「这个页面怎么定义？并不是所有工作区都是为了 GitHub 代码仓库而做的，也不是所有用户都是程序员」——病在**把唯一实现出来的那种来源当成了页面本身的身份**：真正的主语是「水獭在哪儿干活」，每个工作区都有一个共用工作文件夹（一容器一卷，ADR-0232），Git 仓库只是往里装东西的一种方式。ADR-0234 已经补过一次文案（「不用仓库是正当状态」），补的是文案不是框架，于是 tab 仍叫「仓库」（非程序员读到的第一个词就跟自己无关）、正文第一句仍在解释「你可能用不到这一页」（一页要先说服你它可能与你无关，就是定义错了）、空目录仍写成「未配仓库」（正当状态被写成缺一格配置）。**「列得出内容」是改名成立的前提不是加分项**：一个叫「文件」却一个文件都列不出来的页面是 #722 撒谎的勾的近亲，所以连着做了 `files`/`files_result` 读帧（控制房，任何在籍成员都能读——卷是整个工作区共用的）。`CsWorkNode` 里两条分界是要紧的：**`absent`（容器还没建起来）与空目录是两回事**（合成一句就会对一个刚建群的人说「你的文件夹是空的」，而那个文件夹此刻并不存在），`binary` 单列一档（「读不出人话」不是失败，那是一张图）。**两道路径闸缺一不可**：`normalizeWorkPath` 挡「客户端要去哪」（`..` 与绝对路径一律拒，**不解释成上跳一级**——解释它意味着两条不同输入映射到同一个地方，那个映射里的任何差错都是一次容器内越界读，而面包屑本来就有完整段列表），容器里的 `realpath` 挡「解析完软链之后真的落在哪」（一条指向 /etc 的软链过得了第一道）。`sandbox.readWork` **刻意不走 `ensure()`**——那条路会建容器、跑 clone、重置 idle 计时，而翻一眼文件是个读动作，不该让「打开设置页」触发一次可能十分钟的 clone；停着的容器起一下并照样 `markActive`（不打点它反而永远没人扫）。`workFiles.ts` 只管一段 shell 脚本和它输出的解析、一个 docker 的字都不碰，这样真正会坏的那三件事（字段顺序 / 分隔符 / 截断了没有）进得了单测：记录间用 NUL 分、字段间用 TAB 分而**名字放最后**（文件名里换行和制表符都合法），被 `head -c` 砍掉半截的尾记录整条丢掉并置 truncated。新增 `FILES_BUCKET`——这是唯一一条会 docker exec 进容器的读帧，没有闸就是「点得够快就能一直起 exec」；`readWork` 写成必需的 dep，忘接线该编译不过。**「读不到」不许说成「里面是空的」**（同 ADR-0243）落在三处：runtime 回一句人话原文只进日志、桌面拿到 `ok:true` 却没有 node 当失败不兜底成空目录、渲染层读失败时上一份清单留在原地错误另起一行。已知代价八条在 ADR-0251，最要紧的两条是**看得见下载不了**与**不能上传**（中继单帧 256 KiB 上限，传文件要分片或另开通道，是独立的大件）。**#1066 把这一页做成了真正的文件区**（ADR-0253，协议 11→12）：照右侧栏那个 `FilesView` 的样子——树（一次只列一层，展开才发帧，折叠不清缓存）+ 过滤框（直接输入 = 按文件名，`?文本` = 内容搜索）+ 高亮预览，图标 / `previewLang` / `rehypeCodeLines` 都是**同一批模块**，搜索那一半连判据都是同一份纯函数（runtime 直接 import `src/shared/files.ts` 的 `parseRgJson`/`matchesFilter`/`classifyRgError`——容器镜像里有 ripgrep 13 且 `-w /work` 下输出与本机逐字同形，**这是 ssh 上真机验出来的，不是推出来的**，ADR-0134）。`CsWorkHit` 与本机 `FileHit` 结构相同但各是各的类型：共用的是判断不是类型，收窄后者会把本机那条路一起打红。**退出码必须单独带回来**——`rg` 的输出要过 `head -c`，管道之后 `$?` 是 head 的，不带的话「rg 没装」(127) 与「没有匹配」(1) 产生的观测一模一样，于是「rg 挂了」被说成「仓里没有」；`rc===0` 在分类前先择出去（`classifyRgError` 收的是抛出来的错误，成功那一路它没见过）。搬不过来的三样说清楚了：打开方式 / 在访达中显示（文件在 VPS 的容器里）、@引用到输入框（这一页没有 composer）、正文里「文件:行号」跳过来（云会话时间线没接）。**顺手修掉 #1056 带进来的一个真 bug**：脚本拼在普通模板串里时 `\0` 是一个**真的 NUL 字节**，而 execve 的参数在 NUL 处截断——`find -printf '…%f\0'` 丢掉分隔符（每个目录都显示成空的）、`tr -d '\0'` 变成 `tr -d ''`（二进制永远判不出来），两处都不报错且单测因为不看脚本字节而全绿。脚本一律走 `String.raw`，保鲜期是 `tests/runtime/workFilesScript.test.ts` 的一条断言：built 出来的脚本里除换行外不许有裸控制字符，**判据是脚本的字节不是「调了哪个函数」**
- `src/renderer/src/lib/cloudTimeline.ts` 的 `hiddenFromCloudTimeline` / `CloudSessionPage` 的 `SpeakerRow` / `WorkspacesSidebarSection` 的 ⋮ 菜单 — **云会话界面对齐本地会话**（ADR-0235，#993，协议 8→9）。维护者一口气点的五处是同一句话的分身：云会话是一条会话，它该长得像一条会话——ADR-0218 立的「一个开法」此前只兑现到「怎么开」，没兑现到「开完之后长什么样」。① 头部 `shrink-0` 钉顶（与 #987 钉底的输入框对称）；② 去掉 `w-[min(760px,92%)]` 居中量尺改占满 + 三段各自 `px-4`——本地那条 `max-w-(--thread-max-width)` 里的变量**本仓从没定义过**，所以本地一直就是「占满 + px-4」；③ `hiddenFromCloudTimeline` 从一条扩到五类，判据是**这条事件说的是群里发生的事还是机器的内务**（`session_created` / `agent_briefed`「就位」/ `request_envelope` / **批准了的** `approval_decision` 四类藏起来，**拒绝仍然画**——它中断了流程，是对话事实不是噪音；与 ADR-0230 让云会话「说人话」是同一条线上的两步；**本地时间线后来照这条判据一口气撤了四格**——ADR-0260 / #1091：`request_envelope` 与 `session_created` 两条事件 + 页脚那格 `cost` + 头部那串 `s-2026…`。同一个问题在两条时间线上给两个答案不是「本地更详细」，是没人回来重判过，而对着一格数字「内务」不好用，所以判据磨成**用户能不能据此行动**：换模型能、换分支能、`session_shared` 能，「工具从 16 把变成 17 把」不能，「会话已创建」四个字不能（它必然是每条会话的第一条事件，于是每条会话的第一眼都是它）。**撤的是投影不是事实**——落盘/去重/隐私闸（`PRIVACY_VERDICTS` 里 `request_envelope` 是 `strip`）一个字不动，`session_created` 更是投影的原料（三个字段各决定 system 提示词多注入哪一段），调参的人去轨迹视图。`isAuditEvent` 那侧写的是显式 `return false` 不是删 case 落 `default`（落 default 是同一个答案，但那是「碰巧对」），另有断言钉住这个**决定**——两份名单对表那条只管两处一致，两处一起加回来它照样绿。**页脚那格钱**：判据是**这条消息自己的 `route`** 不是「此刻订没订阅」（页脚报的是已经发生的事，而订阅是此刻的状态——同 ADR-0254 的 `showsCost`），`hosted` 不出这一格，因为 `costUsd` 查的是厂商按量价、而订阅用户付的是月费，写出来就是编一个他没花过的数（ADR-0241）；**不给替身**（不换算成额度百分比、不改写成 credit），混着两条路的 turn 整段不出（同 ADR-0211「有一条算不出整段就算不出」，残数看着像全部花销）；顺手删了 `timingStats`——`src/` 零调用的旧版，留着这次修法就有两份、其中一份看起来还活着（同 ADR-0249/0250）。**头部那串 id** 是一条悄悄失效的规则：那句注释写着「Default 是系统内部名字不显示」，判据却是 `workspace !== builtinWorkspace`，而 ADR-0206 之后任务会话拿的是 `<Default>/<sessionId>/` **子目录**——等号判据从那天起就漏掉了它们，失败模式安静（不报错，只多写一串 id）；换成 `isDefaultWorkspace`，分隔点搬进条件里面（同 `BranchPicker` 的 `leadingSep`，否则头部留一个孤零零的「·」）。**同一条 issue 还修掉了上下文浮层那半为什么没跟着 ADR-0254 消失**：判据没错（`showsCost` 问的是「日志里有没有一笔走自己的 key」），病在**记账**——`deriveUsage.billed()` 里写着 `e.type === "assistant_message" ? (e.route ?? "direct") : "direct"`，依据是旁边那句「外挂小调用只走用户自己的 key」，而 ADR-0248 让订阅用户的小模型改走托管之后那句话就成了假的：`helperHostedRoute()` 真的打了托管网关，落盘那几条却一格 `route` 都没有，于是①`showsCost` 判 true 整段冒出来②按价目表标一个用户没花过的钱数（ADR-0241）；`context_compacted` 更刺眼——#1017 给它补了 `creditCostMicro` 却没补 `route`，一条**自带 credit 的账**被判成 direct 再按 $ 计价。修法是给六类计费事件（`context_compacted`/`section_classified`/`suggestions_generated`/`micro_compacted`/`session_autotitled`/`session_topic_assigned`）各加一格可选 `route`，`billed()` 从此不分事件类型一律 `e.route ?? "direct"`；**`route` 跟着 `usage` 走不单独挂**（annotate 一次调用落四条事件、账只挂先落那条，分开挂就成了四笔），**缺席仍按 direct 不加 `unknown` 一档**（旧日志里它们确实是 direct）。断言要**从事件日志推一遍**——`CostPanel.test.tsx` 原来喂的是造好的 `ModelUsage`，对这个 bug 结构性失明，而出错的正是「事件 → 行」那一步。代价八条在 ADR-0260，最要紧的四条：订阅用户页脚从此三格（「这轮烧了多少额度」在页脚上没有答案）、任务会话头部只剩会话名于是**「打包为项目」之前看不到产出在哪**、**已经写下的那些外挂调用永远是 direct**（append-only 补不回去，所以旧会话的浮层里那一段还在）、代读员（`image_described`）压根没有 `usage` 字段、**每条带图消息跑的那次视觉模型从来没进过账**）；④ 标签行一视同仁只剩「名字 · 时间」、「→ 发给谁」整条去掉（**ADR-0236 收窄了这一条**：当时连着把 `SpeakerRow` 的 `mine` 参数一起删了，而维护者要的只是标签行那一层——排布上「自己发的靠右、其他群成员跟 agent 一样靠左」#995 第 2 条已经把 `mine` 加了回来，判据是 `fromUid === selfUid` 这个稳定键；改这里之前先分清动的是标签行还是排布）；⑤ 归档搬进侧栏那条会话行的 ⋮，`archive` 帧跟着 `config`（ADR-0234）走同一条路进控制房（带 workspaceId + sessionId，新增 `archive_result` 回执——控制房没有 `session_archived` 广播可当回执），判据与 ADR-0234 逐字相同：**归档一条会话不该以「你此刻正开着它」为前提**。**菜单里没有「删除」**：0016 迁移把 `wss_delete_publisher` 钉死在 `kind='package'`，云会话故意删不掉（删了 VPS 上那份权威日志就成孤儿），给一颗点了必然失败的钮是撒谎。**滚动区底部留白是算出来的不是「留口气」**（ADR-0236 第 1 条，#995）：页脚顶上那道渐隐绝对定位在滚动区最后 40px 之上（`-top-10 h-10`），而底部内边距只有 12px，末尾 28px 的正文——含未决审批卡、错误行、「不知道有没有发出去」那一行——永远蒙在暗底下；改成 `pb-14` 抄本地那把尺（`thread.tsx` 的 `mb-14`，且运行指示条另外 `sticky z-10` 骑在渐隐之上）。这笔账**故意没写成断言**：两个 Tailwind 字面量之间的算术，扫源码的正则会在任何无害重构上翻红，保鲜期在紧挨类名那句写着「为什么是 14 不是 3」的注释里
- `src/renderer/src/lib/chatBubbles.ts` / `CloudSessionPage.tsx` 的 `AgentBubbles` / `src/session/deriveMessages.ts` 的 `PLAIN_TALK` — **云会话的回复像人说话**（ADR-0266，#1132）。维护者原话「缺乏活人感，噼里啪啦一大堆专业术语……如果消息过长，可以分段输出」，真机形态是 `**3. 小标题**`（云气泡是纯文字，星号原样露出来）+ 长清单 + 五百字一口气。#989 那段只挡住了 otto-* 围栏，没说「怎么像人」。提示词改成四句各对一条病（像同事在群里聊天先说结论 / 别一次倒细节 / **空行 = 下一条消息** / 气泡是纯文字别用 Markdown 骨架），界面把一条回复按空行拆成几张气泡（代码围栏里的空行不算；终态与流式预览共用一份，每帧重拆、答案落下来时张数不变）。**日志一个字不动，拆是投影**；提示词与界面说的是同一句话、两边各有一半，缺一边都不成立，所以同一个 PR 各钉一条断言。否决了分隔记号 / runtime 拆多条 / 渲染 Markdown / 拆人类消息 / 本地跟着改，理由在 ADR。提示词在 runtime 要部署、渲染层要发版
- `src/renderer/src/components/elements/typing-indicator.tsx` / `CloudSessionPage.tsx` 的 `PendingTurnLines` / `cloudTimeline.ts` 的 `isAgentStep` — **云会话里 agent 干活的痕迹只剩一枚输入指示器**（ADR-0250，#1055）。ADR-0229 把中间步骤折成一行「「管理员」处理过程 · 1 步 · 1 次工具调用」，#1055 连那一行也撤了：那句摘要报的是**干活的量**（几步、几次调用），读者既不能据此判断进度也不能据此做任何事。ADR-0229 当初**否决过**「连折叠行都不画」，理由是「即使模型这一轮一个字没说，用户也能看见它干了什么」——那条纪律的前提是**没有别的东西在说「它在忙」**，输入指示器补上这一格之后前提不再成立，所以否决翻得掉。判据**合进 `hiddenFromCloudTimeline` 第 ⑥ 条**（原来渲染循环要连问两道，同一个问题两份判据迟早分家），`foldAgentSteps` 那台状态机随之删掉——它的全部存在理由就是给折叠头算摘要，`hidden` 那格恰好等于 `isAgentStep` 这个纯谓词；`AssistantMessageRow` 的 `hasText` 与 `toolCalls.map` 两段变成**由构造保证**跑不到的死支，一起删（同 ADR-0214 的纪律）。指示器长在**回复要落的位置**上、气泡与 `AssistantMessageRow` 那张逐字同款（`muted` / align start）——答案到了是同一张气泡里点变成字，不是一个东西消失另一个出现；**`queued` 故意不画**（一个 token 都还没跑，画上去就是 #722 那个撒谎的勾），照旧一行小灰字，两者的分别因此是结构性的而不是「跳的点 vs 不跳的点」。registry 抄件唯一的非机械改动是 `aria-label` 开放成 `label`：群聊同一刻可能有好几只在打字，念四遍 "Assistant is typing" 等于没说。同一条 issue 还撤了 `relayAfterTurn` 里「@ 了 N 个名单里没有的名字」那条系统旁白——与 `say()` 那条几乎同字的孪生兄弟只差一个词：**谁写的那个 @**。模型写的那个没人能据此行动（外部表现与它压根没 @ 任何人同形），人写的那个是他唯一的信号，所以前者删后者留；围着前者搭的那套栏杆（只回显数量、降级名单不说话，#957 E2-3）跟着没了——那是为「一只 agent 能以系统的名义对全场说话」这个面搭的，面没了栏杆也不用留，三条用例改成守更强的那句「一条署名系统的发言都不落」。已知代价：工具调用在云会话时间线上**没有任何痕迹**（只跑工具、一个字没说且顺利收口的一轮，收口后什么都不留），接力 @ 空了完全无声，且后半落在 `services/runtime/` **要重新部署 VPS 才生效**（#791）
- `src/shared/remote/cloudSession.ts` 的 `delete` / `delete_result` / `services/runtime/src/daemon.ts` 的 `remove` + `retireRoom` — **云会话删得掉了**（ADR-0245，#1044，协议 9→10）。ADR-0235 当初写「云会话故意删不掉」，理由是 0016 迁移把 `wss_delete_publisher` 钉死在 `kind='package'`——那条前提**只对客户端直连 Supabase 成立**，runtime 拿的是 service key，`EventStore.purge()` 也早就在（本机那颗「彻底删除」用的就是它）。判据与归档逐字同一条（owner 或建的人；两颗钮并排住在同一个菜单里，判据不同就要另有一套没地方讲的解释，而云端的归档本来也没有回头路），入口两个（侧栏 ⋮ + 工作区设置页「已归档的云会话」那一节——**最想删的那批恰恰不在侧栏里**，所以 `sessions.get()` 不能当判据，另加一条从台账现查 `publisher_uid` 的 `creatorOf`）。runtime 那三步顺序各有一个坏结局挡在后面：**先走一遍归档**（`session_archived` 是房里的人唯一收得到的信号，删除本身没有任何广播可当回执）、**等排空才 purge**（purge 不是锁，engine 还在收口时抹表会凭空长出半条会话）、**先删台账那行再 purge 日志**（反过来时删库失败留下「台账里有、日志空了」的会话，daemon 重启照样开房间，而没有 `session_created` 的日志一条 system 消息都投不出来）；purge 抛了也算删成功（fork 保护会抛，#352——台账那行已经没了，回 false 只会让人再点一次撞上「不存在」）。`creatorOf` **查询失败往上抛不兜底成 null**：说成「这条会话不存在」会让人以为已经删干净了（同 ADR-0243 的三态纪律）。收摊那一段抽成 `retireRoom` 归档与删除共用，归档照旧不 await 它
- `src/renderer/src/components/ui/nav-stack.tsx` / `ui/inset-list.tsx` / `lib/spring.ts` / `WorkspacePage.tsx` — **工作区设置七格从分段控件换成推入式导航**（ADR-0264，#1120，合并 #1115）。抽屉宽 420px 里塞七个 `TabsTrigger`，真机上「智能体」「连接器」已经挤到快认不出，而整页视觉最响的元素是页头右上角那颗红色「解散工作区」——**最危险、最少用的那一个占了锚点**（已搬到最底下、红字、单独一组，`confirm()` 二次确认不动）。换成目录 + 推入页之后每一格有多宽由它自己说了算、目录那层能给每行写一句「里面有什么」（tab 只有两个字）、二级页有完整一屏——于是**三处编辑器（智能体 / 添加主机 / 记忆）从弹窗变成推入页**，#997/#998 那套「DialogContent 没有 max-h/overflow 兜底」的账在这三处自动清掉。代价明写在文件头：**换一格从一次点击变成两次**（设置面低频，人来这儿通常只为改一件事；哪天要在几格之间来回跳就该重判）。目录行那句 hint **只从已经在手的 `ws` 推**——为了给目录写一个数就多打一次网络，等于让「打开抽屉」变贵，而这句话的活是「让人知道该点哪一格」不是报数；它顺带堵掉「同一个事实两份写法」（这轮第一版就是目录写着 `$1.86` 而里面已经是百分比）。动效**自己写一层弹簧不引依赖**（加库是 Tech stack 改动）：Apple 那套两参数口径（response/damping）+ 动量投影 + 越界阻尼，纯的所以进得了单测；不用 CSS transition 是因为定时曲线抓不住半路反向——用户可以在任何时刻抓住一页拖回去，而 transition 改目标时速度会断掉。三个安静出错的地方各钉一条断言：掉帧 dt=2s 时显式欧拉一大步会把弹簧炸出屏幕、两帧挤在同一毫秒能算出荒唐的速度、临界阻尼不许过冲。**副作用不写在 `setState` 的 updater 里**（StrictMode 跑两遍 = 一次返回推两条退场记录）。`prefers-reduced-motion` 下退化成**瞬切**不是「快一点」。列表语言换成分组卡 + **内缩**分隔线（内缩量按组给不按行给——同组前导槽位一致，按行算只会画出参差的线头）+ 组尾解释（挨着它解释的那组东西）；图标底座**中性不上彩色方块**（Apple Settings 那种彩色是故意没抄的一处：本仓的颜色只用来说「出事了」）；行用高亮不用缩放（一列行里某行忽然缩小会打断基线），按钮反之；能点的行**是 div + 一层铺满的透明按钮**不是一颗 `<button>`——行尾还要挂自己的钮而 button 套 button 非法（#1044 拆过一次），代价是读屏念 `label ?? title`。已知代价六条在 ADR-0264，最要紧的三条：保存钮在页脚不在导航条（导航条那格在表单组件外面渲染，要给 NavStack 加「每页自己的 provider」）、行尾动作仍是常驻文字钮不是左划露出、**真机一次都没跑过**。**左缘右划的指针捕获要等手势过了迟滞（8px）才拿，不在 pointerdown 拿**（#1137）：在 pointerdown 就 `setPointerCapture` 会把之后的 pointerup 改派到 section，而 click 落在 pointerdown / pointerup 两个目标的公共祖先上、返回按钮自己的 onClick 永远收不到——左缘 26px 里正好是那枚 chevron，真机形态是「点返回有时候没反应」（点箭头没反应、点「返回」两个字才有；Chromium 151 上 playwright 逐字复现过，记在 #1137）。jsdom 不做这套改派，用例钉的是机制不是症状；`SideChatWindow` 那两处 pointerdown 捕获不受影响——标题栏拖拽先 `closest("button")` 把按钮排除了，缩放 handle 没有子元素
- `src/renderer/src/lib/workspaceUsageView.ts` / `services/edge/src/usageAttribution.ts` 的 `weekLimitFor` / `billingView.ts` 的 `usedPercentOf` — **工作区用量页不报钱，报「占所有者本周额度的百分之几」**（ADR-0264，#1120）。那一格原来写 `12.3 credit`，而 credit 是一笔钱数（1 credit = 1 美分）、云会话统一走所有者的订阅额度（ADR-0233）——那笔钱在月费里，ADR-0241 已经为 `CostPanel` 判过同一条，ADR-0209/0239 为账号页与浮层定的口径是「报还剩百分之几不报 credit」，这一页是同族里没跟上的那一格。分母是**新加的** `WorkspaceUsage.weekLimitMicro`：`fetchWorkspaceUsage` 本来就把 owner 的订阅行读出来了，`planSnapshotOf` 里就是 `week_limit_micro`，所以是给响应加一格不是新查一次库；**没订阅根本不查 plan 表**（分母无论如何是 null），**档位查不到也回 null 不挑一个顶上**——分母错了那一页每个百分比都跟着错且不报错。解析时**缺席 = null 不是解析失败**（新客户端连老网关那一页还得画得出来），0 与非数也一律当没有（那是个分母，0 会让每个百分比变成 Infinity）。`usedPercentOf` 挂在 `remainingPercent` 旁边：同一把尺子的两头，**一位小数向下取整**的理由也是同一条。行里的条按**本工作区合计**归一化（各行加起来正好是 hero 那个数）不按最大值——那样花得最多的那只常年满格，而一根满格的条会把「一切正常」画得比「快没了」还响；一律中性灰。每只 agent 画脸，**名单里查不到的（已删除 / 未归因）不给脸**——`agentAvatarSrc` 对陌生 id 会按哈希派生一张，画上去等于宣称它还在名册里。**分母缺席时不回落到 credit**，退回「占本工作区本周合计」并在组尾说明换了口径；页顶那格此时只报调用次数，不拿合计当分母硬报 100%。**明确不做**「所有者还剩多少」：那要 owner 跨工作区的全量用量，而账号页已经画着那两扇窗，再画一份就有了同一扇窗两个数的风险（ADR-0209）。要部署 edge 才拿得到分母，部署前走退路、不报错
- `src/renderer/src/components/WorkspaceSessionsTab.tsx` — 工作区设置的「会话」页列**全部**云会话，进行中排在归档上面（ADR-0264 决策 8，#1115）。原来只列归档的、一条都没有时整节不画，于是最常见的情形（有会话、都没归档）是一片空白——而它叫「会话」，还是点 ⚙ 落地的第一格。当初「这里只是归档的去处」的理由（ADR-0217）成立不代表代价付了，同一个判据 #1056 已经下过一次：**一个叫「文件」却一个文件都列不出来的页面是 #722 撒谎的勾的近亲**。「读不到」与「一条都没有」判据分家：`undefined`（从来没成功读到过）vs `[]`（确实没有），中间还要一档 `loading`——否则刚挂载那一帧会对着「还没拉过」喊读不到。侧栏那份不动：那边答「我现在要进哪条」，这边答「这个工作区有哪些」
- `src/renderer/src/components/ui/confirm-dialog.tsx` / `CONTEXT.md` 的「团队 / 工作区」那一条 — **界面上「工作区」改名「团队」**，以及**二次确认从 `window.confirm` 换成 shadcn AlertDialog**（ADR-0265，#1126 / #1127）。改名的唯一难点是仓库里「工作区」是**两个东西**：团队（`workspaces` 那一行：一群人 + 连接器 + 云会话 + agent 名册）与工作区（水獭干活的那个文件夹：本机 cwd、内置 Default、检查点/锁/协作记录，以及云端每团队那一个共用工作目录）——`WorkspaceSettings.tsx` 里「工作区就是水獭干活的文件夹」说的是后者，一次全局 sed 会把它改成一句**人读得懂的假话且不报错**。所以是逐文件判概念（混住两种的四个文件逐行判），范围划在**用户可见文案**（含模型可见的群聊 system 提示词 / 工具描述 / runtime 错误消息）；**代码标识符、DB 表名、既有 ADR、567 处注释一律不动** —— 前两样改了断账断引用（同 ADR-0221「记 id 不记名」），ADR 是历史不是现状，注释以本机义为主、无差别铺会改错本机那批，判据因此写进 CONTEXT.md 而不是代码里。确认弹窗那半推翻了 ADR-0264 明写的「保留 `confirm()`……不新造 AlertDialog 视觉语言」——那条理由本来就站不住，视觉语言早在仓库里（三个登录弹窗在用）。形状是 `useConfirm()` 回 `(opts) => Promise<boolean>`，21 个调用点各多一个 `await`；四条判断里最要紧的两条：**provider 缺席时抛错不回落 `window.confirm`**（那种回落会让「忘了挂 provider」表现成「弹窗长得不对」，而那正是这条要修的事；八个测试文件当场红，各自包了一层），**结算只有 `onOpenChange(false)` 一个出口**（按钮 onClick 只记答案 —— 两处都 settle 的话一次点击摘两个：Radix 跑完调用方 onClick 后自己会关闭，排在后面那个问题连问都没问就被答成「取消」，真踩过）。同一轮里 #1125（NavStack 点第一行没反应）**两条 lane 各修了一遍**，合并前 re-fetch 发现 PR #1128 已经落地同一个修法，两个文件整份取了它的 —— start-of-shift 那道碰撞检查搜的是开工那一刻的 issue 与分支，而 #1128 是之后才出现的，结构上盖不住；接住它的是合并前的 re-fetch（同项目 ADR-0074 对 ADR 撞号的规矩）。已知代价七条在 ADR-0265，最要紧的两条：**`services/` 那半要重新部署 VPS 才生效**（同 ADR-0250）、真机一次都没跑过
- `src/renderer/src/components/ui/dialog.tsx` / `ui/alert-dialog.tsx` / `tests/renderer/dialogOverflow.test.tsx` — **弹窗溢出兜底收进共享组件**（ADR-0279，#998）：`DialogContent`/`AlertDialogContent` 从 `grid` 换成 `flex flex-col` + `max-h-[calc(100dvh-2rem)] overflow-y-auto`——上游默认没有 max-h/overflow，内容超高就朝两头溢出且滑不动，失败是静默的。三层分工：什么都不写的消费方整窗滚（关闭 X 跟滚是已知代价）；想固定头尾的把会长那截包 `min-h-0 overflow-y-auto`——**只有 flex 下这层才缩得动**，grid 的 auto 行在被 max-h 夹住的容器里按 max-content 定死、内层一格都不滚（维护者实测，#998 评论）；自己另有安排的（TrajectoryView 85vh / MemorySettings 4rem）靠 twMerge 后写胜出照旧。断言钉的是契约字面量：兜底类在、`grid` 不在、**消费方自己的 max-h 必须赢**；几何那个数刻意没写死（同 ADR-0236 第 1 条）。`VoiceCallOverlay` 不走 DialogContent，不在射程内
- `src/renderer/src/components/ui/sidebar.tsx` 的 `SidebarMenuAction` — 行尾那颗动作按钮**真居中**（ADR-0245 决策 7，#1044）：上游钉的 `top-1.5` 是按 `h-8` 算的，而本仓的行几乎都是 `h-auto` + 自己的 py（云会话行 26px、好友行 26px、史前会话行两行高），于是它一律偏低。改在共享组件里不改在调用点——改调用点的话每加一行新的 `h-auto` 列表都要重新踩一次，而它不报错，只是歪着。刻意没写断言（同 ADR-0236 第 1 条）
- `src/renderer/src/lib/sandboxApprovalControl.ts` / `src/renderer/src/components/BypassSwitch.tsx` 的 `SandboxApprovalToggle` — **「沙箱内免审」从工作区设置搬进云会话输入框那一行**（ADR-0243，#1029）。判据是**这个设置什么时候被人想起**：它是干活当中踩的刹车，被想起的那一刻人必然正开着会话、正看着一张挡住整个群的卡——所以给它加「先开一条会话」这个前置条件不构成负担，这是 ADR-0234「工作区级设置不该挂在会话上」的一处**说得出理由的例外**（仓库地址是开工之前配一次的东西，那条判据对它照旧成立）。**只搬位置不搬语义**：作用域仍是整个工作区（隔离面是容器，而同工作区所有会话共用一容器一卷，ADR-0232——做成按会话就跟 ADR-0231 否掉的「按 agent 配免审」一样挡不住任何东西，还会把授权人从 owner 放宽到任何在籍成员）。与本地那颗四点不同（作用域 / 只管容器里那两把刀 / 只有 owner / 落库不是内存态）；**2026-09-08 维护者对着真机把标签从「沙箱内免审」改回「免审批」**（ADR-0243 原先刻意不同名并有一条断言钉住，那条断言随之作废），四点差别改由各自 title 承担；两枚共用药丸外形、不共用组件，并排住在 `BypassSwitch.tsx` 里让想合并的人先读到那四点。非 owner **不画开关**画只读事实（点了必然拿到「无权修改」的钮 = #722 那个撒谎的勾），但危险状态**不按人分可见性**——它花的是 owner 的额度、动的是共用的卷。~~开着时输入框上方常驻一行警示~~ **也被同日那次拍板撤掉**：ADR-0231 曾把那句话记成这笔账的缓解措施，维护者判定开着就是开着、警示色药丸自己说；这一行如今只剩「读不到」一种情形（那一格恰恰可能正开着免审，闷着等于把危险藏进 `title`）。顺带修的两处静默失败：`WorkspaceSnapshot.sandboxApproval` 收窄成三态（`null` = **读不到**，那一列走单独一条容错查询，原来兜底成 `"ask"` 会让界面画一枚关着的开关、而 runtime 用 service key 走另一条查询照旧按真值免批——界面与实际反着说且毫无提示；脏值仍回 `"ask"`，「读不到」与「值不认识」分家）、`sessionService` 的 `jobSandboxPolicy` 改成**单向复用**（解析成 `auto` 就丢缓存下次重查 = 关掉立刻生效；解析成**确认的** `ask` 照旧钉住这一轮 = ADR-0231 的「下一轮生效」不变；**「问不出来」是第三种结局、也不钉**——拿它当确认值钉住整轮的话，一次抖动会把这个 `auto` 工作区剩下的每一把刀都翻成要人批。这条要成立，`daemon.ts` 装配那行的 `.catch(() => "ask")` 必须拿掉：吞掉之后「问不出来」与「确认是 ask」在 sessionService 那层形状相同，区分两者的整段代码在**真机接线下一次都跑不到而单测照样绿**），同 ADR-0238「钱只能往下压」是同一条纪律：**没有任何输入能让这一轮比它开始时更松**。已知代价：没有活云会话的工作区摸不到这一格、免审工作区每次真撞门多一次单行查询、`workspaceGroups` 无推送通道所以别人翻了开关这边是陈旧的、自动放行那条 `approval_decision` 在时间线上仍然是藏起来的（ADR-0235 ③）
- `services/runtime/src/dispatch.ts` / `sessionService.ts` 的 `say()` 派活分支（`resolveExplicitTargets` + `dispatchVerdictFor`）— **不 @ 谁的话，谁的活谁接**（ADR-0270，#1153；维护者拍板三条：没人对口归管理员 / 开局卡走同一条路 / 派活失败群里说一声）。人类的话没 @ 任何人时，`say()` 用网关最便宜那款读一遍「名册 + 最近 8 句 + 这句话」（先例 ADR-0237 的 Auto：同样的接线、同样的「判不出来一律回落今天的行为」、同样的照常记账），回编号或 none；挑中的落成**一条** `user_message{mentions, dispatch:"auto"}`，与人亲手 @ 的那条逐字节同一条路——排队/停止/护栏/接力链首（`isHumanOpening` 只看 `mentions && !relay`）全部免费拿到，`dispatch` 只是记号。**否决了接力那种两事件形状**（先落 chat_message 再补隐藏开场白）：chat_message 不带 mentions，正在跑的那一轮会在增量采样里读到并顺手答了，之后补的开场白再给同一只排一个 job = 双答（#934 换个入口重演）；单事件的代价是发送方的消息晚 0.3～1 秒出现（回执等判定，`DISPATCH_TIMEOUT_MS` 5 秒封顶）。**何时不判**：正文里有任何 @ token（含打错的）或 `memberMentions` 非空 = 人已经在指名；agent 的回复不触发派活（自激）。**失败回落改动前的行为**：`mention:true` 回落名单第一只（不带记号、不出声），`mention:false` 落 chat_message + 一句「没派出去（原因），需要谁接的话 @ 一下」；**所有者没订阅是第四档 `skipped`——回落但不出声**（那个团队一只 agent 都起不了 turn，头部那行 blocked 已经在说，逐句再说是噪音；探不到才是 failed）；限速在挑出 n 只之后、落盘之前按 n 问价，被拒时话照落只是没派出去（人亲手 @ 被限速仍整句拒收）。输出要编号不要名字（名字要经过模型的嘴）、封顶 3 只（截断不拒绝）、`max_tokens` 64 给推理模型留余量（真机一次没跑过，这个数要靠真机校）。不新增事件类型、帧不变、协议不进位（旧桌面照普通人话画；只发 `mention` 布尔的客户端走同一条路——今天只有开局卡与冒烟脚本，**手机端还没有云会话客户端**）；**runtime 要重新部署**（#791）
- `services/runtime/src/gitTools.ts` / `src/shared/gitTools.ts` / `services/runtime/src/gitScripts.ts` / `WorkspaceConnectorsTab.tsx` 的「代码仓库」组 — 团队里的 Git 是**凭据 + 三把刀**（#1101～#1105，spec 在 `docs/superpowers/specs/2026-09-08-workspace-git-credentials-design.md`），不再绑一个仓库：`clone_repo`（`--depth 1`；目标目录三态，任何一条都不删文件）/ `git_push`（提交并推到**非默认分支**、不强推、作者署点火的人）/ `create_repo`（只做 GitHub），三把全过审批门、给所有 agent。token 按 host 存在 VPS 的 `git-credentials.json`（所有者在「团队设置 → 连接器 → 代码仓库」加），**只到一次性旁路容器为止**，水獭的容器里从不带凭据（ADR-0200 决策②）——所以 `git fetch --unshallow` 只对公开仓成立，push 永远要 token（没存时 `git_push` 在起容器之前就说去哪儿加，#1206）。**提示词与工具表必须说同一句话**（#1206）：`deriveMessages.ts` 的 `CLOUD_SESSION_TEXT` 曾停在「不允许 git push、凭据用完就烧了」的时代，真机语音通话里「开发」手里有 `git_push` 却照那句告诉用户推不了——模型信的是提示词，不是工具表。改的是 `src/session/`，**云会话要重新部署 runtime 才生效**（#791）
- `src/shared/voiceCall.ts` / `src/shared/agentVoice.ts` / `src/shared/tts.ts` / `services/edge/src/ttsUpstream.ts` / `services/runtime/src/inviteToCallTool.ts` / `src/main/teamVoice.ts` / `src/renderer/src/lib/voiceCall.ts` + `voicePlayer.ts` / `components/VoiceCallBar.tsx` — **团队语音通话**（ADR-0271，#1163，协议 16→17）：微信群语音那种形态——拉几只 agent 进通话，它们之后的回复经 edge `/llm/v1/speech`（`model_route.kind='tts'`，0033）调 MiniMax `speech-2.8-turbo` 读出来。四条判据各有一处：**通话是日志事实**（`voice_call_changed`，空名单 = 结束，投影 `voiceCallOf` 三端一份；本机状态 runtime 读不到，派活限制就做不了）；**通话里只有通话成员参与**（派活候选 / 开局卡回落只在 `callRoster` 里挑，agent @ 了通话外的不接力 + 群里一句 `relayOutsideCallText`，人亲手 @ 了通话外的**先落并集名单再落开场白**；`invite_to_call` 每只都挂、**不过审批门**——维护者拍板口头同意就行，「先问再调」写在 deriveMessages 的通话块里）；**TTS 钱按字符数、听的人付**（`ttsUnits` 汉字 2 其余 1 与官方口径逐字对齐、¥2/万字符按 7.2 折进 `price_out`，`serveTts` 与 chat 的三处差别写在头注——尤其 **HTTP 200 + `base_resp.status_code≠0` 是失败**；没订阅 / 网关不供语音不画语音钮，通话栏照画）；**一段写完就出声**（delta 快照按 `splitBubbles` 切段、完成的段立刻合成、终态补读，已读判据是原文相等不是下标；`VoicePlayer` 全局串行 + 预取一段）。结束 = 全组（二次确认）、静音 = 本机、发起人自动加入、别人点「加入」才听；音色按 agent_id 派生（`fnv1a` 抽到 shared 与头像共用）。已知代价：首段约 3 秒才出声（没做流式 TTS）、人类「谁在听」不落盘、**要跑 0033 + `wrangler secret put MINIMAX_API_KEY` + 部署 edge 与 runtime 才生效**，2026-09-09 真机冒烟已过（#1163 评论）。**拉进通话的先开口**（ADR-0272，#1174）：真机第一次用的形态是「开始通话 → 等 → 沉默」——语音只读回复而 `voice_call_changed` 不起任何一轮；修法是 `setVoiceCall` / `invite_to_call` 落下新名单后对**新增**的每只各落一条带 `greeting` 记号的开场白并入队（`greetNewcomers`，同接力开场白那条路，不新增事件类型），人亲手 @ 通话外的那条不打（他那句话就是开场白）；`call` 帧递 turn 桶问价按新增只数扣，被拒时名单照落、不打招呼、群里一句说清；时间线不画这条正文。不 @ 也能聊不在这条里——ADR-0270 的派活在通话里按职责挑通话成员接。**通话里必须有人应**（ADR-0275，#1183）：真机第二次用的形态是「你能听到吗」被分类器判成闲聊 none、整场沉默——文字群聊里「闲聊没人接」是对的口径，电话里没人应就是坏了。通话里 none / failed 一律落到**最近开口的那只**（`lastSpeakerAmong`，没有就管理员 / 通话第一只），只有一只时**不问分类器**直接派给它（省掉人说完到 agent 开口之间最贵的一段网关往返）；没有通话时一字不变
- `native/MrOttoSpeech/` / `src/main/speechBridge.ts` / `src/renderer/src/lib/voiceMic.ts` / `scripts/build-speech.mjs` — **群语音里人说话走 macOS 本机识别**（ADR-0273，#1176）：真机第二次用的形态是「对着它说话 → 没反应」——ADR-0271 没做 STT。一个 Swift helper（SFSpeechRecognizer + AVAudioEngine，on-device、免费、不过网关，形态同另外两个 helper）：进通话就开麦（常开，维护者拍板），断句在 helper 的 `Endpointer`——识别器连续模式不会自己说「这句完了」，「转写 1.5 秒没再变」= 一句说完 → `final` → 渲染层 `cloudSay(text, [], [])`（不 @ = ADR-0270 派活）；每句收口换新 request（连续模式下音频一直攒着）。**半双工**：agent 在说或队列里还有段 → `speechPause`（判据看 `queued` 不只看 `speaking`：段间会闪一下 null），空了 `speechResume`——不闭麦就是扬声器里它自己的话被录回去再发出去的自激回路。桥**不是请求-响应**（事件自己冒出来），helper 懒起、崩了补 `error` + `listening:false`（#913 失败无声那一族）。权限两道各自说清去哪儿勾；helper 是裸二进制，usage description 用 `-sectcreate __TEXT,__info_plist` 嵌进去，且**启动时 disclaim 成自己的责任进程**（#1180：TCC 沿进程树归到最顶上的 GUI app，dev 下是起 dev 的终端，它没那句就把 helper 杀掉——给 Electron.app 补 plist 没用，它不是顶上那个；`extendInfo` 只是打包态的兜底）。只有 macOS、语言钉 zh-CN。**有系统回声消除就不半双工，人可以插嘴**（ADR-0277，#1184）：helper 开 VPIO（真机探针：扬声器放的 `say` 一个字都没被录回去、人话照常识别）→ `status.aec` 为真时麦真常开，agent 说话时来一片够长（≥3 token）且不像回声（与正在读那句 token 重叠 <70%）的 partial = 插嘴，停播放、这几只这一轮剩下的不读（`interrupted`，turn_ended 清）；断句改能量时钟（`LevelGate` 底噪自适应 + `Endpointer.feedLevel`：停嘴 700 ms（像说完了）/ 2.5 s（没说完）收口，文本静默兜底 1.5 s / 3 s（#1196 真机把边想边说的长句切成了三条，短答只认 ≤3 字）；**开麦时给识别器喂上下文词表**（`speechHints`：agent 名 / 成员名 / 团队名 + 开发常用英文词 → `SFSpeechRecognitionRequest.contextualStrings`，真机「push 到 GitHub 的 main」听成「p到get up的密封值」，#1196））；TTS 按句不按段（`splitSpoken`）；`level` 事件 10 Hz 画声浪。开不了 AEC / 旧 helper 照旧半双工。**agent 的语音由 helper 自己的音频引擎播**（ADR-0280，#1201）：VPIO 一开 macOS 就压低别的 app 的音频（ducking，`.min` 不是零），真机十分钟四次「听不清」；`mic.aec` 为真时 `VoicePlayer.createAudio` 换成 `helperAudio`（字节经主进程落临时文件 → `play{id,path}` → helper 的 AVAudioPlayerNode 挂在识别那同一个 engine 上 → `played`/`playError` 回执），不被压、还是回声消除的远端参考；否则 Web Audio 照旧。**全屏通话视图**（ADR-0278，#1185）：点通话栏（左半整块 / 「全屏」钮）展开 `VoiceCallOverlay`——深色一屏、头像里**有人**（发起人 + 自己；别的成员有没有在听不落盘所以不画，ADR-0271 已知代价）、每格一个状态词（agent：播放器在读 = 在说、`openTurns` 欠回答 = 在想、其余在听；自己：能量门有声 = 在说且外圈按 `mic.level` 撑开）、顶上一句按优先级、底下两行字幕（播放器的 `text` + 识别快照）；控制条与通话栏同一批回调。判据抽在 `src/renderer/src/lib/voiceCallView.ts`（`callTiles` / `callStatusText` / `callStarterUid`），两处画同一份事实
- `services/runtime/src/autoModel.ts` / `services/edge/src/billingQueries.ts` 的 `routesQuery` — **Auto：便宜模型先判一手再决定这一 turn 用哪款**（ADR-0237，#1009）。触发条件只有一个——这只 agent 的型号白名单是**空**的（界面上的 Auto，也是新 agent 的默认）。「哪款更强」这个信息今天在系统里不存在（`BillingMe.models` 只是一串 id，`model_route` 有价没有能力字段），所以**拿价当代理、不加 tier 列**：价目是照厂商价维护的、厂商本来就按能力定价，这个代理自己会保鲜，而加一列是第二份要人手同步的事实。落地是 `routesQuery` 补两个排序键（`priority.asc,price_out_micro_per_m.asc,id.asc`），于是 `me.models` **从便宜到贵有序**——`modelForDifficulty` 拿 `[0]` 与 `at(-1)` 当两档，判据是那条查询不是这里的假设（`tests/edge/billingQueries.test.ts` 钉住）。顺带清了一笔旧账：六行 `priority` 全是 10，只按它排的话同分行之间**未定义顺序**，「没指定型号时用哪款」是碰运气而最贵与最便宜差 21 倍；补键之后**默认款 = 最便宜那款，是个承诺**。**判不出来一律回落到今天的行为**（网络挂了 / 非 2xx / 答案认不出 / 清单不足两款 / 所有者没订阅全部回 null，不加 override）——不猜也不「拿不准就往贵的偏」，同 ADR-0233 拆掉「额度用完改烧自带 key」那条纪律：安静地换一条更贵的路是这套东西最不该有的失败模式。注意这与**分类器自己**的偏向是两回事：提示词里明写「拿不准回 hard」（判错往便宜了走是一整轮白跑，往贵了走只是这轮多花钱），而「没有答案」不是一种答案。覆盖方式是**换一份 spec 交给 `engineFor`**（`{...spec, models:[picked]}`），不是给路由层加参数——`preferredModels` 本来就是每轮现读的那一格，`decideRuntimeRoute` 一个字不改；`SessionServiceDeps.pickAutoModel` 是可选的，缺席 = 行为一字不变。分类调用走同一条网关带同样的 workspace/session/agent 头，**照常落 `usage_event`**——一次用户看不见的模型调用不进账，那本账就不再是「钱的唯一事实」。已知代价：接力链每一棒各判一次；分类器读的是**开场白**不是完整上下文；Auto 够得到最贵那款、今天没有上限
- `src/shared/agentRelay.ts` 的 `RelayBounds` / `promptSafe.ts` 的 `promptSafeBody` / `services/runtime/src/approvalRouter.ts` 的 `setRelayTurn` / `src/model/openaiCompatible.ts` 的 `retryDelayFor` + `markBilling` / `supabaseWorkspacesApi.ts` 与 `services/runtime/src/workspaceMemory.ts` 的版本串 CAS — 工作区多智能体第五批：四批自查留下编号的七条 issue（ADR-0230，#958 #959 #960 #961 #962 #965 #968）。**判据仍然一律挂在自己算得出的事实上**，这一批的形状是「把注释里的天花板变成机制」：`boundedContextEvents` 找不到 user turn 一律退回全量而不是丢段（#961；agentView 的 64 条上限从「压低概率」变成真兜底）；say 先扣一个 say 令牌再查名单（#968；say 桶是「有没有资格开口」的粗闸，turn 桶才是价钱，`onThrottled` 按 (kind,uid) 去重所以 #819「一个时段只记一笔」仍成立）；每 turn 只读日志尾段（#958；两条保守下界在 `notify` 里单调推进，`openTurns`/`openingDepthFor` 单遍，等价性靠 200 份伪随机日志对拍旧实现——一只 agent 本进程第一次 turn 仍全量，engine 侧上下文投影仍全量）；接力 turn 上的**任何**审批 2 分钟超时 + 群里每 job 出声一次（#959；runtime 没有 bypass，bash/write_file/create_agent 同样过审批门，冻结是工具无关的；候选「等审批的 job 让出」留到真机出现连续多次审批各等满 2 分钟再做）；`too_many_inflight` 在 hosted 分支排队约 90 秒再放弃并说人话（#960；错误原文不改——桌面 `humanizeError` 靠原文挖信封；排队次数不计入瞬态重试预算；`MAX_INFLIGHT` 挪到 `src/shared/billing.ts` 三端共用；按档位放宽没做）；正文闸与标签剥不可见字符（#965）；记忆 CAS 换 `updated_at` 原串、桌面保存回新版本串（#962；不加 version 列——要跑 migration）。
- `src/main/memoryNudge.ts` 的 `reviewerInstructionsFrom` / `builtinSubagents.ts` 的 memory-reviewer 两步指令 / `agent.ts` 与 `subagentRunner.ts` 的 `memoryProject` — **本机记忆不上 LLM wiki，先修错档、reviewer 先巡检再新增**（ADR-0269，#1155；对照团队侧 #1140）。真机数据：47 次 memory 调用里 32 次来自 reviewer 子会话，而它一直**没有项目档、也不过点名守卫**——子会话装配不传 `memory`、日志里也没有 `memory_loaded`，`memoryProject` 推出来必然是 null，于是 target 枚举里没有 `project`（ADR-0116 决策 4）、守卫整段跳过（ADR-0143「没有项目根时不设防」），项目事实只能落全局档；全局档 11 条里 7 条是项目事实、3 条与 AGENTS.md 逐字重复。修法是**父会话把自己那份 `memoryProject` 原样递给子会话**（同一份对象，不从 workspace 重新解析——键与路径要出自同一来源），以及 reviewer 指令改成两步：先巡检（错档搬进 PROJECT 时**先 add 后 remove**、与项目指令重复的删、重复合并、过时只在有反证时改——**没有证据不猜不删**，便宜档模型误删一条仍然为真的事实比留一条过期的贵）再新增；任务串附主会话本次注入的项目指令（从同一条日志的 `project_instructions` 渲，不另读盘、不再截）。**不上 wiki 的判据是「每个会话都用得上吗」不是「记忆多不多」**：USER / MEMORY / PROJECT 每会话都用得上，全量注入永远赢、按需读只会漏；只有 TOPIC 无界、且一个会话只碰一两个主题，预定在「顶到上限且每条仍为真」或「用户开始喂领域文档」时换成 wiki 形状的检索层，纯层从团队那份接（对 #1140 的约束：wiki 纯层放 `src/shared/`，IO 两份）。已知代价：恢复出来的子会话（`createChildAgent`）仍没有项目档；巡检的证据纪律只靠提示词
