// 语音断句「扣住 / 合并」那扇窗的时长——渲染层与主进程各用一次，必须是同一个数（#1281，spec §5.4 ⑤）。
//
// 渲染层 utteranceHold.ts 拿它当扣住之后等人接着说的时长；主进程 endpointJudge.ts
// 拿它当真值窗口——final 之后这么久内人有没有接着说，就是回答「扣那么久值不值」。
// 两处量的必须是同一扇窗：分开各写一份常量，靠注释互相指着不是耦合，只是两句
// 谁都不会去核对的话——调一边的人看不见另一边，真值日志核对的其实是渲染层根本
// 没在等的窗口，看着在验证却验证不了任何东西。
//
// 抽到这里是因为主进程不该 import 渲染层（ADR-0001 的后悔药）；同样的处置见
// ADR-0289 把 quotaView 的窗口数学从 renderer/src/lib 抬进 shared。
//
// 取 1.8s：helper 对「没说完」的句子本来就肯等 2.5s。
export const HOLD_MS = 1800;
